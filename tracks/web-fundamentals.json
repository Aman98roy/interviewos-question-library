{
  "schemaVersion": 1,
  "slug": "web-fundamentals",
  "questions": [
    {
      "id": "web-fundamentals-basic-01",
      "level": "basic",
      "kind": "conceptual",
      "prompt": "What is Semantic HTML in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code. Your answer should explain the mental model, vocabulary, and one important limit.",
      "skillsTested": [
        "Semantic HTML",
        "Concept explanation",
        "Basic reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** Semantic HTML is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Use the element whose built-in contract matches the job.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n- **Operation:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n- **Proof:** I would verify it by showing one expected Semantic HTML behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee Semantic HTML provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code. This names a concrete boundary and observable outcome rather than calling Semantic HTML “best practice.”\n\n**Depth and trade-off:** I would keep the vocabulary concrete and use one small example plus one boundary case. That shows what Semantic HTML does and the difference between correct use and accidental success.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Choose elements for purpose, then style them. My final rule is **Use the element whose built-in contract matches the job.**",
        "modelAnswer": "**Direct answer:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n\n**Production use:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n\n**Concrete example:** A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code.\n\n**How to verify it:** Showing one expected Semantic HTML behavior and one boundary case, then connecting both to observable evidence\n\n**Trade-off and limit:** My rule of thumb is “Use the element whose built-in contract matches the job.” I would explain one limit of Semantic HTML instead of only naming it.",
        "explanation": "**Why this answer is correct:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n\n**In plain language:** Use the element whose built-in contract matches the job.\n\n**How it applies in production:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n\n**Evidence to ask for:** Showing one expected Semantic HTML behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Explain the mental model, vocabulary, and one important limit.",
        "keyPoints": [
          "**Remember:** Use the element whose built-in contract matches the job.",
          "Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs.",
          "Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions."
        ],
        "realWorldExample": "🏭 **In production:** A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code.\n\n✅ **Why this is a strong answer:** It connects Semantic HTML to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Semantic HTML without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-basic-04",
      "level": "basic",
      "kind": "conceptual",
      "prompt": "What is CSS layout in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks. Your answer should explain the mental model, vocabulary, and one important limit.",
      "skillsTested": [
        "CSS layout",
        "Concept explanation",
        "Basic reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** CSS layout is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Let content size first; constrain only where necessary.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n- **Operation:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n- **Proof:** I would verify it by showing one expected CSS layout behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee CSS layout provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks. This names a concrete boundary and observable outcome rather than calling CSS layout “best practice.”\n\n**Depth and trade-off:** I would keep the vocabulary concrete and use one small example plus one boundary case. That shows what CSS layout does and the difference between correct use and accidental success.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Responsive design should follow content breakpoints rather than device names. My final rule is **Let content size first; constrain only where necessary.**",
        "modelAnswer": "**Direct answer:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n\n**Production use:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n\n**Concrete example:** A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks.\n\n**How to verify it:** Showing one expected CSS layout behavior and one boundary case, then connecting both to observable evidence\n\n**Trade-off and limit:** My rule of thumb is “Let content size first; constrain only where necessary.” I would explain one limit of CSS layout instead of only naming it.",
        "explanation": "**Why this answer is correct:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n\n**In plain language:** Let content size first; constrain only where necessary.\n\n**How it applies in production:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n\n**Evidence to ask for:** Showing one expected CSS layout behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Explain the mental model, vocabulary, and one important limit.",
        "keyPoints": [
          "**Remember:** Let content size first; constrain only where necessary.",
          "CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space.",
          "Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions."
        ],
        "realWorldExample": "🏭 **In production:** A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks.\n\n✅ **Why this is a strong answer:** It connects CSS layout to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming CSS layout without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-basic-07",
      "level": "basic",
      "kind": "conceptual",
      "prompt": "What is Accessibility in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard. Your answer should explain the mental model, vocabulary, and one important limit.",
      "skillsTested": [
        "Accessibility",
        "Concept explanation",
        "Basic reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** Accessibility is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Name it, focus it, announce it, and keep it operable.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n- **Operation:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n- **Proof:** I would verify it by showing one expected Accessibility behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee Accessibility provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard. This names a concrete boundary and observable outcome rather than calling Accessibility “best practice.”\n\n**Depth and trade-off:** I would keep the vocabulary concrete and use one small example plus one boundary case. That shows what Accessibility does and the difference between correct use and accidental success.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** WCAG is a testable baseline, not a substitute for user testing. My final rule is **Name it, focus it, announce it, and keep it operable.**",
        "modelAnswer": "**Direct answer:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n\n**Production use:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n\n**Concrete example:** Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard.\n\n**How to verify it:** Showing one expected Accessibility behavior and one boundary case, then connecting both to observable evidence\n\n**Trade-off and limit:** My rule of thumb is “Name it, focus it, announce it, and keep it operable.” I would explain one limit of Accessibility instead of only naming it.",
        "explanation": "**Why this answer is correct:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n\n**In plain language:** Name it, focus it, announce it, and keep it operable.\n\n**How it applies in production:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n\n**Evidence to ask for:** Showing one expected Accessibility behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Explain the mental model, vocabulary, and one important limit.",
        "keyPoints": [
          "**Remember:** Name it, focus it, announce it, and keep it operable.",
          "Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods.",
          "Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size."
        ],
        "realWorldExample": "🏭 **In production:** Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard.\n\n✅ **Why this is a strong answer:** It connects Accessibility to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Accessibility without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-basic-10",
      "level": "basic",
      "kind": "conceptual",
      "prompt": "What is HTTP and browsers in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck. Your answer should explain the mental model, vocabulary, and one important limit.",
      "skillsTested": [
        "HTTP and browsers",
        "Concept explanation",
        "Basic reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** HTTP and browsers is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Follow the request from name lookup to pixels.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n- **Operation:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n- **Proof:** I would verify it by showing one expected HTTP and browsers behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee HTTP and browsers provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck. This names a concrete boundary and observable outcome rather than calling HTTP and browsers “best practice.”\n\n**Depth and trade-off:** I would keep the vocabulary concrete and use one small example plus one boundary case. That shows what HTTP and browsers does and the difference between correct use and accidental success.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Caching and connection reuse can matter more than JavaScript micro-optimizations. My final rule is **Follow the request from name lookup to pixels.**",
        "modelAnswer": "**Direct answer:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n\n**Production use:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n\n**Concrete example:** A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck.\n\n**How to verify it:** Showing one expected HTTP and browsers behavior and one boundary case, then connecting both to observable evidence\n\n**Trade-off and limit:** My rule of thumb is “Follow the request from name lookup to pixels.” I would explain one limit of HTTP and browsers instead of only naming it.",
        "explanation": "**Why this answer is correct:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n\n**In plain language:** Follow the request from name lookup to pixels.\n\n**How it applies in production:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n\n**Evidence to ask for:** Showing one expected HTTP and browsers behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Explain the mental model, vocabulary, and one important limit.",
        "keyPoints": [
          "**Remember:** Follow the request from name lookup to pixels.",
          "A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites.",
          "Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays."
        ],
        "realWorldExample": "🏭 **In production:** A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck.\n\n✅ **Why this is a strong answer:** It connects HTTP and browsers to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming HTTP and browsers without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-basic-13",
      "level": "basic",
      "kind": "conceptual",
      "prompt": "What is Core Web Vitals in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP. Your answer should explain the mental model, vocabulary, and one important limit.",
      "skillsTested": [
        "Core Web Vitals",
        "Concept explanation",
        "Basic reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** Core Web Vitals is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Load the main thing, respond quickly, move nothing unexpectedly.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n- **Operation:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n- **Proof:** I would verify it by showing one expected Core Web Vitals behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee Core Web Vitals provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP. This names a concrete boundary and observable outcome rather than calling Core Web Vitals “best practice.”\n\n**Depth and trade-off:** I would keep the vocabulary concrete and use one small example plus one boundary case. That shows what Core Web Vitals does and the difference between correct use and accidental success.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Field data matters because lab hardware and networks are only controlled samples. My final rule is **Load the main thing, respond quickly, move nothing unexpectedly.**",
        "modelAnswer": "**Direct answer:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n\n**Production use:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n\n**Concrete example:** A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP.\n\n**How to verify it:** Showing one expected Core Web Vitals behavior and one boundary case, then connecting both to observable evidence\n\n**Trade-off and limit:** My rule of thumb is “Load the main thing, respond quickly, move nothing unexpectedly.” I would explain one limit of Core Web Vitals instead of only naming it.",
        "explanation": "**Why this answer is correct:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n\n**In plain language:** Load the main thing, respond quickly, move nothing unexpectedly.\n\n**How it applies in production:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n\n**Evidence to ask for:** Showing one expected Core Web Vitals behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Explain the mental model, vocabulary, and one important limit.",
        "keyPoints": [
          "**Remember:** Load the main thing, respond quickly, move nothing unexpectedly.",
          "Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability.",
          "Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content."
        ],
        "realWorldExample": "🏭 **In production:** A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP.\n\n✅ **Why this is a strong answer:** It connects Core Web Vitals to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Core Web Vitals without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-basic-16",
      "level": "basic",
      "kind": "conceptual",
      "prompt": "What is Forms and validation in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order. Your answer should explain the mental model, vocabulary, and one important limit.",
      "skillsTested": [
        "Forms and validation",
        "Concept explanation",
        "Basic reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** Forms and validation is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Validate near the user for feedback, at the server for trust.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n- **Operation:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n- **Proof:** I would verify it by showing one expected Forms and validation behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee Forms and validation provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order. This names a concrete boundary and observable outcome rather than calling Forms and validation “best practice.”\n\n**Depth and trade-off:** I would keep the vocabulary concrete and use one small example plus one boundary case. That shows what Forms and validation does and the difference between correct use and accidental success.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted. My final rule is **Validate near the user for feedback, at the server for trust.**",
        "modelAnswer": "**Direct answer:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n\n**Production use:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n\n**Concrete example:** A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order.\n\n**How to verify it:** Showing one expected Forms and validation behavior and one boundary case, then connecting both to observable evidence\n\n**Trade-off and limit:** My rule of thumb is “Validate near the user for feedback, at the server for trust.” I would explain one limit of Forms and validation instead of only naming it.",
        "explanation": "**Why this answer is correct:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n\n**In plain language:** Validate near the user for feedback, at the server for trust.\n\n**How it applies in production:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n\n**Evidence to ask for:** Showing one expected Forms and validation behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Explain the mental model, vocabulary, and one important limit.",
        "keyPoints": [
          "**Remember:** Validate near the user for feedback, at the server for trust.",
          "HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility.",
          "Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server."
        ],
        "realWorldExample": "🏭 **In production:** A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order.\n\n✅ **Why this is a strong answer:** It connects Forms and validation to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Forms and validation without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-basic-19",
      "level": "basic",
      "kind": "conceptual",
      "prompt": "What is Browser JavaScript in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke. Your answer should explain the mental model, vocabulary, and one important limit.",
      "skillsTested": [
        "Browser JavaScript",
        "Concept explanation",
        "Basic reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** Browser JavaScript is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **The main thread renders and handles input; do not block it.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n- **Operation:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n- **Proof:** I would verify it by showing one expected Browser JavaScript behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee Browser JavaScript provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke. This names a concrete boundary and observable outcome rather than calling Browser JavaScript “best practice.”\n\n**Depth and trade-off:** I would keep the vocabulary concrete and use one small example plus one boundary case. That shows what Browser JavaScript does and the difference between correct use and accidental success.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness. My final rule is **The main thread renders and handles input; do not block it.**",
        "modelAnswer": "**Direct answer:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n\n**Production use:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n\n**Concrete example:** A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke.\n\n**How to verify it:** Showing one expected Browser JavaScript behavior and one boundary case, then connecting both to observable evidence\n\n**Trade-off and limit:** My rule of thumb is “The main thread renders and handles input; do not block it.” I would explain one limit of Browser JavaScript instead of only naming it.",
        "explanation": "**Why this answer is correct:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n\n**In plain language:** The main thread renders and handles input; do not block it.\n\n**How it applies in production:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n\n**Evidence to ask for:** Showing one expected Browser JavaScript behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Explain the mental model, vocabulary, and one important limit.",
        "keyPoints": [
          "**Remember:** The main thread renders and handles input; do not block it.",
          "Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread.",
          "Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks."
        ],
        "realWorldExample": "🏭 **In production:** A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke.\n\n✅ **Why this is a strong answer:** It connects Browser JavaScript to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Browser JavaScript without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-basic-22",
      "level": "basic",
      "kind": "conceptual",
      "prompt": "What is Web security in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts. Your answer should explain the mental model, vocabulary, and one important limit.",
      "skillsTested": [
        "Web security",
        "Concept explanation",
        "Basic reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** Web security is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Never trust the client; escape output and lock down the browser.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n- **Operation:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n- **Proof:** I would verify it by showing one expected Web security behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee Web security provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts. This names a concrete boundary and observable outcome rather than calling Web security “best practice.”\n\n**Depth and trade-off:** I would keep the vocabulary concrete and use one small example plus one boundary case. That shows what Web security does and the difference between correct use and accidental success.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls. My final rule is **Never trust the client; escape output and lock down the browser.**",
        "modelAnswer": "**Direct answer:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n\n**Production use:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n\n**Concrete example:** A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts.\n\n**How to verify it:** Showing one expected Web security behavior and one boundary case, then connecting both to observable evidence\n\n**Trade-off and limit:** My rule of thumb is “Never trust the client; escape output and lock down the browser.” I would explain one limit of Web security instead of only naming it.",
        "explanation": "**Why this answer is correct:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n\n**In plain language:** Never trust the client; escape output and lock down the browser.\n\n**How it applies in production:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n\n**Evidence to ask for:** Showing one expected Web security behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Explain the mental model, vocabulary, and one important limit.",
        "keyPoints": [
          "**Remember:** Never trust the client; escape output and lock down the browser.",
          "Web security depends on not trusting the client and controlling what the browser is allowed to do.",
          "Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins."
        ],
        "realWorldExample": "🏭 **In production:** A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts.\n\n✅ **Why this is a strong answer:** It connects Web security to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Web security without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-basic-25",
      "level": "basic",
      "kind": "conceptual",
      "prompt": "What is Caching and CDNs in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect. Your answer should explain the mental model, vocabulary, and one important limit.",
      "skillsTested": [
        "Caching and CDNs",
        "Concept explanation",
        "Basic reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** Caching and CDNs is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Cache immutable assets hard; revalidate the documents that change.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n- **Operation:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n- **Proof:** I would verify it by showing one expected Caching and CDNs behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee Caching and CDNs provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect. This names a concrete boundary and observable outcome rather than calling Caching and CDNs “best practice.”\n\n**Depth and trade-off:** I would keep the vocabulary concrete and use one small example plus one boundary case. That shows what Caching and CDNs does and the difference between correct use and accidental success.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely. My final rule is **Cache immutable assets hard; revalidate the documents that change.**",
        "modelAnswer": "**Direct answer:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n\n**Production use:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n\n**Concrete example:** A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect.\n\n**How to verify it:** Showing one expected Caching and CDNs behavior and one boundary case, then connecting both to observable evidence\n\n**Trade-off and limit:** My rule of thumb is “Cache immutable assets hard; revalidate the documents that change.” I would explain one limit of Caching and CDNs instead of only naming it.",
        "explanation": "**Why this answer is correct:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n\n**In plain language:** Cache immutable assets hard; revalidate the documents that change.\n\n**How it applies in production:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n\n**Evidence to ask for:** Showing one expected Caching and CDNs behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Explain the mental model, vocabulary, and one important limit.",
        "keyPoints": [
          "**Remember:** Cache immutable assets hard; revalidate the documents that change.",
          "Browser and CDN caching avoid re-fetching unchanged resources.",
          "Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change."
        ],
        "realWorldExample": "🏭 **In production:** A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect.\n\n✅ **Why this is a strong answer:** It connects Caching and CDNs to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Caching and CDNs without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-basic-28",
      "level": "basic",
      "kind": "conceptual",
      "prompt": "What is Progressive enhancement in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully. Your answer should explain the mental model, vocabulary, and one important limit.",
      "skillsTested": [
        "Progressive enhancement",
        "Concept explanation",
        "Basic reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** Progressive enhancement is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Working baseline first; enhancement is a bonus, not a requirement.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n- **Operation:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n- **Proof:** I would verify it by showing one expected Progressive enhancement behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee Progressive enhancement provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully. This names a concrete boundary and observable outcome rather than calling Progressive enhancement “best practice.”\n\n**Depth and trade-off:** I would keep the vocabulary concrete and use one small example plus one boundary case. That shows what Progressive enhancement does and the difference between correct use and accidental success.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** The page still functions when a script fails, on slow networks, or for assistive technology. My final rule is **Working baseline first; enhancement is a bonus, not a requirement.**",
        "modelAnswer": "**Direct answer:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n\n**Production use:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n\n**Concrete example:** A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully.\n\n**How to verify it:** Showing one expected Progressive enhancement behavior and one boundary case, then connecting both to observable evidence\n\n**Trade-off and limit:** My rule of thumb is “Working baseline first; enhancement is a bonus, not a requirement.” I would explain one limit of Progressive enhancement instead of only naming it.",
        "explanation": "**Why this answer is correct:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n\n**In plain language:** Working baseline first; enhancement is a bonus, not a requirement.\n\n**How it applies in production:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n\n**Evidence to ask for:** Showing one expected Progressive enhancement behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Explain the mental model, vocabulary, and one important limit.",
        "keyPoints": [
          "**Remember:** Working baseline first; enhancement is a bonus, not a requirement.",
          "Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences.",
          "Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled."
        ],
        "realWorldExample": "🏭 **In production:** A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully.\n\n✅ **Why this is a strong answer:** It connects Progressive enhancement to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Progressive enhancement without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-basic-02",
      "level": "basic",
      "kind": "practical",
      "prompt": "How would you use Web Fundamentals to achieve the Semantic HTML outcome below for a commerce page used across mobile devices and assistive technology? Target outcome: A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code. Your answer should show the smallest correct use and how a beginner can verify it.",
      "skillsTested": [
        "Semantic HTML",
        "Implementation and testing",
        "Basic reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would use Semantic HTML through the smallest production-safe configuration that meets the stated outcome. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Use the element whose built-in contract matches the job.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n- **Operation:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n- **Proof:** I would verify it by testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics.\n\n**Implementation:** I begin with the smallest configuration that can meet the stated outcome, keep access and resource scope narrow, and make invalid input or unsupported states fail clearly. I then add the operational controls in the same change: ownership, useful telemetry, bounded resource use, and a recovery path. Each extra component must address a named requirement rather than anticipated future complexity.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code. This names a concrete boundary and observable outcome rather than calling Semantic HTML “best practice.”\n\n**Depth and trade-off:** I would keep the vocabulary concrete and use one small example plus one boundary case. That shows what Semantic HTML does and the difference between correct use and accidental success.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Choose elements for purpose, then style them. My final rule is **Use the element whose built-in contract matches the job.**",
        "modelAnswer": "**Recommended approach:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n\n**Implementation:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n\n**Concrete example:** A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code.\n\n**How to test it:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**Trade-off and limit:** My rule of thumb is “Use the element whose built-in contract matches the job.” I would explain one limit of Semantic HTML instead of only naming it.",
        "explanation": "**Why this answer is correct:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n\n**In plain language:** Use the element whose built-in contract matches the job.\n\n**How it applies in production:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n\n**Evidence to ask for:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**What a complete answer includes:** Show the smallest correct use and how a beginner can verify it.",
        "keyPoints": [
          "**Remember:** Use the element whose built-in contract matches the job.",
          "Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs.",
          "Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions."
        ],
        "realWorldExample": "🏭 **In production:** A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code.\n\n✅ **Why this is a strong answer:** It connects Semantic HTML to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Semantic HTML without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "code": {
          "language": "html",
          "source": "<fieldset>\n  <legend>Delivery speed</legend>\n  <label><input type=\"radio\" name=\"speed\" value=\"standard\"> Standard</label>\n  <label><input type=\"radio\" name=\"speed\" value=\"express\"> Express</label>\n</fieldset>"
        }
      }
    },
    {
      "id": "web-fundamentals-basic-05",
      "level": "basic",
      "kind": "practical",
      "prompt": "How would you use Web Fundamentals to achieve the CSS layout outcome below for a commerce page used across mobile devices and assistive technology? Target outcome: A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks. Your answer should show the smallest correct use and how a beginner can verify it.",
      "skillsTested": [
        "CSS layout",
        "Implementation and testing",
        "Basic reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would use CSS layout through the smallest production-safe configuration that meets the stated outcome. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Let content size first; constrain only where necessary.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n- **Operation:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n- **Proof:** I would verify it by testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics.\n\n**Implementation:** I begin with the smallest configuration that can meet the stated outcome, keep access and resource scope narrow, and make invalid input or unsupported states fail clearly. I then add the operational controls in the same change: ownership, useful telemetry, bounded resource use, and a recovery path. Each extra component must address a named requirement rather than anticipated future complexity.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks. This names a concrete boundary and observable outcome rather than calling CSS layout “best practice.”\n\n**Depth and trade-off:** I would keep the vocabulary concrete and use one small example plus one boundary case. That shows what CSS layout does and the difference between correct use and accidental success.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Responsive design should follow content breakpoints rather than device names. My final rule is **Let content size first; constrain only where necessary.**",
        "modelAnswer": "**Recommended approach:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n\n**Implementation:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n\n**Concrete example:** A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks.\n\n**How to test it:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**Trade-off and limit:** My rule of thumb is “Let content size first; constrain only where necessary.” I would explain one limit of CSS layout instead of only naming it.",
        "explanation": "**Why this answer is correct:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n\n**In plain language:** Let content size first; constrain only where necessary.\n\n**How it applies in production:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n\n**Evidence to ask for:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**What a complete answer includes:** Show the smallest correct use and how a beginner can verify it.",
        "keyPoints": [
          "**Remember:** Let content size first; constrain only where necessary.",
          "CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space.",
          "Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions."
        ],
        "realWorldExample": "🏭 **In production:** A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks.\n\n✅ **Why this is a strong answer:** It connects CSS layout to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming CSS layout without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-basic-08",
      "level": "basic",
      "kind": "practical",
      "prompt": "How would you use Web Fundamentals to achieve the Accessibility outcome below for a commerce page used across mobile devices and assistive technology? Target outcome: Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard. Your answer should show the smallest correct use and how a beginner can verify it.",
      "skillsTested": [
        "Accessibility",
        "Implementation and testing",
        "Basic reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would use Accessibility through the smallest production-safe configuration that meets the stated outcome. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Name it, focus it, announce it, and keep it operable.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n- **Operation:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n- **Proof:** I would verify it by testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics.\n\n**Implementation:** I begin with the smallest configuration that can meet the stated outcome, keep access and resource scope narrow, and make invalid input or unsupported states fail clearly. I then add the operational controls in the same change: ownership, useful telemetry, bounded resource use, and a recovery path. Each extra component must address a named requirement rather than anticipated future complexity.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard. This names a concrete boundary and observable outcome rather than calling Accessibility “best practice.”\n\n**Depth and trade-off:** I would keep the vocabulary concrete and use one small example plus one boundary case. That shows what Accessibility does and the difference between correct use and accidental success.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** WCAG is a testable baseline, not a substitute for user testing. My final rule is **Name it, focus it, announce it, and keep it operable.**",
        "modelAnswer": "**Recommended approach:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n\n**Implementation:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n\n**Concrete example:** Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard.\n\n**How to test it:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**Trade-off and limit:** My rule of thumb is “Name it, focus it, announce it, and keep it operable.” I would explain one limit of Accessibility instead of only naming it.",
        "explanation": "**Why this answer is correct:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n\n**In plain language:** Name it, focus it, announce it, and keep it operable.\n\n**How it applies in production:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n\n**Evidence to ask for:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**What a complete answer includes:** Show the smallest correct use and how a beginner can verify it.",
        "keyPoints": [
          "**Remember:** Name it, focus it, announce it, and keep it operable.",
          "Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods.",
          "Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size."
        ],
        "realWorldExample": "🏭 **In production:** Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard.\n\n✅ **Why this is a strong answer:** It connects Accessibility to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Accessibility without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-basic-11",
      "level": "basic",
      "kind": "practical",
      "prompt": "How would you use Web Fundamentals to achieve the HTTP and browsers outcome below for a commerce page used across mobile devices and assistive technology? Target outcome: A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck. Your answer should show the smallest correct use and how a beginner can verify it.",
      "skillsTested": [
        "HTTP and browsers",
        "Implementation and testing",
        "Basic reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would use HTTP and browsers through the smallest production-safe configuration that meets the stated outcome. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Follow the request from name lookup to pixels.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n- **Operation:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n- **Proof:** I would verify it by testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics.\n\n**Implementation:** I begin with the smallest configuration that can meet the stated outcome, keep access and resource scope narrow, and make invalid input or unsupported states fail clearly. I then add the operational controls in the same change: ownership, useful telemetry, bounded resource use, and a recovery path. Each extra component must address a named requirement rather than anticipated future complexity.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck. This names a concrete boundary and observable outcome rather than calling HTTP and browsers “best practice.”\n\n**Depth and trade-off:** I would keep the vocabulary concrete and use one small example plus one boundary case. That shows what HTTP and browsers does and the difference between correct use and accidental success.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Caching and connection reuse can matter more than JavaScript micro-optimizations. My final rule is **Follow the request from name lookup to pixels.**",
        "modelAnswer": "**Recommended approach:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n\n**Implementation:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n\n**Concrete example:** A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck.\n\n**How to test it:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**Trade-off and limit:** My rule of thumb is “Follow the request from name lookup to pixels.” I would explain one limit of HTTP and browsers instead of only naming it.",
        "explanation": "**Why this answer is correct:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n\n**In plain language:** Follow the request from name lookup to pixels.\n\n**How it applies in production:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n\n**Evidence to ask for:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**What a complete answer includes:** Show the smallest correct use and how a beginner can verify it.",
        "keyPoints": [
          "**Remember:** Follow the request from name lookup to pixels.",
          "A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites.",
          "Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays."
        ],
        "realWorldExample": "🏭 **In production:** A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck.\n\n✅ **Why this is a strong answer:** It connects HTTP and browsers to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming HTTP and browsers without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-basic-14",
      "level": "basic",
      "kind": "practical",
      "prompt": "How would you use Web Fundamentals to achieve the Core Web Vitals outcome below for a commerce page used across mobile devices and assistive technology? Target outcome: A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP. Your answer should show the smallest correct use and how a beginner can verify it.",
      "skillsTested": [
        "Core Web Vitals",
        "Implementation and testing",
        "Basic reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would use Core Web Vitals through the smallest production-safe configuration that meets the stated outcome. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Load the main thing, respond quickly, move nothing unexpectedly.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n- **Operation:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n- **Proof:** I would verify it by testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics.\n\n**Implementation:** I begin with the smallest configuration that can meet the stated outcome, keep access and resource scope narrow, and make invalid input or unsupported states fail clearly. I then add the operational controls in the same change: ownership, useful telemetry, bounded resource use, and a recovery path. Each extra component must address a named requirement rather than anticipated future complexity.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP. This names a concrete boundary and observable outcome rather than calling Core Web Vitals “best practice.”\n\n**Depth and trade-off:** I would keep the vocabulary concrete and use one small example plus one boundary case. That shows what Core Web Vitals does and the difference between correct use and accidental success.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Field data matters because lab hardware and networks are only controlled samples. My final rule is **Load the main thing, respond quickly, move nothing unexpectedly.**",
        "modelAnswer": "**Recommended approach:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n\n**Implementation:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n\n**Concrete example:** A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP.\n\n**How to test it:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**Trade-off and limit:** My rule of thumb is “Load the main thing, respond quickly, move nothing unexpectedly.” I would explain one limit of Core Web Vitals instead of only naming it.",
        "explanation": "**Why this answer is correct:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n\n**In plain language:** Load the main thing, respond quickly, move nothing unexpectedly.\n\n**How it applies in production:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n\n**Evidence to ask for:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**What a complete answer includes:** Show the smallest correct use and how a beginner can verify it.",
        "keyPoints": [
          "**Remember:** Load the main thing, respond quickly, move nothing unexpectedly.",
          "Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability.",
          "Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content."
        ],
        "realWorldExample": "🏭 **In production:** A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP.\n\n✅ **Why this is a strong answer:** It connects Core Web Vitals to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Core Web Vitals without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-basic-17",
      "level": "basic",
      "kind": "practical",
      "prompt": "How would you use Web Fundamentals to achieve the Forms and validation outcome below for a commerce page used across mobile devices and assistive technology? Target outcome: A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order. Your answer should show the smallest correct use and how a beginner can verify it.",
      "skillsTested": [
        "Forms and validation",
        "Implementation and testing",
        "Basic reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would use Forms and validation through the smallest production-safe configuration that meets the stated outcome. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Validate near the user for feedback, at the server for trust.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n- **Operation:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n- **Proof:** I would verify it by testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics.\n\n**Implementation:** I begin with the smallest configuration that can meet the stated outcome, keep access and resource scope narrow, and make invalid input or unsupported states fail clearly. I then add the operational controls in the same change: ownership, useful telemetry, bounded resource use, and a recovery path. Each extra component must address a named requirement rather than anticipated future complexity.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order. This names a concrete boundary and observable outcome rather than calling Forms and validation “best practice.”\n\n**Depth and trade-off:** I would keep the vocabulary concrete and use one small example plus one boundary case. That shows what Forms and validation does and the difference between correct use and accidental success.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted. My final rule is **Validate near the user for feedback, at the server for trust.**",
        "modelAnswer": "**Recommended approach:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n\n**Implementation:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n\n**Concrete example:** A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order.\n\n**How to test it:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**Trade-off and limit:** My rule of thumb is “Validate near the user for feedback, at the server for trust.” I would explain one limit of Forms and validation instead of only naming it.",
        "explanation": "**Why this answer is correct:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n\n**In plain language:** Validate near the user for feedback, at the server for trust.\n\n**How it applies in production:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n\n**Evidence to ask for:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**What a complete answer includes:** Show the smallest correct use and how a beginner can verify it.",
        "keyPoints": [
          "**Remember:** Validate near the user for feedback, at the server for trust.",
          "HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility.",
          "Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server."
        ],
        "realWorldExample": "🏭 **In production:** A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order.\n\n✅ **Why this is a strong answer:** It connects Forms and validation to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Forms and validation without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-basic-20",
      "level": "basic",
      "kind": "practical",
      "prompt": "How would you use Web Fundamentals to achieve the Browser JavaScript outcome below for a commerce page used across mobile devices and assistive technology? Target outcome: A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke. Your answer should show the smallest correct use and how a beginner can verify it.",
      "skillsTested": [
        "Browser JavaScript",
        "Implementation and testing",
        "Basic reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would use Browser JavaScript through the smallest production-safe configuration that meets the stated outcome. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **The main thread renders and handles input; do not block it.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n- **Operation:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n- **Proof:** I would verify it by testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics.\n\n**Implementation:** I begin with the smallest configuration that can meet the stated outcome, keep access and resource scope narrow, and make invalid input or unsupported states fail clearly. I then add the operational controls in the same change: ownership, useful telemetry, bounded resource use, and a recovery path. Each extra component must address a named requirement rather than anticipated future complexity.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke. This names a concrete boundary and observable outcome rather than calling Browser JavaScript “best practice.”\n\n**Depth and trade-off:** I would keep the vocabulary concrete and use one small example plus one boundary case. That shows what Browser JavaScript does and the difference between correct use and accidental success.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness. My final rule is **The main thread renders and handles input; do not block it.**",
        "modelAnswer": "**Recommended approach:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n\n**Implementation:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n\n**Concrete example:** A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke.\n\n**How to test it:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**Trade-off and limit:** My rule of thumb is “The main thread renders and handles input; do not block it.” I would explain one limit of Browser JavaScript instead of only naming it.",
        "explanation": "**Why this answer is correct:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n\n**In plain language:** The main thread renders and handles input; do not block it.\n\n**How it applies in production:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n\n**Evidence to ask for:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**What a complete answer includes:** Show the smallest correct use and how a beginner can verify it.",
        "keyPoints": [
          "**Remember:** The main thread renders and handles input; do not block it.",
          "Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread.",
          "Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks."
        ],
        "realWorldExample": "🏭 **In production:** A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke.\n\n✅ **Why this is a strong answer:** It connects Browser JavaScript to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Browser JavaScript without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-basic-23",
      "level": "basic",
      "kind": "practical",
      "prompt": "How would you use Web Fundamentals to achieve the Web security outcome below for a commerce page used across mobile devices and assistive technology? Target outcome: A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts. Your answer should show the smallest correct use and how a beginner can verify it.",
      "skillsTested": [
        "Web security",
        "Implementation and testing",
        "Basic reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would use Web security through the smallest production-safe configuration that meets the stated outcome. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Never trust the client; escape output and lock down the browser.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n- **Operation:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n- **Proof:** I would verify it by testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics.\n\n**Implementation:** I begin with the smallest configuration that can meet the stated outcome, keep access and resource scope narrow, and make invalid input or unsupported states fail clearly. I then add the operational controls in the same change: ownership, useful telemetry, bounded resource use, and a recovery path. Each extra component must address a named requirement rather than anticipated future complexity.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts. This names a concrete boundary and observable outcome rather than calling Web security “best practice.”\n\n**Depth and trade-off:** I would keep the vocabulary concrete and use one small example plus one boundary case. That shows what Web security does and the difference between correct use and accidental success.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls. My final rule is **Never trust the client; escape output and lock down the browser.**",
        "modelAnswer": "**Recommended approach:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n\n**Implementation:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n\n**Concrete example:** A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts.\n\n**How to test it:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**Trade-off and limit:** My rule of thumb is “Never trust the client; escape output and lock down the browser.” I would explain one limit of Web security instead of only naming it.",
        "explanation": "**Why this answer is correct:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n\n**In plain language:** Never trust the client; escape output and lock down the browser.\n\n**How it applies in production:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n\n**Evidence to ask for:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**What a complete answer includes:** Show the smallest correct use and how a beginner can verify it.",
        "keyPoints": [
          "**Remember:** Never trust the client; escape output and lock down the browser.",
          "Web security depends on not trusting the client and controlling what the browser is allowed to do.",
          "Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins."
        ],
        "realWorldExample": "🏭 **In production:** A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts.\n\n✅ **Why this is a strong answer:** It connects Web security to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Web security without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-basic-26",
      "level": "basic",
      "kind": "practical",
      "prompt": "How would you use Web Fundamentals to achieve the Caching and CDNs outcome below for a commerce page used across mobile devices and assistive technology? Target outcome: A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect. Your answer should show the smallest correct use and how a beginner can verify it.",
      "skillsTested": [
        "Caching and CDNs",
        "Implementation and testing",
        "Basic reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would use Caching and CDNs through the smallest production-safe configuration that meets the stated outcome. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Cache immutable assets hard; revalidate the documents that change.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n- **Operation:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n- **Proof:** I would verify it by testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics.\n\n**Implementation:** I begin with the smallest configuration that can meet the stated outcome, keep access and resource scope narrow, and make invalid input or unsupported states fail clearly. I then add the operational controls in the same change: ownership, useful telemetry, bounded resource use, and a recovery path. Each extra component must address a named requirement rather than anticipated future complexity.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect. This names a concrete boundary and observable outcome rather than calling Caching and CDNs “best practice.”\n\n**Depth and trade-off:** I would keep the vocabulary concrete and use one small example plus one boundary case. That shows what Caching and CDNs does and the difference between correct use and accidental success.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely. My final rule is **Cache immutable assets hard; revalidate the documents that change.**",
        "modelAnswer": "**Recommended approach:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n\n**Implementation:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n\n**Concrete example:** A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect.\n\n**How to test it:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**Trade-off and limit:** My rule of thumb is “Cache immutable assets hard; revalidate the documents that change.” I would explain one limit of Caching and CDNs instead of only naming it.",
        "explanation": "**Why this answer is correct:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n\n**In plain language:** Cache immutable assets hard; revalidate the documents that change.\n\n**How it applies in production:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n\n**Evidence to ask for:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**What a complete answer includes:** Show the smallest correct use and how a beginner can verify it.",
        "keyPoints": [
          "**Remember:** Cache immutable assets hard; revalidate the documents that change.",
          "Browser and CDN caching avoid re-fetching unchanged resources.",
          "Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change."
        ],
        "realWorldExample": "🏭 **In production:** A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect.\n\n✅ **Why this is a strong answer:** It connects Caching and CDNs to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Caching and CDNs without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-basic-29",
      "level": "basic",
      "kind": "practical",
      "prompt": "How would you use Web Fundamentals to achieve the Progressive enhancement outcome below for a commerce page used across mobile devices and assistive technology? Target outcome: A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully. Your answer should show the smallest correct use and how a beginner can verify it.",
      "skillsTested": [
        "Progressive enhancement",
        "Implementation and testing",
        "Basic reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would use Progressive enhancement through the smallest production-safe configuration that meets the stated outcome. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Working baseline first; enhancement is a bonus, not a requirement.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n- **Operation:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n- **Proof:** I would verify it by testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics.\n\n**Implementation:** I begin with the smallest configuration that can meet the stated outcome, keep access and resource scope narrow, and make invalid input or unsupported states fail clearly. I then add the operational controls in the same change: ownership, useful telemetry, bounded resource use, and a recovery path. Each extra component must address a named requirement rather than anticipated future complexity.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully. This names a concrete boundary and observable outcome rather than calling Progressive enhancement “best practice.”\n\n**Depth and trade-off:** I would keep the vocabulary concrete and use one small example plus one boundary case. That shows what Progressive enhancement does and the difference between correct use and accidental success.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** The page still functions when a script fails, on slow networks, or for assistive technology. My final rule is **Working baseline first; enhancement is a bonus, not a requirement.**",
        "modelAnswer": "**Recommended approach:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n\n**Implementation:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n\n**Concrete example:** A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully.\n\n**How to test it:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**Trade-off and limit:** My rule of thumb is “Working baseline first; enhancement is a bonus, not a requirement.” I would explain one limit of Progressive enhancement instead of only naming it.",
        "explanation": "**Why this answer is correct:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n\n**In plain language:** Working baseline first; enhancement is a bonus, not a requirement.\n\n**How it applies in production:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n\n**Evidence to ask for:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**What a complete answer includes:** Show the smallest correct use and how a beginner can verify it.",
        "keyPoints": [
          "**Remember:** Working baseline first; enhancement is a bonus, not a requirement.",
          "Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences.",
          "Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled."
        ],
        "realWorldExample": "🏭 **In production:** A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully.\n\n✅ **Why this is a strong answer:** It connects Progressive enhancement to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Progressive enhancement without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-basic-03",
      "level": "basic",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code. Focusing on Semantic HTML, how would you investigate without guessing? Your answer should recognize a common failure from simple evidence and correct it safely.",
      "skillsTested": [
        "Semantic HTML",
        "Evidence-based diagnosis",
        "Basic reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the Semantic HTML failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Use the element whose built-in contract matches the job.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n- **Operation:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code. This names a concrete boundary and observable outcome rather than calling Semantic HTML “best practice.”\n\n**Depth and trade-off:** I would keep the vocabulary concrete and use one small example plus one boundary case. That shows what Semantic HTML does and the difference between correct use and accidental success.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Choose elements for purpose, then style them. My final rule is **Use the element whose built-in contract matches the job.**",
        "modelAnswer": "**Expected behavior:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** My rule of thumb is “Use the element whose built-in contract matches the job.” I would explain one limit of Semantic HTML instead of only naming it.",
        "explanation": "**Why this answer is correct:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n\n**In plain language:** Use the element whose built-in contract matches the job.\n\n**How it applies in production:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Recognize a common failure from simple evidence and correct it safely.",
        "keyPoints": [
          "**Remember:** Use the element whose built-in contract matches the job.",
          "Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs.",
          "Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions."
        ],
        "realWorldExample": "🏭 **In production:** A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code.\n\n✅ **Why this is a strong answer:** It connects Semantic HTML to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Semantic HTML without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-basic-06",
      "level": "basic",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks. Focusing on CSS layout, how would you investigate without guessing? Your answer should recognize a common failure from simple evidence and correct it safely.",
      "skillsTested": [
        "CSS layout",
        "Evidence-based diagnosis",
        "Basic reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the CSS layout failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Let content size first; constrain only where necessary.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n- **Operation:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks. This names a concrete boundary and observable outcome rather than calling CSS layout “best practice.”\n\n**Depth and trade-off:** I would keep the vocabulary concrete and use one small example plus one boundary case. That shows what CSS layout does and the difference between correct use and accidental success.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Responsive design should follow content breakpoints rather than device names. My final rule is **Let content size first; constrain only where necessary.**",
        "modelAnswer": "**Expected behavior:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** My rule of thumb is “Let content size first; constrain only where necessary.” I would explain one limit of CSS layout instead of only naming it.",
        "explanation": "**Why this answer is correct:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n\n**In plain language:** Let content size first; constrain only where necessary.\n\n**How it applies in production:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Recognize a common failure from simple evidence and correct it safely.",
        "keyPoints": [
          "**Remember:** Let content size first; constrain only where necessary.",
          "CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space.",
          "Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions."
        ],
        "realWorldExample": "🏭 **In production:** A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks.\n\n✅ **Why this is a strong answer:** It connects CSS layout to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming CSS layout without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-basic-09",
      "level": "basic",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard. Focusing on Accessibility, how would you investigate without guessing? Your answer should recognize a common failure from simple evidence and correct it safely.",
      "skillsTested": [
        "Accessibility",
        "Evidence-based diagnosis",
        "Basic reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the Accessibility failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Name it, focus it, announce it, and keep it operable.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n- **Operation:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard. This names a concrete boundary and observable outcome rather than calling Accessibility “best practice.”\n\n**Depth and trade-off:** I would keep the vocabulary concrete and use one small example plus one boundary case. That shows what Accessibility does and the difference between correct use and accidental success.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** WCAG is a testable baseline, not a substitute for user testing. My final rule is **Name it, focus it, announce it, and keep it operable.**",
        "modelAnswer": "**Expected behavior:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** My rule of thumb is “Name it, focus it, announce it, and keep it operable.” I would explain one limit of Accessibility instead of only naming it.",
        "explanation": "**Why this answer is correct:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n\n**In plain language:** Name it, focus it, announce it, and keep it operable.\n\n**How it applies in production:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Recognize a common failure from simple evidence and correct it safely.",
        "keyPoints": [
          "**Remember:** Name it, focus it, announce it, and keep it operable.",
          "Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods.",
          "Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size."
        ],
        "realWorldExample": "🏭 **In production:** Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard.\n\n✅ **Why this is a strong answer:** It connects Accessibility to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Accessibility without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-basic-12",
      "level": "basic",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck. Focusing on HTTP and browsers, how would you investigate without guessing? Your answer should recognize a common failure from simple evidence and correct it safely.",
      "skillsTested": [
        "HTTP and browsers",
        "Evidence-based diagnosis",
        "Basic reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the HTTP and browsers failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Follow the request from name lookup to pixels.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n- **Operation:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck. This names a concrete boundary and observable outcome rather than calling HTTP and browsers “best practice.”\n\n**Depth and trade-off:** I would keep the vocabulary concrete and use one small example plus one boundary case. That shows what HTTP and browsers does and the difference between correct use and accidental success.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Caching and connection reuse can matter more than JavaScript micro-optimizations. My final rule is **Follow the request from name lookup to pixels.**",
        "modelAnswer": "**Expected behavior:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** My rule of thumb is “Follow the request from name lookup to pixels.” I would explain one limit of HTTP and browsers instead of only naming it.",
        "explanation": "**Why this answer is correct:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n\n**In plain language:** Follow the request from name lookup to pixels.\n\n**How it applies in production:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Recognize a common failure from simple evidence and correct it safely.",
        "keyPoints": [
          "**Remember:** Follow the request from name lookup to pixels.",
          "A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites.",
          "Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays."
        ],
        "realWorldExample": "🏭 **In production:** A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck.\n\n✅ **Why this is a strong answer:** It connects HTTP and browsers to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming HTTP and browsers without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-basic-15",
      "level": "basic",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP. Focusing on Core Web Vitals, how would you investigate without guessing? Your answer should recognize a common failure from simple evidence and correct it safely.",
      "skillsTested": [
        "Core Web Vitals",
        "Evidence-based diagnosis",
        "Basic reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the Core Web Vitals failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Load the main thing, respond quickly, move nothing unexpectedly.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n- **Operation:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP. This names a concrete boundary and observable outcome rather than calling Core Web Vitals “best practice.”\n\n**Depth and trade-off:** I would keep the vocabulary concrete and use one small example plus one boundary case. That shows what Core Web Vitals does and the difference between correct use and accidental success.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Field data matters because lab hardware and networks are only controlled samples. My final rule is **Load the main thing, respond quickly, move nothing unexpectedly.**",
        "modelAnswer": "**Expected behavior:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** My rule of thumb is “Load the main thing, respond quickly, move nothing unexpectedly.” I would explain one limit of Core Web Vitals instead of only naming it.",
        "explanation": "**Why this answer is correct:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n\n**In plain language:** Load the main thing, respond quickly, move nothing unexpectedly.\n\n**How it applies in production:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Recognize a common failure from simple evidence and correct it safely.",
        "keyPoints": [
          "**Remember:** Load the main thing, respond quickly, move nothing unexpectedly.",
          "Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability.",
          "Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content."
        ],
        "realWorldExample": "🏭 **In production:** A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP.\n\n✅ **Why this is a strong answer:** It connects Core Web Vitals to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Core Web Vitals without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-basic-18",
      "level": "basic",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order. Focusing on Forms and validation, how would you investigate without guessing? Your answer should recognize a common failure from simple evidence and correct it safely.",
      "skillsTested": [
        "Forms and validation",
        "Evidence-based diagnosis",
        "Basic reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the Forms and validation failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Validate near the user for feedback, at the server for trust.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n- **Operation:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order. This names a concrete boundary and observable outcome rather than calling Forms and validation “best practice.”\n\n**Depth and trade-off:** I would keep the vocabulary concrete and use one small example plus one boundary case. That shows what Forms and validation does and the difference between correct use and accidental success.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted. My final rule is **Validate near the user for feedback, at the server for trust.**",
        "modelAnswer": "**Expected behavior:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** My rule of thumb is “Validate near the user for feedback, at the server for trust.” I would explain one limit of Forms and validation instead of only naming it.",
        "explanation": "**Why this answer is correct:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n\n**In plain language:** Validate near the user for feedback, at the server for trust.\n\n**How it applies in production:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Recognize a common failure from simple evidence and correct it safely.",
        "keyPoints": [
          "**Remember:** Validate near the user for feedback, at the server for trust.",
          "HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility.",
          "Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server."
        ],
        "realWorldExample": "🏭 **In production:** A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order.\n\n✅ **Why this is a strong answer:** It connects Forms and validation to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Forms and validation without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-basic-21",
      "level": "basic",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke. Focusing on Browser JavaScript, how would you investigate without guessing? Your answer should recognize a common failure from simple evidence and correct it safely.",
      "skillsTested": [
        "Browser JavaScript",
        "Evidence-based diagnosis",
        "Basic reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the Browser JavaScript failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **The main thread renders and handles input; do not block it.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n- **Operation:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke. This names a concrete boundary and observable outcome rather than calling Browser JavaScript “best practice.”\n\n**Depth and trade-off:** I would keep the vocabulary concrete and use one small example plus one boundary case. That shows what Browser JavaScript does and the difference between correct use and accidental success.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness. My final rule is **The main thread renders and handles input; do not block it.**",
        "modelAnswer": "**Expected behavior:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** My rule of thumb is “The main thread renders and handles input; do not block it.” I would explain one limit of Browser JavaScript instead of only naming it.",
        "explanation": "**Why this answer is correct:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n\n**In plain language:** The main thread renders and handles input; do not block it.\n\n**How it applies in production:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Recognize a common failure from simple evidence and correct it safely.",
        "keyPoints": [
          "**Remember:** The main thread renders and handles input; do not block it.",
          "Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread.",
          "Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks."
        ],
        "realWorldExample": "🏭 **In production:** A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke.\n\n✅ **Why this is a strong answer:** It connects Browser JavaScript to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Browser JavaScript without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-basic-24",
      "level": "basic",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts. Focusing on Web security, how would you investigate without guessing? Your answer should recognize a common failure from simple evidence and correct it safely.",
      "skillsTested": [
        "Web security",
        "Evidence-based diagnosis",
        "Basic reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the Web security failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Never trust the client; escape output and lock down the browser.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n- **Operation:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts. This names a concrete boundary and observable outcome rather than calling Web security “best practice.”\n\n**Depth and trade-off:** I would keep the vocabulary concrete and use one small example plus one boundary case. That shows what Web security does and the difference between correct use and accidental success.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls. My final rule is **Never trust the client; escape output and lock down the browser.**",
        "modelAnswer": "**Expected behavior:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** My rule of thumb is “Never trust the client; escape output and lock down the browser.” I would explain one limit of Web security instead of only naming it.",
        "explanation": "**Why this answer is correct:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n\n**In plain language:** Never trust the client; escape output and lock down the browser.\n\n**How it applies in production:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Recognize a common failure from simple evidence and correct it safely.",
        "keyPoints": [
          "**Remember:** Never trust the client; escape output and lock down the browser.",
          "Web security depends on not trusting the client and controlling what the browser is allowed to do.",
          "Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins."
        ],
        "realWorldExample": "🏭 **In production:** A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts.\n\n✅ **Why this is a strong answer:** It connects Web security to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Web security without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-basic-27",
      "level": "basic",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect. Focusing on Caching and CDNs, how would you investigate without guessing? Your answer should recognize a common failure from simple evidence and correct it safely.",
      "skillsTested": [
        "Caching and CDNs",
        "Evidence-based diagnosis",
        "Basic reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the Caching and CDNs failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Cache immutable assets hard; revalidate the documents that change.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n- **Operation:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect. This names a concrete boundary and observable outcome rather than calling Caching and CDNs “best practice.”\n\n**Depth and trade-off:** I would keep the vocabulary concrete and use one small example plus one boundary case. That shows what Caching and CDNs does and the difference between correct use and accidental success.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely. My final rule is **Cache immutable assets hard; revalidate the documents that change.**",
        "modelAnswer": "**Expected behavior:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** My rule of thumb is “Cache immutable assets hard; revalidate the documents that change.” I would explain one limit of Caching and CDNs instead of only naming it.",
        "explanation": "**Why this answer is correct:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n\n**In plain language:** Cache immutable assets hard; revalidate the documents that change.\n\n**How it applies in production:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Recognize a common failure from simple evidence and correct it safely.",
        "keyPoints": [
          "**Remember:** Cache immutable assets hard; revalidate the documents that change.",
          "Browser and CDN caching avoid re-fetching unchanged resources.",
          "Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change."
        ],
        "realWorldExample": "🏭 **In production:** A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect.\n\n✅ **Why this is a strong answer:** It connects Caching and CDNs to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Caching and CDNs without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-basic-30",
      "level": "basic",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully. Focusing on Progressive enhancement, how would you investigate without guessing? Your answer should recognize a common failure from simple evidence and correct it safely.",
      "skillsTested": [
        "Progressive enhancement",
        "Evidence-based diagnosis",
        "Basic reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the Progressive enhancement failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Working baseline first; enhancement is a bonus, not a requirement.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n- **Operation:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully. This names a concrete boundary and observable outcome rather than calling Progressive enhancement “best practice.”\n\n**Depth and trade-off:** I would keep the vocabulary concrete and use one small example plus one boundary case. That shows what Progressive enhancement does and the difference between correct use and accidental success.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** The page still functions when a script fails, on slow networks, or for assistive technology. My final rule is **Working baseline first; enhancement is a bonus, not a requirement.**",
        "modelAnswer": "**Expected behavior:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** My rule of thumb is “Working baseline first; enhancement is a bonus, not a requirement.” I would explain one limit of Progressive enhancement instead of only naming it.",
        "explanation": "**Why this answer is correct:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n\n**In plain language:** Working baseline first; enhancement is a bonus, not a requirement.\n\n**How it applies in production:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Recognize a common failure from simple evidence and correct it safely.",
        "keyPoints": [
          "**Remember:** Working baseline first; enhancement is a bonus, not a requirement.",
          "Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences.",
          "Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled."
        ],
        "realWorldExample": "🏭 **In production:** A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully.\n\n✅ **Why this is a strong answer:** It connects Progressive enhancement to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Progressive enhancement without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-intermediate-01",
      "level": "intermediate",
      "kind": "conceptual",
      "prompt": "What is Semantic HTML in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code. Your answer should connect the mechanism to implementation and operational trade-offs.",
      "skillsTested": [
        "Semantic HTML",
        "Concept explanation",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** Semantic HTML is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Use the element whose built-in contract matches the job.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n- **Operation:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n- **Proof:** I would verify it by showing one expected Semantic HTML behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee Semantic HTML provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code. This names a concrete boundary and observable outcome rather than calling Semantic HTML “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Choose elements for purpose, then style them. My final rule is **Use the element whose built-in contract matches the job.**",
        "modelAnswer": "**Direct answer:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n\n**Production use:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n\n**Concrete example:** A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code.\n\n**How to verify it:** Showing one expected Semantic HTML behavior and one boundary case, then connecting both to observable evidence\n\n**Trade-off and limit:** Before shipping, I would make Semantic HTML ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions.",
        "explanation": "**Why this answer is correct:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n\n**In plain language:** Use the element whose built-in contract matches the job.\n\n**How it applies in production:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n\n**Evidence to ask for:** Showing one expected Semantic HTML behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Connect the mechanism to implementation and operational trade-offs.",
        "keyPoints": [
          "**Remember:** Use the element whose built-in contract matches the job.",
          "Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs.",
          "Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions."
        ],
        "realWorldExample": "🏭 **In production:** A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code.\n\n✅ **Why this is a strong answer:** It connects Semantic HTML to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Semantic HTML without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-intermediate-05",
      "level": "intermediate",
      "kind": "conceptual",
      "prompt": "What is CSS layout in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks. Your answer should connect the mechanism to implementation and operational trade-offs.",
      "skillsTested": [
        "CSS layout",
        "Concept explanation",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** CSS layout is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Let content size first; constrain only where necessary.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n- **Operation:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n- **Proof:** I would verify it by showing one expected CSS layout behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee CSS layout provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks. This names a concrete boundary and observable outcome rather than calling CSS layout “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Responsive design should follow content breakpoints rather than device names. My final rule is **Let content size first; constrain only where necessary.**",
        "modelAnswer": "**Direct answer:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n\n**Production use:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n\n**Concrete example:** A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks.\n\n**How to verify it:** Showing one expected CSS layout behavior and one boundary case, then connecting both to observable evidence\n\n**Trade-off and limit:** Before shipping, I would make CSS layout ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.",
        "explanation": "**Why this answer is correct:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n\n**In plain language:** Let content size first; constrain only where necessary.\n\n**How it applies in production:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n\n**Evidence to ask for:** Showing one expected CSS layout behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Connect the mechanism to implementation and operational trade-offs.",
        "keyPoints": [
          "**Remember:** Let content size first; constrain only where necessary.",
          "CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space.",
          "Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions."
        ],
        "realWorldExample": "🏭 **In production:** A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks.\n\n✅ **Why this is a strong answer:** It connects CSS layout to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming CSS layout without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-intermediate-09",
      "level": "intermediate",
      "kind": "conceptual",
      "prompt": "What is Accessibility in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard. Your answer should connect the mechanism to implementation and operational trade-offs.",
      "skillsTested": [
        "Accessibility",
        "Concept explanation",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** Accessibility is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Name it, focus it, announce it, and keep it operable.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n- **Operation:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n- **Proof:** I would verify it by showing one expected Accessibility behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee Accessibility provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard. This names a concrete boundary and observable outcome rather than calling Accessibility “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** WCAG is a testable baseline, not a substitute for user testing. My final rule is **Name it, focus it, announce it, and keep it operable.**",
        "modelAnswer": "**Direct answer:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n\n**Production use:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n\n**Concrete example:** Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard.\n\n**How to verify it:** Showing one expected Accessibility behavior and one boundary case, then connecting both to observable evidence\n\n**Trade-off and limit:** Before shipping, I would make Accessibility ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size.",
        "explanation": "**Why this answer is correct:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n\n**In plain language:** Name it, focus it, announce it, and keep it operable.\n\n**How it applies in production:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n\n**Evidence to ask for:** Showing one expected Accessibility behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Connect the mechanism to implementation and operational trade-offs.",
        "keyPoints": [
          "**Remember:** Name it, focus it, announce it, and keep it operable.",
          "Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods.",
          "Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size."
        ],
        "realWorldExample": "🏭 **In production:** Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard.\n\n✅ **Why this is a strong answer:** It connects Accessibility to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Accessibility without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-intermediate-13",
      "level": "intermediate",
      "kind": "conceptual",
      "prompt": "What is HTTP and browsers in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck. Your answer should connect the mechanism to implementation and operational trade-offs.",
      "skillsTested": [
        "HTTP and browsers",
        "Concept explanation",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** HTTP and browsers is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Follow the request from name lookup to pixels.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n- **Operation:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n- **Proof:** I would verify it by showing one expected HTTP and browsers behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee HTTP and browsers provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck. This names a concrete boundary and observable outcome rather than calling HTTP and browsers “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Caching and connection reuse can matter more than JavaScript micro-optimizations. My final rule is **Follow the request from name lookup to pixels.**",
        "modelAnswer": "**Direct answer:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n\n**Production use:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n\n**Concrete example:** A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck.\n\n**How to verify it:** Showing one expected HTTP and browsers behavior and one boundary case, then connecting both to observable evidence\n\n**Trade-off and limit:** Before shipping, I would make HTTP and browsers ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays.",
        "explanation": "**Why this answer is correct:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n\n**In plain language:** Follow the request from name lookup to pixels.\n\n**How it applies in production:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n\n**Evidence to ask for:** Showing one expected HTTP and browsers behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Connect the mechanism to implementation and operational trade-offs.",
        "keyPoints": [
          "**Remember:** Follow the request from name lookup to pixels.",
          "A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites.",
          "Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays."
        ],
        "realWorldExample": "🏭 **In production:** A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck.\n\n✅ **Why this is a strong answer:** It connects HTTP and browsers to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming HTTP and browsers without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-intermediate-17",
      "level": "intermediate",
      "kind": "conceptual",
      "prompt": "What is Core Web Vitals in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP. Your answer should connect the mechanism to implementation and operational trade-offs.",
      "skillsTested": [
        "Core Web Vitals",
        "Concept explanation",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** Core Web Vitals is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Load the main thing, respond quickly, move nothing unexpectedly.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n- **Operation:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n- **Proof:** I would verify it by showing one expected Core Web Vitals behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee Core Web Vitals provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP. This names a concrete boundary and observable outcome rather than calling Core Web Vitals “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Field data matters because lab hardware and networks are only controlled samples. My final rule is **Load the main thing, respond quickly, move nothing unexpectedly.**",
        "modelAnswer": "**Direct answer:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n\n**Production use:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n\n**Concrete example:** A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP.\n\n**How to verify it:** Showing one expected Core Web Vitals behavior and one boundary case, then connecting both to observable evidence\n\n**Trade-off and limit:** Before shipping, I would make Core Web Vitals ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content.",
        "explanation": "**Why this answer is correct:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n\n**In plain language:** Load the main thing, respond quickly, move nothing unexpectedly.\n\n**How it applies in production:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n\n**Evidence to ask for:** Showing one expected Core Web Vitals behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Connect the mechanism to implementation and operational trade-offs.",
        "keyPoints": [
          "**Remember:** Load the main thing, respond quickly, move nothing unexpectedly.",
          "Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability.",
          "Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content."
        ],
        "realWorldExample": "🏭 **In production:** A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP.\n\n✅ **Why this is a strong answer:** It connects Core Web Vitals to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Core Web Vitals without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-intermediate-21",
      "level": "intermediate",
      "kind": "conceptual",
      "prompt": "What is Forms and validation in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order. Your answer should connect the mechanism to implementation and operational trade-offs.",
      "skillsTested": [
        "Forms and validation",
        "Concept explanation",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** Forms and validation is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Validate near the user for feedback, at the server for trust.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n- **Operation:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n- **Proof:** I would verify it by showing one expected Forms and validation behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee Forms and validation provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order. This names a concrete boundary and observable outcome rather than calling Forms and validation “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted. My final rule is **Validate near the user for feedback, at the server for trust.**",
        "modelAnswer": "**Direct answer:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n\n**Production use:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n\n**Concrete example:** A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order.\n\n**How to verify it:** Showing one expected Forms and validation behavior and one boundary case, then connecting both to observable evidence\n\n**Trade-off and limit:** Before shipping, I would make Forms and validation ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.",
        "explanation": "**Why this answer is correct:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n\n**In plain language:** Validate near the user for feedback, at the server for trust.\n\n**How it applies in production:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n\n**Evidence to ask for:** Showing one expected Forms and validation behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Connect the mechanism to implementation and operational trade-offs.",
        "keyPoints": [
          "**Remember:** Validate near the user for feedback, at the server for trust.",
          "HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility.",
          "Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server."
        ],
        "realWorldExample": "🏭 **In production:** A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order.\n\n✅ **Why this is a strong answer:** It connects Forms and validation to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Forms and validation without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-intermediate-25",
      "level": "intermediate",
      "kind": "conceptual",
      "prompt": "What is Browser JavaScript in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke. Your answer should connect the mechanism to implementation and operational trade-offs.",
      "skillsTested": [
        "Browser JavaScript",
        "Concept explanation",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** Browser JavaScript is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **The main thread renders and handles input; do not block it.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n- **Operation:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n- **Proof:** I would verify it by showing one expected Browser JavaScript behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee Browser JavaScript provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke. This names a concrete boundary and observable outcome rather than calling Browser JavaScript “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness. My final rule is **The main thread renders and handles input; do not block it.**",
        "modelAnswer": "**Direct answer:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n\n**Production use:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n\n**Concrete example:** A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke.\n\n**How to verify it:** Showing one expected Browser JavaScript behavior and one boundary case, then connecting both to observable evidence\n\n**Trade-off and limit:** Before shipping, I would make Browser JavaScript ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.",
        "explanation": "**Why this answer is correct:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n\n**In plain language:** The main thread renders and handles input; do not block it.\n\n**How it applies in production:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n\n**Evidence to ask for:** Showing one expected Browser JavaScript behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Connect the mechanism to implementation and operational trade-offs.",
        "keyPoints": [
          "**Remember:** The main thread renders and handles input; do not block it.",
          "Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread.",
          "Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks."
        ],
        "realWorldExample": "🏭 **In production:** A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke.\n\n✅ **Why this is a strong answer:** It connects Browser JavaScript to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Browser JavaScript without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-intermediate-29",
      "level": "intermediate",
      "kind": "conceptual",
      "prompt": "What is Web security in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts. Your answer should connect the mechanism to implementation and operational trade-offs.",
      "skillsTested": [
        "Web security",
        "Concept explanation",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** Web security is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Never trust the client; escape output and lock down the browser.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n- **Operation:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n- **Proof:** I would verify it by showing one expected Web security behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee Web security provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts. This names a concrete boundary and observable outcome rather than calling Web security “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls. My final rule is **Never trust the client; escape output and lock down the browser.**",
        "modelAnswer": "**Direct answer:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n\n**Production use:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n\n**Concrete example:** A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts.\n\n**How to verify it:** Showing one expected Web security behavior and one boundary case, then connecting both to observable evidence\n\n**Trade-off and limit:** Before shipping, I would make Web security ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.",
        "explanation": "**Why this answer is correct:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n\n**In plain language:** Never trust the client; escape output and lock down the browser.\n\n**How it applies in production:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n\n**Evidence to ask for:** Showing one expected Web security behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Connect the mechanism to implementation and operational trade-offs.",
        "keyPoints": [
          "**Remember:** Never trust the client; escape output and lock down the browser.",
          "Web security depends on not trusting the client and controlling what the browser is allowed to do.",
          "Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins."
        ],
        "realWorldExample": "🏭 **In production:** A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts.\n\n✅ **Why this is a strong answer:** It connects Web security to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Web security without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-intermediate-33",
      "level": "intermediate",
      "kind": "conceptual",
      "prompt": "What is Caching and CDNs in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect. Your answer should connect the mechanism to implementation and operational trade-offs.",
      "skillsTested": [
        "Caching and CDNs",
        "Concept explanation",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** Caching and CDNs is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Cache immutable assets hard; revalidate the documents that change.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n- **Operation:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n- **Proof:** I would verify it by showing one expected Caching and CDNs behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee Caching and CDNs provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect. This names a concrete boundary and observable outcome rather than calling Caching and CDNs “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely. My final rule is **Cache immutable assets hard; revalidate the documents that change.**",
        "modelAnswer": "**Direct answer:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n\n**Production use:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n\n**Concrete example:** A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect.\n\n**How to verify it:** Showing one expected Caching and CDNs behavior and one boundary case, then connecting both to observable evidence\n\n**Trade-off and limit:** Before shipping, I would make Caching and CDNs ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.",
        "explanation": "**Why this answer is correct:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n\n**In plain language:** Cache immutable assets hard; revalidate the documents that change.\n\n**How it applies in production:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n\n**Evidence to ask for:** Showing one expected Caching and CDNs behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Connect the mechanism to implementation and operational trade-offs.",
        "keyPoints": [
          "**Remember:** Cache immutable assets hard; revalidate the documents that change.",
          "Browser and CDN caching avoid re-fetching unchanged resources.",
          "Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change."
        ],
        "realWorldExample": "🏭 **In production:** A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect.\n\n✅ **Why this is a strong answer:** It connects Caching and CDNs to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Caching and CDNs without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-intermediate-37",
      "level": "intermediate",
      "kind": "conceptual",
      "prompt": "What is Progressive enhancement in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully. Your answer should connect the mechanism to implementation and operational trade-offs.",
      "skillsTested": [
        "Progressive enhancement",
        "Concept explanation",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** Progressive enhancement is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Working baseline first; enhancement is a bonus, not a requirement.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n- **Operation:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n- **Proof:** I would verify it by showing one expected Progressive enhancement behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee Progressive enhancement provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully. This names a concrete boundary and observable outcome rather than calling Progressive enhancement “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** The page still functions when a script fails, on slow networks, or for assistive technology. My final rule is **Working baseline first; enhancement is a bonus, not a requirement.**",
        "modelAnswer": "**Direct answer:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n\n**Production use:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n\n**Concrete example:** A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully.\n\n**How to verify it:** Showing one expected Progressive enhancement behavior and one boundary case, then connecting both to observable evidence\n\n**Trade-off and limit:** Before shipping, I would make Progressive enhancement ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.",
        "explanation": "**Why this answer is correct:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n\n**In plain language:** Working baseline first; enhancement is a bonus, not a requirement.\n\n**How it applies in production:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n\n**Evidence to ask for:** Showing one expected Progressive enhancement behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Connect the mechanism to implementation and operational trade-offs.",
        "keyPoints": [
          "**Remember:** Working baseline first; enhancement is a bonus, not a requirement.",
          "Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences.",
          "Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled."
        ],
        "realWorldExample": "🏭 **In production:** A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully.\n\n✅ **Why this is a strong answer:** It connects Progressive enhancement to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Progressive enhancement without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-intermediate-02",
      "level": "intermediate",
      "kind": "practical",
      "prompt": "How would you use Web Fundamentals to achieve the Semantic HTML outcome below for a commerce page used across mobile devices and assistive technology? Target outcome: A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code. Your answer should implement it with validation, security, observability, and failure handling.",
      "skillsTested": [
        "Semantic HTML",
        "Implementation and testing",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would use Semantic HTML through the smallest production-safe configuration that meets the stated outcome. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Use the element whose built-in contract matches the job.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n- **Operation:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n- **Proof:** I would verify it by testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics.\n\n**Implementation:** I begin with the smallest configuration that can meet the stated outcome, keep access and resource scope narrow, and make invalid input or unsupported states fail clearly. I then add the operational controls in the same change: ownership, useful telemetry, bounded resource use, and a recovery path. Each extra component must address a named requirement rather than anticipated future complexity.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code. This names a concrete boundary and observable outcome rather than calling Semantic HTML “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Choose elements for purpose, then style them. My final rule is **Use the element whose built-in contract matches the job.**",
        "modelAnswer": "**Recommended approach:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n\n**Implementation:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n\n**Concrete example:** A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code.\n\n**How to test it:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**Trade-off and limit:** Before shipping, I would make Semantic HTML ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions.",
        "explanation": "**Why this answer is correct:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n\n**In plain language:** Use the element whose built-in contract matches the job.\n\n**How it applies in production:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n\n**Evidence to ask for:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**What a complete answer includes:** Implement it with validation, security, observability, and failure handling.",
        "keyPoints": [
          "**Remember:** Use the element whose built-in contract matches the job.",
          "Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs.",
          "Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions."
        ],
        "realWorldExample": "🏭 **In production:** A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code.\n\n✅ **Why this is a strong answer:** It connects Semantic HTML to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Semantic HTML without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "code": {
          "language": "html",
          "source": "<fieldset>\n  <legend>Delivery speed</legend>\n  <label><input type=\"radio\" name=\"speed\" value=\"standard\"> Standard</label>\n  <label><input type=\"radio\" name=\"speed\" value=\"express\"> Express</label>\n</fieldset>"
        }
      }
    },
    {
      "id": "web-fundamentals-intermediate-06",
      "level": "intermediate",
      "kind": "practical",
      "prompt": "How would you use Web Fundamentals to achieve the CSS layout outcome below for a commerce page used across mobile devices and assistive technology? Target outcome: A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks. Your answer should implement it with validation, security, observability, and failure handling.",
      "skillsTested": [
        "CSS layout",
        "Implementation and testing",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would use CSS layout through the smallest production-safe configuration that meets the stated outcome. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Let content size first; constrain only where necessary.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n- **Operation:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n- **Proof:** I would verify it by testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics.\n\n**Implementation:** I begin with the smallest configuration that can meet the stated outcome, keep access and resource scope narrow, and make invalid input or unsupported states fail clearly. I then add the operational controls in the same change: ownership, useful telemetry, bounded resource use, and a recovery path. Each extra component must address a named requirement rather than anticipated future complexity.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks. This names a concrete boundary and observable outcome rather than calling CSS layout “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Responsive design should follow content breakpoints rather than device names. My final rule is **Let content size first; constrain only where necessary.**",
        "modelAnswer": "**Recommended approach:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n\n**Implementation:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n\n**Concrete example:** A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks.\n\n**How to test it:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**Trade-off and limit:** Before shipping, I would make CSS layout ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.",
        "explanation": "**Why this answer is correct:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n\n**In plain language:** Let content size first; constrain only where necessary.\n\n**How it applies in production:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n\n**Evidence to ask for:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**What a complete answer includes:** Implement it with validation, security, observability, and failure handling.",
        "keyPoints": [
          "**Remember:** Let content size first; constrain only where necessary.",
          "CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space.",
          "Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions."
        ],
        "realWorldExample": "🏭 **In production:** A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks.\n\n✅ **Why this is a strong answer:** It connects CSS layout to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming CSS layout without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-intermediate-10",
      "level": "intermediate",
      "kind": "practical",
      "prompt": "How would you use Web Fundamentals to achieve the Accessibility outcome below for a commerce page used across mobile devices and assistive technology? Target outcome: Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard. Your answer should implement it with validation, security, observability, and failure handling.",
      "skillsTested": [
        "Accessibility",
        "Implementation and testing",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would use Accessibility through the smallest production-safe configuration that meets the stated outcome. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Name it, focus it, announce it, and keep it operable.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n- **Operation:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n- **Proof:** I would verify it by testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics.\n\n**Implementation:** I begin with the smallest configuration that can meet the stated outcome, keep access and resource scope narrow, and make invalid input or unsupported states fail clearly. I then add the operational controls in the same change: ownership, useful telemetry, bounded resource use, and a recovery path. Each extra component must address a named requirement rather than anticipated future complexity.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard. This names a concrete boundary and observable outcome rather than calling Accessibility “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** WCAG is a testable baseline, not a substitute for user testing. My final rule is **Name it, focus it, announce it, and keep it operable.**",
        "modelAnswer": "**Recommended approach:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n\n**Implementation:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n\n**Concrete example:** Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard.\n\n**How to test it:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**Trade-off and limit:** Before shipping, I would make Accessibility ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size.",
        "explanation": "**Why this answer is correct:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n\n**In plain language:** Name it, focus it, announce it, and keep it operable.\n\n**How it applies in production:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n\n**Evidence to ask for:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**What a complete answer includes:** Implement it with validation, security, observability, and failure handling.",
        "keyPoints": [
          "**Remember:** Name it, focus it, announce it, and keep it operable.",
          "Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods.",
          "Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size."
        ],
        "realWorldExample": "🏭 **In production:** Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard.\n\n✅ **Why this is a strong answer:** It connects Accessibility to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Accessibility without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-intermediate-14",
      "level": "intermediate",
      "kind": "practical",
      "prompt": "How would you use Web Fundamentals to achieve the HTTP and browsers outcome below for a commerce page used across mobile devices and assistive technology? Target outcome: A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck. Your answer should implement it with validation, security, observability, and failure handling.",
      "skillsTested": [
        "HTTP and browsers",
        "Implementation and testing",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would use HTTP and browsers through the smallest production-safe configuration that meets the stated outcome. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Follow the request from name lookup to pixels.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n- **Operation:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n- **Proof:** I would verify it by testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics.\n\n**Implementation:** I begin with the smallest configuration that can meet the stated outcome, keep access and resource scope narrow, and make invalid input or unsupported states fail clearly. I then add the operational controls in the same change: ownership, useful telemetry, bounded resource use, and a recovery path. Each extra component must address a named requirement rather than anticipated future complexity.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck. This names a concrete boundary and observable outcome rather than calling HTTP and browsers “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Caching and connection reuse can matter more than JavaScript micro-optimizations. My final rule is **Follow the request from name lookup to pixels.**",
        "modelAnswer": "**Recommended approach:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n\n**Implementation:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n\n**Concrete example:** A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck.\n\n**How to test it:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**Trade-off and limit:** Before shipping, I would make HTTP and browsers ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays.",
        "explanation": "**Why this answer is correct:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n\n**In plain language:** Follow the request from name lookup to pixels.\n\n**How it applies in production:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n\n**Evidence to ask for:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**What a complete answer includes:** Implement it with validation, security, observability, and failure handling.",
        "keyPoints": [
          "**Remember:** Follow the request from name lookup to pixels.",
          "A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites.",
          "Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays."
        ],
        "realWorldExample": "🏭 **In production:** A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck.\n\n✅ **Why this is a strong answer:** It connects HTTP and browsers to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming HTTP and browsers without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-intermediate-18",
      "level": "intermediate",
      "kind": "practical",
      "prompt": "How would you use Web Fundamentals to achieve the Core Web Vitals outcome below for a commerce page used across mobile devices and assistive technology? Target outcome: A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP. Your answer should implement it with validation, security, observability, and failure handling.",
      "skillsTested": [
        "Core Web Vitals",
        "Implementation and testing",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would use Core Web Vitals through the smallest production-safe configuration that meets the stated outcome. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Load the main thing, respond quickly, move nothing unexpectedly.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n- **Operation:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n- **Proof:** I would verify it by testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics.\n\n**Implementation:** I begin with the smallest configuration that can meet the stated outcome, keep access and resource scope narrow, and make invalid input or unsupported states fail clearly. I then add the operational controls in the same change: ownership, useful telemetry, bounded resource use, and a recovery path. Each extra component must address a named requirement rather than anticipated future complexity.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP. This names a concrete boundary and observable outcome rather than calling Core Web Vitals “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Field data matters because lab hardware and networks are only controlled samples. My final rule is **Load the main thing, respond quickly, move nothing unexpectedly.**",
        "modelAnswer": "**Recommended approach:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n\n**Implementation:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n\n**Concrete example:** A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP.\n\n**How to test it:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**Trade-off and limit:** Before shipping, I would make Core Web Vitals ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content.",
        "explanation": "**Why this answer is correct:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n\n**In plain language:** Load the main thing, respond quickly, move nothing unexpectedly.\n\n**How it applies in production:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n\n**Evidence to ask for:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**What a complete answer includes:** Implement it with validation, security, observability, and failure handling.",
        "keyPoints": [
          "**Remember:** Load the main thing, respond quickly, move nothing unexpectedly.",
          "Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability.",
          "Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content."
        ],
        "realWorldExample": "🏭 **In production:** A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP.\n\n✅ **Why this is a strong answer:** It connects Core Web Vitals to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Core Web Vitals without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-intermediate-22",
      "level": "intermediate",
      "kind": "practical",
      "prompt": "How would you use Web Fundamentals to achieve the Forms and validation outcome below for a commerce page used across mobile devices and assistive technology? Target outcome: A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order. Your answer should implement it with validation, security, observability, and failure handling.",
      "skillsTested": [
        "Forms and validation",
        "Implementation and testing",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would use Forms and validation through the smallest production-safe configuration that meets the stated outcome. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Validate near the user for feedback, at the server for trust.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n- **Operation:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n- **Proof:** I would verify it by testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics.\n\n**Implementation:** I begin with the smallest configuration that can meet the stated outcome, keep access and resource scope narrow, and make invalid input or unsupported states fail clearly. I then add the operational controls in the same change: ownership, useful telemetry, bounded resource use, and a recovery path. Each extra component must address a named requirement rather than anticipated future complexity.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order. This names a concrete boundary and observable outcome rather than calling Forms and validation “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted. My final rule is **Validate near the user for feedback, at the server for trust.**",
        "modelAnswer": "**Recommended approach:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n\n**Implementation:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n\n**Concrete example:** A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order.\n\n**How to test it:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**Trade-off and limit:** Before shipping, I would make Forms and validation ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.",
        "explanation": "**Why this answer is correct:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n\n**In plain language:** Validate near the user for feedback, at the server for trust.\n\n**How it applies in production:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n\n**Evidence to ask for:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**What a complete answer includes:** Implement it with validation, security, observability, and failure handling.",
        "keyPoints": [
          "**Remember:** Validate near the user for feedback, at the server for trust.",
          "HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility.",
          "Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server."
        ],
        "realWorldExample": "🏭 **In production:** A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order.\n\n✅ **Why this is a strong answer:** It connects Forms and validation to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Forms and validation without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-intermediate-26",
      "level": "intermediate",
      "kind": "practical",
      "prompt": "How would you use Web Fundamentals to achieve the Browser JavaScript outcome below for a commerce page used across mobile devices and assistive technology? Target outcome: A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke. Your answer should implement it with validation, security, observability, and failure handling.",
      "skillsTested": [
        "Browser JavaScript",
        "Implementation and testing",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would use Browser JavaScript through the smallest production-safe configuration that meets the stated outcome. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **The main thread renders and handles input; do not block it.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n- **Operation:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n- **Proof:** I would verify it by testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics.\n\n**Implementation:** I begin with the smallest configuration that can meet the stated outcome, keep access and resource scope narrow, and make invalid input or unsupported states fail clearly. I then add the operational controls in the same change: ownership, useful telemetry, bounded resource use, and a recovery path. Each extra component must address a named requirement rather than anticipated future complexity.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke. This names a concrete boundary and observable outcome rather than calling Browser JavaScript “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness. My final rule is **The main thread renders and handles input; do not block it.**",
        "modelAnswer": "**Recommended approach:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n\n**Implementation:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n\n**Concrete example:** A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke.\n\n**How to test it:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**Trade-off and limit:** Before shipping, I would make Browser JavaScript ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.",
        "explanation": "**Why this answer is correct:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n\n**In plain language:** The main thread renders and handles input; do not block it.\n\n**How it applies in production:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n\n**Evidence to ask for:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**What a complete answer includes:** Implement it with validation, security, observability, and failure handling.",
        "keyPoints": [
          "**Remember:** The main thread renders and handles input; do not block it.",
          "Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread.",
          "Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks."
        ],
        "realWorldExample": "🏭 **In production:** A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke.\n\n✅ **Why this is a strong answer:** It connects Browser JavaScript to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Browser JavaScript without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-intermediate-30",
      "level": "intermediate",
      "kind": "practical",
      "prompt": "How would you use Web Fundamentals to achieve the Web security outcome below for a commerce page used across mobile devices and assistive technology? Target outcome: A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts. Your answer should implement it with validation, security, observability, and failure handling.",
      "skillsTested": [
        "Web security",
        "Implementation and testing",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would use Web security through the smallest production-safe configuration that meets the stated outcome. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Never trust the client; escape output and lock down the browser.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n- **Operation:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n- **Proof:** I would verify it by testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics.\n\n**Implementation:** I begin with the smallest configuration that can meet the stated outcome, keep access and resource scope narrow, and make invalid input or unsupported states fail clearly. I then add the operational controls in the same change: ownership, useful telemetry, bounded resource use, and a recovery path. Each extra component must address a named requirement rather than anticipated future complexity.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts. This names a concrete boundary and observable outcome rather than calling Web security “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls. My final rule is **Never trust the client; escape output and lock down the browser.**",
        "modelAnswer": "**Recommended approach:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n\n**Implementation:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n\n**Concrete example:** A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts.\n\n**How to test it:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**Trade-off and limit:** Before shipping, I would make Web security ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.",
        "explanation": "**Why this answer is correct:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n\n**In plain language:** Never trust the client; escape output and lock down the browser.\n\n**How it applies in production:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n\n**Evidence to ask for:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**What a complete answer includes:** Implement it with validation, security, observability, and failure handling.",
        "keyPoints": [
          "**Remember:** Never trust the client; escape output and lock down the browser.",
          "Web security depends on not trusting the client and controlling what the browser is allowed to do.",
          "Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins."
        ],
        "realWorldExample": "🏭 **In production:** A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts.\n\n✅ **Why this is a strong answer:** It connects Web security to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Web security without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-intermediate-34",
      "level": "intermediate",
      "kind": "practical",
      "prompt": "How would you use Web Fundamentals to achieve the Caching and CDNs outcome below for a commerce page used across mobile devices and assistive technology? Target outcome: A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect. Your answer should implement it with validation, security, observability, and failure handling.",
      "skillsTested": [
        "Caching and CDNs",
        "Implementation and testing",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would use Caching and CDNs through the smallest production-safe configuration that meets the stated outcome. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Cache immutable assets hard; revalidate the documents that change.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n- **Operation:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n- **Proof:** I would verify it by testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics.\n\n**Implementation:** I begin with the smallest configuration that can meet the stated outcome, keep access and resource scope narrow, and make invalid input or unsupported states fail clearly. I then add the operational controls in the same change: ownership, useful telemetry, bounded resource use, and a recovery path. Each extra component must address a named requirement rather than anticipated future complexity.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect. This names a concrete boundary and observable outcome rather than calling Caching and CDNs “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely. My final rule is **Cache immutable assets hard; revalidate the documents that change.**",
        "modelAnswer": "**Recommended approach:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n\n**Implementation:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n\n**Concrete example:** A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect.\n\n**How to test it:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**Trade-off and limit:** Before shipping, I would make Caching and CDNs ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.",
        "explanation": "**Why this answer is correct:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n\n**In plain language:** Cache immutable assets hard; revalidate the documents that change.\n\n**How it applies in production:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n\n**Evidence to ask for:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**What a complete answer includes:** Implement it with validation, security, observability, and failure handling.",
        "keyPoints": [
          "**Remember:** Cache immutable assets hard; revalidate the documents that change.",
          "Browser and CDN caching avoid re-fetching unchanged resources.",
          "Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change."
        ],
        "realWorldExample": "🏭 **In production:** A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect.\n\n✅ **Why this is a strong answer:** It connects Caching and CDNs to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Caching and CDNs without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-intermediate-38",
      "level": "intermediate",
      "kind": "practical",
      "prompt": "How would you use Web Fundamentals to achieve the Progressive enhancement outcome below for a commerce page used across mobile devices and assistive technology? Target outcome: A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully. Your answer should implement it with validation, security, observability, and failure handling.",
      "skillsTested": [
        "Progressive enhancement",
        "Implementation and testing",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would use Progressive enhancement through the smallest production-safe configuration that meets the stated outcome. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Working baseline first; enhancement is a bonus, not a requirement.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n- **Operation:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n- **Proof:** I would verify it by testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics.\n\n**Implementation:** I begin with the smallest configuration that can meet the stated outcome, keep access and resource scope narrow, and make invalid input or unsupported states fail clearly. I then add the operational controls in the same change: ownership, useful telemetry, bounded resource use, and a recovery path. Each extra component must address a named requirement rather than anticipated future complexity.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully. This names a concrete boundary and observable outcome rather than calling Progressive enhancement “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** The page still functions when a script fails, on slow networks, or for assistive technology. My final rule is **Working baseline first; enhancement is a bonus, not a requirement.**",
        "modelAnswer": "**Recommended approach:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n\n**Implementation:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n\n**Concrete example:** A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully.\n\n**How to test it:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**Trade-off and limit:** Before shipping, I would make Progressive enhancement ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.",
        "explanation": "**Why this answer is correct:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n\n**In plain language:** Working baseline first; enhancement is a bonus, not a requirement.\n\n**How it applies in production:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n\n**Evidence to ask for:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**What a complete answer includes:** Implement it with validation, security, observability, and failure handling.",
        "keyPoints": [
          "**Remember:** Working baseline first; enhancement is a bonus, not a requirement.",
          "Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences.",
          "Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled."
        ],
        "realWorldExample": "🏭 **In production:** A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully.\n\n✅ **Why this is a strong answer:** It connects Progressive enhancement to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Progressive enhancement without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-intermediate-03",
      "level": "intermediate",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code. Focusing on Semantic HTML, how would you investigate without guessing? Your answer should diagnose an intermittent production failure using ordered evidence.",
      "skillsTested": [
        "Semantic HTML",
        "Evidence-based diagnosis",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the Semantic HTML failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Use the element whose built-in contract matches the job.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n- **Operation:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code. This names a concrete boundary and observable outcome rather than calling Semantic HTML “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Choose elements for purpose, then style them. My final rule is **Use the element whose built-in contract matches the job.**",
        "modelAnswer": "**Expected behavior:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** Before shipping, I would make Semantic HTML ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions.",
        "explanation": "**Why this answer is correct:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n\n**In plain language:** Use the element whose built-in contract matches the job.\n\n**How it applies in production:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Diagnose an intermittent production failure using ordered evidence.",
        "keyPoints": [
          "**Remember:** Use the element whose built-in contract matches the job.",
          "Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs.",
          "Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions."
        ],
        "realWorldExample": "🏭 **In production:** A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code.\n\n✅ **Why this is a strong answer:** It connects Semantic HTML to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Semantic HTML without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-intermediate-07",
      "level": "intermediate",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks. Focusing on CSS layout, how would you investigate without guessing? Your answer should diagnose an intermittent production failure using ordered evidence.",
      "skillsTested": [
        "CSS layout",
        "Evidence-based diagnosis",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the CSS layout failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Let content size first; constrain only where necessary.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n- **Operation:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks. This names a concrete boundary and observable outcome rather than calling CSS layout “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Responsive design should follow content breakpoints rather than device names. My final rule is **Let content size first; constrain only where necessary.**",
        "modelAnswer": "**Expected behavior:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** Before shipping, I would make CSS layout ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.",
        "explanation": "**Why this answer is correct:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n\n**In plain language:** Let content size first; constrain only where necessary.\n\n**How it applies in production:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Diagnose an intermittent production failure using ordered evidence.",
        "keyPoints": [
          "**Remember:** Let content size first; constrain only where necessary.",
          "CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space.",
          "Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions."
        ],
        "realWorldExample": "🏭 **In production:** A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks.\n\n✅ **Why this is a strong answer:** It connects CSS layout to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming CSS layout without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-intermediate-11",
      "level": "intermediate",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard. Focusing on Accessibility, how would you investigate without guessing? Your answer should diagnose an intermittent production failure using ordered evidence.",
      "skillsTested": [
        "Accessibility",
        "Evidence-based diagnosis",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the Accessibility failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Name it, focus it, announce it, and keep it operable.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n- **Operation:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard. This names a concrete boundary and observable outcome rather than calling Accessibility “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** WCAG is a testable baseline, not a substitute for user testing. My final rule is **Name it, focus it, announce it, and keep it operable.**",
        "modelAnswer": "**Expected behavior:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** Before shipping, I would make Accessibility ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size.",
        "explanation": "**Why this answer is correct:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n\n**In plain language:** Name it, focus it, announce it, and keep it operable.\n\n**How it applies in production:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Diagnose an intermittent production failure using ordered evidence.",
        "keyPoints": [
          "**Remember:** Name it, focus it, announce it, and keep it operable.",
          "Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods.",
          "Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size."
        ],
        "realWorldExample": "🏭 **In production:** Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard.\n\n✅ **Why this is a strong answer:** It connects Accessibility to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Accessibility without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-intermediate-15",
      "level": "intermediate",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck. Focusing on HTTP and browsers, how would you investigate without guessing? Your answer should diagnose an intermittent production failure using ordered evidence.",
      "skillsTested": [
        "HTTP and browsers",
        "Evidence-based diagnosis",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the HTTP and browsers failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Follow the request from name lookup to pixels.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n- **Operation:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck. This names a concrete boundary and observable outcome rather than calling HTTP and browsers “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Caching and connection reuse can matter more than JavaScript micro-optimizations. My final rule is **Follow the request from name lookup to pixels.**",
        "modelAnswer": "**Expected behavior:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** Before shipping, I would make HTTP and browsers ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays.",
        "explanation": "**Why this answer is correct:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n\n**In plain language:** Follow the request from name lookup to pixels.\n\n**How it applies in production:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Diagnose an intermittent production failure using ordered evidence.",
        "keyPoints": [
          "**Remember:** Follow the request from name lookup to pixels.",
          "A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites.",
          "Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays."
        ],
        "realWorldExample": "🏭 **In production:** A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck.\n\n✅ **Why this is a strong answer:** It connects HTTP and browsers to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming HTTP and browsers without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-intermediate-19",
      "level": "intermediate",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP. Focusing on Core Web Vitals, how would you investigate without guessing? Your answer should diagnose an intermittent production failure using ordered evidence.",
      "skillsTested": [
        "Core Web Vitals",
        "Evidence-based diagnosis",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the Core Web Vitals failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Load the main thing, respond quickly, move nothing unexpectedly.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n- **Operation:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP. This names a concrete boundary and observable outcome rather than calling Core Web Vitals “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Field data matters because lab hardware and networks are only controlled samples. My final rule is **Load the main thing, respond quickly, move nothing unexpectedly.**",
        "modelAnswer": "**Expected behavior:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** Before shipping, I would make Core Web Vitals ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content.",
        "explanation": "**Why this answer is correct:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n\n**In plain language:** Load the main thing, respond quickly, move nothing unexpectedly.\n\n**How it applies in production:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Diagnose an intermittent production failure using ordered evidence.",
        "keyPoints": [
          "**Remember:** Load the main thing, respond quickly, move nothing unexpectedly.",
          "Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability.",
          "Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content."
        ],
        "realWorldExample": "🏭 **In production:** A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP.\n\n✅ **Why this is a strong answer:** It connects Core Web Vitals to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Core Web Vitals without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-intermediate-23",
      "level": "intermediate",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order. Focusing on Forms and validation, how would you investigate without guessing? Your answer should diagnose an intermittent production failure using ordered evidence.",
      "skillsTested": [
        "Forms and validation",
        "Evidence-based diagnosis",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the Forms and validation failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Validate near the user for feedback, at the server for trust.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n- **Operation:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order. This names a concrete boundary and observable outcome rather than calling Forms and validation “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted. My final rule is **Validate near the user for feedback, at the server for trust.**",
        "modelAnswer": "**Expected behavior:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** Before shipping, I would make Forms and validation ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.",
        "explanation": "**Why this answer is correct:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n\n**In plain language:** Validate near the user for feedback, at the server for trust.\n\n**How it applies in production:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Diagnose an intermittent production failure using ordered evidence.",
        "keyPoints": [
          "**Remember:** Validate near the user for feedback, at the server for trust.",
          "HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility.",
          "Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server."
        ],
        "realWorldExample": "🏭 **In production:** A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order.\n\n✅ **Why this is a strong answer:** It connects Forms and validation to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Forms and validation without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-intermediate-27",
      "level": "intermediate",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke. Focusing on Browser JavaScript, how would you investigate without guessing? Your answer should diagnose an intermittent production failure using ordered evidence.",
      "skillsTested": [
        "Browser JavaScript",
        "Evidence-based diagnosis",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the Browser JavaScript failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **The main thread renders and handles input; do not block it.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n- **Operation:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke. This names a concrete boundary and observable outcome rather than calling Browser JavaScript “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness. My final rule is **The main thread renders and handles input; do not block it.**",
        "modelAnswer": "**Expected behavior:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** Before shipping, I would make Browser JavaScript ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.",
        "explanation": "**Why this answer is correct:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n\n**In plain language:** The main thread renders and handles input; do not block it.\n\n**How it applies in production:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Diagnose an intermittent production failure using ordered evidence.",
        "keyPoints": [
          "**Remember:** The main thread renders and handles input; do not block it.",
          "Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread.",
          "Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks."
        ],
        "realWorldExample": "🏭 **In production:** A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke.\n\n✅ **Why this is a strong answer:** It connects Browser JavaScript to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Browser JavaScript without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-intermediate-31",
      "level": "intermediate",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts. Focusing on Web security, how would you investigate without guessing? Your answer should diagnose an intermittent production failure using ordered evidence.",
      "skillsTested": [
        "Web security",
        "Evidence-based diagnosis",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the Web security failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Never trust the client; escape output and lock down the browser.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n- **Operation:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts. This names a concrete boundary and observable outcome rather than calling Web security “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls. My final rule is **Never trust the client; escape output and lock down the browser.**",
        "modelAnswer": "**Expected behavior:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** Before shipping, I would make Web security ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.",
        "explanation": "**Why this answer is correct:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n\n**In plain language:** Never trust the client; escape output and lock down the browser.\n\n**How it applies in production:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Diagnose an intermittent production failure using ordered evidence.",
        "keyPoints": [
          "**Remember:** Never trust the client; escape output and lock down the browser.",
          "Web security depends on not trusting the client and controlling what the browser is allowed to do.",
          "Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins."
        ],
        "realWorldExample": "🏭 **In production:** A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts.\n\n✅ **Why this is a strong answer:** It connects Web security to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Web security without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-intermediate-35",
      "level": "intermediate",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect. Focusing on Caching and CDNs, how would you investigate without guessing? Your answer should diagnose an intermittent production failure using ordered evidence.",
      "skillsTested": [
        "Caching and CDNs",
        "Evidence-based diagnosis",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the Caching and CDNs failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Cache immutable assets hard; revalidate the documents that change.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n- **Operation:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect. This names a concrete boundary and observable outcome rather than calling Caching and CDNs “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely. My final rule is **Cache immutable assets hard; revalidate the documents that change.**",
        "modelAnswer": "**Expected behavior:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** Before shipping, I would make Caching and CDNs ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.",
        "explanation": "**Why this answer is correct:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n\n**In plain language:** Cache immutable assets hard; revalidate the documents that change.\n\n**How it applies in production:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Diagnose an intermittent production failure using ordered evidence.",
        "keyPoints": [
          "**Remember:** Cache immutable assets hard; revalidate the documents that change.",
          "Browser and CDN caching avoid re-fetching unchanged resources.",
          "Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change."
        ],
        "realWorldExample": "🏭 **In production:** A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect.\n\n✅ **Why this is a strong answer:** It connects Caching and CDNs to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Caching and CDNs without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-intermediate-39",
      "level": "intermediate",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully. Focusing on Progressive enhancement, how would you investigate without guessing? Your answer should diagnose an intermittent production failure using ordered evidence.",
      "skillsTested": [
        "Progressive enhancement",
        "Evidence-based diagnosis",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the Progressive enhancement failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Working baseline first; enhancement is a bonus, not a requirement.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n- **Operation:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully. This names a concrete boundary and observable outcome rather than calling Progressive enhancement “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** The page still functions when a script fails, on slow networks, or for assistive technology. My final rule is **Working baseline first; enhancement is a bonus, not a requirement.**",
        "modelAnswer": "**Expected behavior:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** Before shipping, I would make Progressive enhancement ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.",
        "explanation": "**Why this answer is correct:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n\n**In plain language:** Working baseline first; enhancement is a bonus, not a requirement.\n\n**How it applies in production:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Diagnose an intermittent production failure using ordered evidence.",
        "keyPoints": [
          "**Remember:** Working baseline first; enhancement is a bonus, not a requirement.",
          "Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences.",
          "Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled."
        ],
        "realWorldExample": "🏭 **In production:** A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully.\n\n✅ **Why this is a strong answer:** It connects Progressive enhancement to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Progressive enhancement without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-intermediate-04",
      "level": "intermediate",
      "kind": "design",
      "prompt": "For a commerce page used across mobile devices and assistive technology, how would you design the part of the system concerned with Semantic HTML using Web Fundamentals? The target outcome is: A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code. Your answer should design the boundary and justify the main alternative you rejected.",
      "skillsTested": [
        "Semantic HTML",
        "System design",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would design Semantic HTML around a clear boundary, predictable failure behavior, and evidence that proves the result. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Use the element whose built-in contract matches the job.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n- **Operation:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n- **Proof:** I would verify it by walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO.\n\n**Design:** I would give Semantic HTML a clear boundary: defined inputs and outputs, one owner, explicit authorization, bounded resource use, observable failure, and a rehearsed recovery path. I would walk a successful request through the design, then repeat the walk for invalid input, overload, dependency failure, and rollback. That exposes hidden coupling before production traffic does.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code. This names a concrete boundary and observable outcome rather than calling Semantic HTML “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Choose elements for purpose, then style them. My final rule is **Use the element whose built-in contract matches the job.**",
        "modelAnswer": "**Boundary:** I would give Semantic HTML a clear owner, inputs, outputs, failure behavior, and recovery path. Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n\n**Production controls:** I would use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n\n**Concrete example:** A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code.\n\n**How to prove it:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**Trade-off and redesign point:** Before shipping, I would make Semantic HTML ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions.",
        "explanation": "**Why this answer is correct:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n\n**In plain language:** Use the element whose built-in contract matches the job.\n\n**How it applies in production:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n\n**Evidence to ask for:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**What a complete answer includes:** Design the boundary and justify the main alternative you rejected.",
        "keyPoints": [
          "**Remember:** Use the element whose built-in contract matches the job.",
          "Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs.",
          "Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions."
        ],
        "realWorldExample": "🏭 **In production:** A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code.\n\n✅ **Why this is a strong answer:** It connects Semantic HTML to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Semantic HTML without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Semantic HTML: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs."
            },
            {
              "label": "Protect",
              "detail": "Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-intermediate-08",
      "level": "intermediate",
      "kind": "design",
      "prompt": "For a commerce page used across mobile devices and assistive technology, how would you design the part of the system concerned with CSS layout using Web Fundamentals? The target outcome is: A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks. Your answer should design the boundary and justify the main alternative you rejected.",
      "skillsTested": [
        "CSS layout",
        "System design",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would design CSS layout around a clear boundary, predictable failure behavior, and evidence that proves the result. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Let content size first; constrain only where necessary.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n- **Operation:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n- **Proof:** I would verify it by walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO.\n\n**Design:** I would give CSS layout a clear boundary: defined inputs and outputs, one owner, explicit authorization, bounded resource use, observable failure, and a rehearsed recovery path. I would walk a successful request through the design, then repeat the walk for invalid input, overload, dependency failure, and rollback. That exposes hidden coupling before production traffic does.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks. This names a concrete boundary and observable outcome rather than calling CSS layout “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Responsive design should follow content breakpoints rather than device names. My final rule is **Let content size first; constrain only where necessary.**",
        "modelAnswer": "**Boundary:** I would give CSS layout a clear owner, inputs, outputs, failure behavior, and recovery path. CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n\n**Production controls:** I would prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n\n**Concrete example:** A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks.\n\n**How to prove it:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**Trade-off and redesign point:** Before shipping, I would make CSS layout ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.",
        "explanation": "**Why this answer is correct:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n\n**In plain language:** Let content size first; constrain only where necessary.\n\n**How it applies in production:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n\n**Evidence to ask for:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**What a complete answer includes:** Design the boundary and justify the main alternative you rejected.",
        "keyPoints": [
          "**Remember:** Let content size first; constrain only where necessary.",
          "CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space.",
          "Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions."
        ],
        "realWorldExample": "🏭 **In production:** A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks.\n\n✅ **Why this is a strong answer:** It connects CSS layout to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming CSS layout without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "CSS layout: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space."
            },
            {
              "label": "Protect",
              "detail": "Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-intermediate-12",
      "level": "intermediate",
      "kind": "design",
      "prompt": "For a commerce page used across mobile devices and assistive technology, how would you design the part of the system concerned with Accessibility using Web Fundamentals? The target outcome is: Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard. Your answer should design the boundary and justify the main alternative you rejected.",
      "skillsTested": [
        "Accessibility",
        "System design",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would design Accessibility around a clear boundary, predictable failure behavior, and evidence that proves the result. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Name it, focus it, announce it, and keep it operable.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n- **Operation:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n- **Proof:** I would verify it by walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO.\n\n**Design:** I would give Accessibility a clear boundary: defined inputs and outputs, one owner, explicit authorization, bounded resource use, observable failure, and a rehearsed recovery path. I would walk a successful request through the design, then repeat the walk for invalid input, overload, dependency failure, and rollback. That exposes hidden coupling before production traffic does.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard. This names a concrete boundary and observable outcome rather than calling Accessibility “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** WCAG is a testable baseline, not a substitute for user testing. My final rule is **Name it, focus it, announce it, and keep it operable.**",
        "modelAnswer": "**Boundary:** I would give Accessibility a clear owner, inputs, outputs, failure behavior, and recovery path. Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n\n**Production controls:** I would preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n\n**Concrete example:** Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard.\n\n**How to prove it:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**Trade-off and redesign point:** Before shipping, I would make Accessibility ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size.",
        "explanation": "**Why this answer is correct:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n\n**In plain language:** Name it, focus it, announce it, and keep it operable.\n\n**How it applies in production:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n\n**Evidence to ask for:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**What a complete answer includes:** Design the boundary and justify the main alternative you rejected.",
        "keyPoints": [
          "**Remember:** Name it, focus it, announce it, and keep it operable.",
          "Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods.",
          "Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size."
        ],
        "realWorldExample": "🏭 **In production:** Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard.\n\n✅ **Why this is a strong answer:** It connects Accessibility to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Accessibility without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Accessibility: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods."
            },
            {
              "label": "Protect",
              "detail": "Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-intermediate-16",
      "level": "intermediate",
      "kind": "design",
      "prompt": "For a commerce page used across mobile devices and assistive technology, how would you design the part of the system concerned with HTTP and browsers using Web Fundamentals? The target outcome is: A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck. Your answer should design the boundary and justify the main alternative you rejected.",
      "skillsTested": [
        "HTTP and browsers",
        "System design",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would design HTTP and browsers around a clear boundary, predictable failure behavior, and evidence that proves the result. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Follow the request from name lookup to pixels.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n- **Operation:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n- **Proof:** I would verify it by walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO.\n\n**Design:** I would give HTTP and browsers a clear boundary: defined inputs and outputs, one owner, explicit authorization, bounded resource use, observable failure, and a rehearsed recovery path. I would walk a successful request through the design, then repeat the walk for invalid input, overload, dependency failure, and rollback. That exposes hidden coupling before production traffic does.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck. This names a concrete boundary and observable outcome rather than calling HTTP and browsers “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Caching and connection reuse can matter more than JavaScript micro-optimizations. My final rule is **Follow the request from name lookup to pixels.**",
        "modelAnswer": "**Boundary:** I would give HTTP and browsers a clear owner, inputs, outputs, failure behavior, and recovery path. A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n\n**Production controls:** I would use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n\n**Concrete example:** A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck.\n\n**How to prove it:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**Trade-off and redesign point:** Before shipping, I would make HTTP and browsers ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays.",
        "explanation": "**Why this answer is correct:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n\n**In plain language:** Follow the request from name lookup to pixels.\n\n**How it applies in production:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n\n**Evidence to ask for:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**What a complete answer includes:** Design the boundary and justify the main alternative you rejected.",
        "keyPoints": [
          "**Remember:** Follow the request from name lookup to pixels.",
          "A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites.",
          "Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays."
        ],
        "realWorldExample": "🏭 **In production:** A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck.\n\n✅ **Why this is a strong answer:** It connects HTTP and browsers to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming HTTP and browsers without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "HTTP and browsers: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites."
            },
            {
              "label": "Protect",
              "detail": "Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-intermediate-20",
      "level": "intermediate",
      "kind": "design",
      "prompt": "For a commerce page used across mobile devices and assistive technology, how would you design the part of the system concerned with Core Web Vitals using Web Fundamentals? The target outcome is: A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP. Your answer should design the boundary and justify the main alternative you rejected.",
      "skillsTested": [
        "Core Web Vitals",
        "System design",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would design Core Web Vitals around a clear boundary, predictable failure behavior, and evidence that proves the result. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Load the main thing, respond quickly, move nothing unexpectedly.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n- **Operation:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n- **Proof:** I would verify it by walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO.\n\n**Design:** I would give Core Web Vitals a clear boundary: defined inputs and outputs, one owner, explicit authorization, bounded resource use, observable failure, and a rehearsed recovery path. I would walk a successful request through the design, then repeat the walk for invalid input, overload, dependency failure, and rollback. That exposes hidden coupling before production traffic does.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP. This names a concrete boundary and observable outcome rather than calling Core Web Vitals “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Field data matters because lab hardware and networks are only controlled samples. My final rule is **Load the main thing, respond quickly, move nothing unexpectedly.**",
        "modelAnswer": "**Boundary:** I would give Core Web Vitals a clear owner, inputs, outputs, failure behavior, and recovery path. Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n\n**Production controls:** I would attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n\n**Concrete example:** A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP.\n\n**How to prove it:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**Trade-off and redesign point:** Before shipping, I would make Core Web Vitals ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content.",
        "explanation": "**Why this answer is correct:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n\n**In plain language:** Load the main thing, respond quickly, move nothing unexpectedly.\n\n**How it applies in production:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n\n**Evidence to ask for:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**What a complete answer includes:** Design the boundary and justify the main alternative you rejected.",
        "keyPoints": [
          "**Remember:** Load the main thing, respond quickly, move nothing unexpectedly.",
          "Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability.",
          "Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content."
        ],
        "realWorldExample": "🏭 **In production:** A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP.\n\n✅ **Why this is a strong answer:** It connects Core Web Vitals to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Core Web Vitals without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Core Web Vitals: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability."
            },
            {
              "label": "Protect",
              "detail": "Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-intermediate-24",
      "level": "intermediate",
      "kind": "design",
      "prompt": "For a commerce page used across mobile devices and assistive technology, how would you design the part of the system concerned with Forms and validation using Web Fundamentals? The target outcome is: A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order. Your answer should design the boundary and justify the main alternative you rejected.",
      "skillsTested": [
        "Forms and validation",
        "System design",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would design Forms and validation around a clear boundary, predictable failure behavior, and evidence that proves the result. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Validate near the user for feedback, at the server for trust.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n- **Operation:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n- **Proof:** I would verify it by walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO.\n\n**Design:** I would give Forms and validation a clear boundary: defined inputs and outputs, one owner, explicit authorization, bounded resource use, observable failure, and a rehearsed recovery path. I would walk a successful request through the design, then repeat the walk for invalid input, overload, dependency failure, and rollback. That exposes hidden coupling before production traffic does.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order. This names a concrete boundary and observable outcome rather than calling Forms and validation “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted. My final rule is **Validate near the user for feedback, at the server for trust.**",
        "modelAnswer": "**Boundary:** I would give Forms and validation a clear owner, inputs, outputs, failure behavior, and recovery path. HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n\n**Production controls:** I would use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n\n**Concrete example:** A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order.\n\n**How to prove it:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**Trade-off and redesign point:** Before shipping, I would make Forms and validation ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.",
        "explanation": "**Why this answer is correct:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n\n**In plain language:** Validate near the user for feedback, at the server for trust.\n\n**How it applies in production:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n\n**Evidence to ask for:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**What a complete answer includes:** Design the boundary and justify the main alternative you rejected.",
        "keyPoints": [
          "**Remember:** Validate near the user for feedback, at the server for trust.",
          "HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility.",
          "Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server."
        ],
        "realWorldExample": "🏭 **In production:** A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order.\n\n✅ **Why this is a strong answer:** It connects Forms and validation to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Forms and validation without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Forms and validation: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility."
            },
            {
              "label": "Protect",
              "detail": "Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-intermediate-28",
      "level": "intermediate",
      "kind": "design",
      "prompt": "For a commerce page used across mobile devices and assistive technology, how would you design the part of the system concerned with Browser JavaScript using Web Fundamentals? The target outcome is: A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke. Your answer should design the boundary and justify the main alternative you rejected.",
      "skillsTested": [
        "Browser JavaScript",
        "System design",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would design Browser JavaScript around a clear boundary, predictable failure behavior, and evidence that proves the result. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **The main thread renders and handles input; do not block it.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n- **Operation:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n- **Proof:** I would verify it by walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO.\n\n**Design:** I would give Browser JavaScript a clear boundary: defined inputs and outputs, one owner, explicit authorization, bounded resource use, observable failure, and a rehearsed recovery path. I would walk a successful request through the design, then repeat the walk for invalid input, overload, dependency failure, and rollback. That exposes hidden coupling before production traffic does.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke. This names a concrete boundary and observable outcome rather than calling Browser JavaScript “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness. My final rule is **The main thread renders and handles input; do not block it.**",
        "modelAnswer": "**Boundary:** I would give Browser JavaScript a clear owner, inputs, outputs, failure behavior, and recovery path. Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n\n**Production controls:** I would delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n\n**Concrete example:** A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke.\n\n**How to prove it:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**Trade-off and redesign point:** Before shipping, I would make Browser JavaScript ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.",
        "explanation": "**Why this answer is correct:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n\n**In plain language:** The main thread renders and handles input; do not block it.\n\n**How it applies in production:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n\n**Evidence to ask for:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**What a complete answer includes:** Design the boundary and justify the main alternative you rejected.",
        "keyPoints": [
          "**Remember:** The main thread renders and handles input; do not block it.",
          "Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread.",
          "Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks."
        ],
        "realWorldExample": "🏭 **In production:** A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke.\n\n✅ **Why this is a strong answer:** It connects Browser JavaScript to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Browser JavaScript without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Browser JavaScript: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread."
            },
            {
              "label": "Protect",
              "detail": "Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-intermediate-32",
      "level": "intermediate",
      "kind": "design",
      "prompt": "For a commerce page used across mobile devices and assistive technology, how would you design the part of the system concerned with Web security using Web Fundamentals? The target outcome is: A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts. Your answer should design the boundary and justify the main alternative you rejected.",
      "skillsTested": [
        "Web security",
        "System design",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would design Web security around a clear boundary, predictable failure behavior, and evidence that proves the result. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Never trust the client; escape output and lock down the browser.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n- **Operation:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n- **Proof:** I would verify it by walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO.\n\n**Design:** I would give Web security a clear boundary: defined inputs and outputs, one owner, explicit authorization, bounded resource use, observable failure, and a rehearsed recovery path. I would walk a successful request through the design, then repeat the walk for invalid input, overload, dependency failure, and rollback. That exposes hidden coupling before production traffic does.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts. This names a concrete boundary and observable outcome rather than calling Web security “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls. My final rule is **Never trust the client; escape output and lock down the browser.**",
        "modelAnswer": "**Boundary:** I would give Web security a clear owner, inputs, outputs, failure behavior, and recovery path. Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n\n**Production controls:** I would escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n\n**Concrete example:** A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts.\n\n**How to prove it:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**Trade-off and redesign point:** Before shipping, I would make Web security ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.",
        "explanation": "**Why this answer is correct:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n\n**In plain language:** Never trust the client; escape output and lock down the browser.\n\n**How it applies in production:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n\n**Evidence to ask for:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**What a complete answer includes:** Design the boundary and justify the main alternative you rejected.",
        "keyPoints": [
          "**Remember:** Never trust the client; escape output and lock down the browser.",
          "Web security depends on not trusting the client and controlling what the browser is allowed to do.",
          "Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins."
        ],
        "realWorldExample": "🏭 **In production:** A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts.\n\n✅ **Why this is a strong answer:** It connects Web security to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Web security without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Web security: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Web security depends on not trusting the client and controlling what the browser is allowed to do."
            },
            {
              "label": "Protect",
              "detail": "Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-intermediate-36",
      "level": "intermediate",
      "kind": "design",
      "prompt": "For a commerce page used across mobile devices and assistive technology, how would you design the part of the system concerned with Caching and CDNs using Web Fundamentals? The target outcome is: A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect. Your answer should design the boundary and justify the main alternative you rejected.",
      "skillsTested": [
        "Caching and CDNs",
        "System design",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would design Caching and CDNs around a clear boundary, predictable failure behavior, and evidence that proves the result. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Cache immutable assets hard; revalidate the documents that change.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n- **Operation:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n- **Proof:** I would verify it by walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO.\n\n**Design:** I would give Caching and CDNs a clear boundary: defined inputs and outputs, one owner, explicit authorization, bounded resource use, observable failure, and a rehearsed recovery path. I would walk a successful request through the design, then repeat the walk for invalid input, overload, dependency failure, and rollback. That exposes hidden coupling before production traffic does.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect. This names a concrete boundary and observable outcome rather than calling Caching and CDNs “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely. My final rule is **Cache immutable assets hard; revalidate the documents that change.**",
        "modelAnswer": "**Boundary:** I would give Caching and CDNs a clear owner, inputs, outputs, failure behavior, and recovery path. Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n\n**Production controls:** I would fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n\n**Concrete example:** A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect.\n\n**How to prove it:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**Trade-off and redesign point:** Before shipping, I would make Caching and CDNs ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.",
        "explanation": "**Why this answer is correct:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n\n**In plain language:** Cache immutable assets hard; revalidate the documents that change.\n\n**How it applies in production:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n\n**Evidence to ask for:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**What a complete answer includes:** Design the boundary and justify the main alternative you rejected.",
        "keyPoints": [
          "**Remember:** Cache immutable assets hard; revalidate the documents that change.",
          "Browser and CDN caching avoid re-fetching unchanged resources.",
          "Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change."
        ],
        "realWorldExample": "🏭 **In production:** A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect.\n\n✅ **Why this is a strong answer:** It connects Caching and CDNs to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Caching and CDNs without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Caching and CDNs: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Browser and CDN caching avoid re-fetching unchanged resources."
            },
            {
              "label": "Protect",
              "detail": "Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-intermediate-40",
      "level": "intermediate",
      "kind": "design",
      "prompt": "For a commerce page used across mobile devices and assistive technology, how would you design the part of the system concerned with Progressive enhancement using Web Fundamentals? The target outcome is: A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully. Your answer should design the boundary and justify the main alternative you rejected.",
      "skillsTested": [
        "Progressive enhancement",
        "System design",
        "Intermediate reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would design Progressive enhancement around a clear boundary, predictable failure behavior, and evidence that proves the result. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Working baseline first; enhancement is a bonus, not a requirement.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n- **Operation:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n- **Proof:** I would verify it by walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO.\n\n**Design:** I would give Progressive enhancement a clear boundary: defined inputs and outputs, one owner, explicit authorization, bounded resource use, observable failure, and a rehearsed recovery path. I would walk a successful request through the design, then repeat the walk for invalid input, overload, dependency failure, and rollback. That exposes hidden coupling before production traffic does.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully. This names a concrete boundary and observable outcome rather than calling Progressive enhancement “best practice.”\n\n**Depth and trade-off:** In production, I would connect mechanism to operations: ownership, constrained permissions, dependency failure, useful telemetry, and safe rollback. A design is incomplete if it describes only the successful path.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** The page still functions when a script fails, on slow networks, or for assistive technology. My final rule is **Working baseline first; enhancement is a bonus, not a requirement.**",
        "modelAnswer": "**Boundary:** I would give Progressive enhancement a clear owner, inputs, outputs, failure behavior, and recovery path. Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n\n**Production controls:** I would make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n\n**Concrete example:** A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully.\n\n**How to prove it:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**Trade-off and redesign point:** Before shipping, I would make Progressive enhancement ownership, invalid input, dependency failure, security, observability, and recovery explicit. Concretely: make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.",
        "explanation": "**Why this answer is correct:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n\n**In plain language:** Working baseline first; enhancement is a bonus, not a requirement.\n\n**How it applies in production:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n\n**Evidence to ask for:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**What a complete answer includes:** Design the boundary and justify the main alternative you rejected.",
        "keyPoints": [
          "**Remember:** Working baseline first; enhancement is a bonus, not a requirement.",
          "Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences.",
          "Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled."
        ],
        "realWorldExample": "🏭 **In production:** A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully.\n\n✅ **Why this is a strong answer:** It connects Progressive enhancement to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Progressive enhancement without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Progressive enhancement: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences."
            },
            {
              "label": "Protect",
              "detail": "Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-advanced-01",
      "level": "advanced",
      "kind": "conceptual",
      "prompt": "What is Semantic HTML in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code. Your answer should explain internal behavior, guarantees, and the subtle limit senior engineers watch.",
      "skillsTested": [
        "Semantic HTML",
        "Concept explanation",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** Semantic HTML is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Use the element whose built-in contract matches the job.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n- **Operation:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n- **Proof:** I would verify it by showing one expected Semantic HTML behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee Semantic HTML provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code. This names a concrete boundary and observable outcome rather than calling Semantic HTML “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Choose elements for purpose, then style them. My final rule is **Use the element whose built-in contract matches the job.**",
        "modelAnswer": "**Direct answer:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n\n**Production use:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n\n**Concrete example:** A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code.\n\n**How to verify it:** Showing one expected Semantic HTML behavior and one boundary case, then connecting both to observable evidence\n\n**Trade-off and limit:** For Semantic HTML at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions.",
        "explanation": "**Why this answer is correct:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n\n**In plain language:** Use the element whose built-in contract matches the job.\n\n**How it applies in production:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n\n**Evidence to ask for:** Showing one expected Semantic HTML behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Explain internal behavior, guarantees, and the subtle limit senior engineers watch.",
        "keyPoints": [
          "**Remember:** Use the element whose built-in contract matches the job.",
          "Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs.",
          "Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions."
        ],
        "realWorldExample": "🏭 **In production:** A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code.\n\n✅ **Why this is a strong answer:** It connects Semantic HTML to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Semantic HTML without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-07",
      "level": "advanced",
      "kind": "conceptual",
      "prompt": "What is CSS layout in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks. Your answer should explain internal behavior, guarantees, and the subtle limit senior engineers watch.",
      "skillsTested": [
        "CSS layout",
        "Concept explanation",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** CSS layout is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Let content size first; constrain only where necessary.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n- **Operation:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n- **Proof:** I would verify it by showing one expected CSS layout behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee CSS layout provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks. This names a concrete boundary and observable outcome rather than calling CSS layout “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Responsive design should follow content breakpoints rather than device names. My final rule is **Let content size first; constrain only where necessary.**",
        "modelAnswer": "**Direct answer:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n\n**Production use:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n\n**Concrete example:** A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks.\n\n**How to verify it:** Showing one expected CSS layout behavior and one boundary case, then connecting both to observable evidence\n\n**Trade-off and limit:** For CSS layout at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.",
        "explanation": "**Why this answer is correct:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n\n**In plain language:** Let content size first; constrain only where necessary.\n\n**How it applies in production:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n\n**Evidence to ask for:** Showing one expected CSS layout behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Explain internal behavior, guarantees, and the subtle limit senior engineers watch.",
        "keyPoints": [
          "**Remember:** Let content size first; constrain only where necessary.",
          "CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space.",
          "Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions."
        ],
        "realWorldExample": "🏭 **In production:** A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks.\n\n✅ **Why this is a strong answer:** It connects CSS layout to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming CSS layout without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-13",
      "level": "advanced",
      "kind": "conceptual",
      "prompt": "What is Accessibility in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard. Your answer should explain internal behavior, guarantees, and the subtle limit senior engineers watch.",
      "skillsTested": [
        "Accessibility",
        "Concept explanation",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** Accessibility is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Name it, focus it, announce it, and keep it operable.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n- **Operation:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n- **Proof:** I would verify it by showing one expected Accessibility behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee Accessibility provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard. This names a concrete boundary and observable outcome rather than calling Accessibility “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** WCAG is a testable baseline, not a substitute for user testing. My final rule is **Name it, focus it, announce it, and keep it operable.**",
        "modelAnswer": "**Direct answer:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n\n**Production use:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n\n**Concrete example:** Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard.\n\n**How to verify it:** Showing one expected Accessibility behavior and one boundary case, then connecting both to observable evidence\n\n**Trade-off and limit:** For Accessibility at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size.",
        "explanation": "**Why this answer is correct:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n\n**In plain language:** Name it, focus it, announce it, and keep it operable.\n\n**How it applies in production:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n\n**Evidence to ask for:** Showing one expected Accessibility behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Explain internal behavior, guarantees, and the subtle limit senior engineers watch.",
        "keyPoints": [
          "**Remember:** Name it, focus it, announce it, and keep it operable.",
          "Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods.",
          "Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size."
        ],
        "realWorldExample": "🏭 **In production:** Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard.\n\n✅ **Why this is a strong answer:** It connects Accessibility to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Accessibility without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-19",
      "level": "advanced",
      "kind": "conceptual",
      "prompt": "What is HTTP and browsers in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck. Your answer should explain internal behavior, guarantees, and the subtle limit senior engineers watch.",
      "skillsTested": [
        "HTTP and browsers",
        "Concept explanation",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** HTTP and browsers is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Follow the request from name lookup to pixels.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n- **Operation:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n- **Proof:** I would verify it by showing one expected HTTP and browsers behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee HTTP and browsers provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck. This names a concrete boundary and observable outcome rather than calling HTTP and browsers “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Caching and connection reuse can matter more than JavaScript micro-optimizations. My final rule is **Follow the request from name lookup to pixels.**",
        "modelAnswer": "**Direct answer:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n\n**Production use:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n\n**Concrete example:** A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck.\n\n**How to verify it:** Showing one expected HTTP and browsers behavior and one boundary case, then connecting both to observable evidence\n\n**Trade-off and limit:** For HTTP and browsers at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays.",
        "explanation": "**Why this answer is correct:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n\n**In plain language:** Follow the request from name lookup to pixels.\n\n**How it applies in production:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n\n**Evidence to ask for:** Showing one expected HTTP and browsers behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Explain internal behavior, guarantees, and the subtle limit senior engineers watch.",
        "keyPoints": [
          "**Remember:** Follow the request from name lookup to pixels.",
          "A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites.",
          "Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays."
        ],
        "realWorldExample": "🏭 **In production:** A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck.\n\n✅ **Why this is a strong answer:** It connects HTTP and browsers to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming HTTP and browsers without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-25",
      "level": "advanced",
      "kind": "conceptual",
      "prompt": "What is Core Web Vitals in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP. Your answer should explain internal behavior, guarantees, and the subtle limit senior engineers watch.",
      "skillsTested": [
        "Core Web Vitals",
        "Concept explanation",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** Core Web Vitals is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Load the main thing, respond quickly, move nothing unexpectedly.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n- **Operation:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n- **Proof:** I would verify it by showing one expected Core Web Vitals behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee Core Web Vitals provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP. This names a concrete boundary and observable outcome rather than calling Core Web Vitals “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Field data matters because lab hardware and networks are only controlled samples. My final rule is **Load the main thing, respond quickly, move nothing unexpectedly.**",
        "modelAnswer": "**Direct answer:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n\n**Production use:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n\n**Concrete example:** A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP.\n\n**How to verify it:** Showing one expected Core Web Vitals behavior and one boundary case, then connecting both to observable evidence\n\n**Trade-off and limit:** For Core Web Vitals at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content.",
        "explanation": "**Why this answer is correct:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n\n**In plain language:** Load the main thing, respond quickly, move nothing unexpectedly.\n\n**How it applies in production:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n\n**Evidence to ask for:** Showing one expected Core Web Vitals behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Explain internal behavior, guarantees, and the subtle limit senior engineers watch.",
        "keyPoints": [
          "**Remember:** Load the main thing, respond quickly, move nothing unexpectedly.",
          "Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability.",
          "Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content."
        ],
        "realWorldExample": "🏭 **In production:** A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP.\n\n✅ **Why this is a strong answer:** It connects Core Web Vitals to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Core Web Vitals without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-31",
      "level": "advanced",
      "kind": "conceptual",
      "prompt": "What is Forms and validation in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order. Your answer should explain internal behavior, guarantees, and the subtle limit senior engineers watch.",
      "skillsTested": [
        "Forms and validation",
        "Concept explanation",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** Forms and validation is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Validate near the user for feedback, at the server for trust.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n- **Operation:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n- **Proof:** I would verify it by showing one expected Forms and validation behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee Forms and validation provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order. This names a concrete boundary and observable outcome rather than calling Forms and validation “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted. My final rule is **Validate near the user for feedback, at the server for trust.**",
        "modelAnswer": "**Direct answer:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n\n**Production use:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n\n**Concrete example:** A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order.\n\n**How to verify it:** Showing one expected Forms and validation behavior and one boundary case, then connecting both to observable evidence\n\n**Trade-off and limit:** For Forms and validation at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.",
        "explanation": "**Why this answer is correct:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n\n**In plain language:** Validate near the user for feedback, at the server for trust.\n\n**How it applies in production:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n\n**Evidence to ask for:** Showing one expected Forms and validation behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Explain internal behavior, guarantees, and the subtle limit senior engineers watch.",
        "keyPoints": [
          "**Remember:** Validate near the user for feedback, at the server for trust.",
          "HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility.",
          "Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server."
        ],
        "realWorldExample": "🏭 **In production:** A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order.\n\n✅ **Why this is a strong answer:** It connects Forms and validation to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Forms and validation without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-37",
      "level": "advanced",
      "kind": "conceptual",
      "prompt": "What is Browser JavaScript in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke. Your answer should explain internal behavior, guarantees, and the subtle limit senior engineers watch.",
      "skillsTested": [
        "Browser JavaScript",
        "Concept explanation",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** Browser JavaScript is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **The main thread renders and handles input; do not block it.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n- **Operation:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n- **Proof:** I would verify it by showing one expected Browser JavaScript behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee Browser JavaScript provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke. This names a concrete boundary and observable outcome rather than calling Browser JavaScript “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness. My final rule is **The main thread renders and handles input; do not block it.**",
        "modelAnswer": "**Direct answer:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n\n**Production use:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n\n**Concrete example:** A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke.\n\n**How to verify it:** Showing one expected Browser JavaScript behavior and one boundary case, then connecting both to observable evidence\n\n**Trade-off and limit:** For Browser JavaScript at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.",
        "explanation": "**Why this answer is correct:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n\n**In plain language:** The main thread renders and handles input; do not block it.\n\n**How it applies in production:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n\n**Evidence to ask for:** Showing one expected Browser JavaScript behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Explain internal behavior, guarantees, and the subtle limit senior engineers watch.",
        "keyPoints": [
          "**Remember:** The main thread renders and handles input; do not block it.",
          "Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread.",
          "Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks."
        ],
        "realWorldExample": "🏭 **In production:** A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke.\n\n✅ **Why this is a strong answer:** It connects Browser JavaScript to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Browser JavaScript without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-43",
      "level": "advanced",
      "kind": "conceptual",
      "prompt": "What is Web security in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts. Your answer should explain internal behavior, guarantees, and the subtle limit senior engineers watch.",
      "skillsTested": [
        "Web security",
        "Concept explanation",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** Web security is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Never trust the client; escape output and lock down the browser.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n- **Operation:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n- **Proof:** I would verify it by showing one expected Web security behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee Web security provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts. This names a concrete boundary and observable outcome rather than calling Web security “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls. My final rule is **Never trust the client; escape output and lock down the browser.**",
        "modelAnswer": "**Direct answer:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n\n**Production use:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n\n**Concrete example:** A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts.\n\n**How to verify it:** Showing one expected Web security behavior and one boundary case, then connecting both to observable evidence\n\n**Trade-off and limit:** For Web security at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.",
        "explanation": "**Why this answer is correct:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n\n**In plain language:** Never trust the client; escape output and lock down the browser.\n\n**How it applies in production:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n\n**Evidence to ask for:** Showing one expected Web security behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Explain internal behavior, guarantees, and the subtle limit senior engineers watch.",
        "keyPoints": [
          "**Remember:** Never trust the client; escape output and lock down the browser.",
          "Web security depends on not trusting the client and controlling what the browser is allowed to do.",
          "Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins."
        ],
        "realWorldExample": "🏭 **In production:** A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts.\n\n✅ **Why this is a strong answer:** It connects Web security to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Web security without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-49",
      "level": "advanced",
      "kind": "conceptual",
      "prompt": "What is Caching and CDNs in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect. Your answer should explain internal behavior, guarantees, and the subtle limit senior engineers watch.",
      "skillsTested": [
        "Caching and CDNs",
        "Concept explanation",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** Caching and CDNs is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Cache immutable assets hard; revalidate the documents that change.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n- **Operation:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n- **Proof:** I would verify it by showing one expected Caching and CDNs behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee Caching and CDNs provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect. This names a concrete boundary and observable outcome rather than calling Caching and CDNs “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely. My final rule is **Cache immutable assets hard; revalidate the documents that change.**",
        "modelAnswer": "**Direct answer:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n\n**Production use:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n\n**Concrete example:** A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect.\n\n**How to verify it:** Showing one expected Caching and CDNs behavior and one boundary case, then connecting both to observable evidence\n\n**Trade-off and limit:** For Caching and CDNs at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.",
        "explanation": "**Why this answer is correct:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n\n**In plain language:** Cache immutable assets hard; revalidate the documents that change.\n\n**How it applies in production:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n\n**Evidence to ask for:** Showing one expected Caching and CDNs behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Explain internal behavior, guarantees, and the subtle limit senior engineers watch.",
        "keyPoints": [
          "**Remember:** Cache immutable assets hard; revalidate the documents that change.",
          "Browser and CDN caching avoid re-fetching unchanged resources.",
          "Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change."
        ],
        "realWorldExample": "🏭 **In production:** A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect.\n\n✅ **Why this is a strong answer:** It connects Caching and CDNs to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Caching and CDNs without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-55",
      "level": "advanced",
      "kind": "conceptual",
      "prompt": "What is Progressive enhancement in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully. Your answer should explain internal behavior, guarantees, and the subtle limit senior engineers watch.",
      "skillsTested": [
        "Progressive enhancement",
        "Concept explanation",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** Progressive enhancement is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Working baseline first; enhancement is a bonus, not a requirement.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n- **Operation:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n- **Proof:** I would verify it by showing one expected Progressive enhancement behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee Progressive enhancement provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully. This names a concrete boundary and observable outcome rather than calling Progressive enhancement “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** The page still functions when a script fails, on slow networks, or for assistive technology. My final rule is **Working baseline first; enhancement is a bonus, not a requirement.**",
        "modelAnswer": "**Direct answer:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n\n**Production use:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n\n**Concrete example:** A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully.\n\n**How to verify it:** Showing one expected Progressive enhancement behavior and one boundary case, then connecting both to observable evidence\n\n**Trade-off and limit:** For Progressive enhancement at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.",
        "explanation": "**Why this answer is correct:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n\n**In plain language:** Working baseline first; enhancement is a bonus, not a requirement.\n\n**How it applies in production:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n\n**Evidence to ask for:** Showing one expected Progressive enhancement behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Explain internal behavior, guarantees, and the subtle limit senior engineers watch.",
        "keyPoints": [
          "**Remember:** Working baseline first; enhancement is a bonus, not a requirement.",
          "Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences.",
          "Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled."
        ],
        "realWorldExample": "🏭 **In production:** A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully.\n\n✅ **Why this is a strong answer:** It connects Progressive enhancement to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Progressive enhancement without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-02",
      "level": "advanced",
      "kind": "practical",
      "prompt": "How would you use Web Fundamentals to achieve the Semantic HTML outcome below for a commerce page used across mobile devices and assistive technology? Target outcome: A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code. Your answer should implement for high scale with bounded resources and measurable correctness.",
      "skillsTested": [
        "Semantic HTML",
        "Implementation and testing",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would use Semantic HTML through the smallest production-safe configuration that meets the stated outcome. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Use the element whose built-in contract matches the job.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n- **Operation:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n- **Proof:** I would verify it by testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics.\n\n**Implementation:** I begin with the smallest configuration that can meet the stated outcome, keep access and resource scope narrow, and make invalid input or unsupported states fail clearly. I then add the operational controls in the same change: ownership, useful telemetry, bounded resource use, and a recovery path. Each extra component must address a named requirement rather than anticipated future complexity.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code. This names a concrete boundary and observable outcome rather than calling Semantic HTML “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Choose elements for purpose, then style them. My final rule is **Use the element whose built-in contract matches the job.**",
        "modelAnswer": "**Recommended approach:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n\n**Implementation:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n\n**Concrete example:** A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code.\n\n**How to test it:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**Trade-off and limit:** For Semantic HTML at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions.",
        "explanation": "**Why this answer is correct:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n\n**In plain language:** Use the element whose built-in contract matches the job.\n\n**How it applies in production:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n\n**Evidence to ask for:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**What a complete answer includes:** Implement for high scale with bounded resources and measurable correctness.",
        "keyPoints": [
          "**Remember:** Use the element whose built-in contract matches the job.",
          "Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs.",
          "Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions."
        ],
        "realWorldExample": "🏭 **In production:** A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code.\n\n✅ **Why this is a strong answer:** It connects Semantic HTML to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Semantic HTML without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "code": {
          "language": "html",
          "source": "<fieldset>\n  <legend>Delivery speed</legend>\n  <label><input type=\"radio\" name=\"speed\" value=\"standard\"> Standard</label>\n  <label><input type=\"radio\" name=\"speed\" value=\"express\"> Express</label>\n</fieldset>"
        }
      }
    },
    {
      "id": "web-fundamentals-advanced-08",
      "level": "advanced",
      "kind": "practical",
      "prompt": "How would you use Web Fundamentals to achieve the CSS layout outcome below for a commerce page used across mobile devices and assistive technology? Target outcome: A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks. Your answer should implement for high scale with bounded resources and measurable correctness.",
      "skillsTested": [
        "CSS layout",
        "Implementation and testing",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would use CSS layout through the smallest production-safe configuration that meets the stated outcome. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Let content size first; constrain only where necessary.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n- **Operation:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n- **Proof:** I would verify it by testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics.\n\n**Implementation:** I begin with the smallest configuration that can meet the stated outcome, keep access and resource scope narrow, and make invalid input or unsupported states fail clearly. I then add the operational controls in the same change: ownership, useful telemetry, bounded resource use, and a recovery path. Each extra component must address a named requirement rather than anticipated future complexity.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks. This names a concrete boundary and observable outcome rather than calling CSS layout “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Responsive design should follow content breakpoints rather than device names. My final rule is **Let content size first; constrain only where necessary.**",
        "modelAnswer": "**Recommended approach:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n\n**Implementation:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n\n**Concrete example:** A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks.\n\n**How to test it:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**Trade-off and limit:** For CSS layout at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.",
        "explanation": "**Why this answer is correct:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n\n**In plain language:** Let content size first; constrain only where necessary.\n\n**How it applies in production:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n\n**Evidence to ask for:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**What a complete answer includes:** Implement for high scale with bounded resources and measurable correctness.",
        "keyPoints": [
          "**Remember:** Let content size first; constrain only where necessary.",
          "CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space.",
          "Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions."
        ],
        "realWorldExample": "🏭 **In production:** A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks.\n\n✅ **Why this is a strong answer:** It connects CSS layout to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming CSS layout without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-14",
      "level": "advanced",
      "kind": "practical",
      "prompt": "How would you use Web Fundamentals to achieve the Accessibility outcome below for a commerce page used across mobile devices and assistive technology? Target outcome: Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard. Your answer should implement for high scale with bounded resources and measurable correctness.",
      "skillsTested": [
        "Accessibility",
        "Implementation and testing",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would use Accessibility through the smallest production-safe configuration that meets the stated outcome. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Name it, focus it, announce it, and keep it operable.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n- **Operation:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n- **Proof:** I would verify it by testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics.\n\n**Implementation:** I begin with the smallest configuration that can meet the stated outcome, keep access and resource scope narrow, and make invalid input or unsupported states fail clearly. I then add the operational controls in the same change: ownership, useful telemetry, bounded resource use, and a recovery path. Each extra component must address a named requirement rather than anticipated future complexity.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard. This names a concrete boundary and observable outcome rather than calling Accessibility “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** WCAG is a testable baseline, not a substitute for user testing. My final rule is **Name it, focus it, announce it, and keep it operable.**",
        "modelAnswer": "**Recommended approach:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n\n**Implementation:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n\n**Concrete example:** Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard.\n\n**How to test it:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**Trade-off and limit:** For Accessibility at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size.",
        "explanation": "**Why this answer is correct:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n\n**In plain language:** Name it, focus it, announce it, and keep it operable.\n\n**How it applies in production:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n\n**Evidence to ask for:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**What a complete answer includes:** Implement for high scale with bounded resources and measurable correctness.",
        "keyPoints": [
          "**Remember:** Name it, focus it, announce it, and keep it operable.",
          "Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods.",
          "Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size."
        ],
        "realWorldExample": "🏭 **In production:** Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard.\n\n✅ **Why this is a strong answer:** It connects Accessibility to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Accessibility without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-20",
      "level": "advanced",
      "kind": "practical",
      "prompt": "How would you use Web Fundamentals to achieve the HTTP and browsers outcome below for a commerce page used across mobile devices and assistive technology? Target outcome: A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck. Your answer should implement for high scale with bounded resources and measurable correctness.",
      "skillsTested": [
        "HTTP and browsers",
        "Implementation and testing",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would use HTTP and browsers through the smallest production-safe configuration that meets the stated outcome. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Follow the request from name lookup to pixels.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n- **Operation:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n- **Proof:** I would verify it by testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics.\n\n**Implementation:** I begin with the smallest configuration that can meet the stated outcome, keep access and resource scope narrow, and make invalid input or unsupported states fail clearly. I then add the operational controls in the same change: ownership, useful telemetry, bounded resource use, and a recovery path. Each extra component must address a named requirement rather than anticipated future complexity.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck. This names a concrete boundary and observable outcome rather than calling HTTP and browsers “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Caching and connection reuse can matter more than JavaScript micro-optimizations. My final rule is **Follow the request from name lookup to pixels.**",
        "modelAnswer": "**Recommended approach:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n\n**Implementation:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n\n**Concrete example:** A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck.\n\n**How to test it:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**Trade-off and limit:** For HTTP and browsers at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays.",
        "explanation": "**Why this answer is correct:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n\n**In plain language:** Follow the request from name lookup to pixels.\n\n**How it applies in production:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n\n**Evidence to ask for:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**What a complete answer includes:** Implement for high scale with bounded resources and measurable correctness.",
        "keyPoints": [
          "**Remember:** Follow the request from name lookup to pixels.",
          "A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites.",
          "Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays."
        ],
        "realWorldExample": "🏭 **In production:** A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck.\n\n✅ **Why this is a strong answer:** It connects HTTP and browsers to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming HTTP and browsers without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-26",
      "level": "advanced",
      "kind": "practical",
      "prompt": "How would you use Web Fundamentals to achieve the Core Web Vitals outcome below for a commerce page used across mobile devices and assistive technology? Target outcome: A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP. Your answer should implement for high scale with bounded resources and measurable correctness.",
      "skillsTested": [
        "Core Web Vitals",
        "Implementation and testing",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would use Core Web Vitals through the smallest production-safe configuration that meets the stated outcome. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Load the main thing, respond quickly, move nothing unexpectedly.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n- **Operation:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n- **Proof:** I would verify it by testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics.\n\n**Implementation:** I begin with the smallest configuration that can meet the stated outcome, keep access and resource scope narrow, and make invalid input or unsupported states fail clearly. I then add the operational controls in the same change: ownership, useful telemetry, bounded resource use, and a recovery path. Each extra component must address a named requirement rather than anticipated future complexity.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP. This names a concrete boundary and observable outcome rather than calling Core Web Vitals “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Field data matters because lab hardware and networks are only controlled samples. My final rule is **Load the main thing, respond quickly, move nothing unexpectedly.**",
        "modelAnswer": "**Recommended approach:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n\n**Implementation:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n\n**Concrete example:** A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP.\n\n**How to test it:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**Trade-off and limit:** For Core Web Vitals at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content.",
        "explanation": "**Why this answer is correct:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n\n**In plain language:** Load the main thing, respond quickly, move nothing unexpectedly.\n\n**How it applies in production:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n\n**Evidence to ask for:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**What a complete answer includes:** Implement for high scale with bounded resources and measurable correctness.",
        "keyPoints": [
          "**Remember:** Load the main thing, respond quickly, move nothing unexpectedly.",
          "Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability.",
          "Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content."
        ],
        "realWorldExample": "🏭 **In production:** A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP.\n\n✅ **Why this is a strong answer:** It connects Core Web Vitals to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Core Web Vitals without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-32",
      "level": "advanced",
      "kind": "practical",
      "prompt": "How would you use Web Fundamentals to achieve the Forms and validation outcome below for a commerce page used across mobile devices and assistive technology? Target outcome: A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order. Your answer should implement for high scale with bounded resources and measurable correctness.",
      "skillsTested": [
        "Forms and validation",
        "Implementation and testing",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would use Forms and validation through the smallest production-safe configuration that meets the stated outcome. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Validate near the user for feedback, at the server for trust.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n- **Operation:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n- **Proof:** I would verify it by testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics.\n\n**Implementation:** I begin with the smallest configuration that can meet the stated outcome, keep access and resource scope narrow, and make invalid input or unsupported states fail clearly. I then add the operational controls in the same change: ownership, useful telemetry, bounded resource use, and a recovery path. Each extra component must address a named requirement rather than anticipated future complexity.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order. This names a concrete boundary and observable outcome rather than calling Forms and validation “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted. My final rule is **Validate near the user for feedback, at the server for trust.**",
        "modelAnswer": "**Recommended approach:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n\n**Implementation:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n\n**Concrete example:** A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order.\n\n**How to test it:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**Trade-off and limit:** For Forms and validation at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.",
        "explanation": "**Why this answer is correct:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n\n**In plain language:** Validate near the user for feedback, at the server for trust.\n\n**How it applies in production:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n\n**Evidence to ask for:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**What a complete answer includes:** Implement for high scale with bounded resources and measurable correctness.",
        "keyPoints": [
          "**Remember:** Validate near the user for feedback, at the server for trust.",
          "HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility.",
          "Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server."
        ],
        "realWorldExample": "🏭 **In production:** A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order.\n\n✅ **Why this is a strong answer:** It connects Forms and validation to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Forms and validation without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-38",
      "level": "advanced",
      "kind": "practical",
      "prompt": "How would you use Web Fundamentals to achieve the Browser JavaScript outcome below for a commerce page used across mobile devices and assistive technology? Target outcome: A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke. Your answer should implement for high scale with bounded resources and measurable correctness.",
      "skillsTested": [
        "Browser JavaScript",
        "Implementation and testing",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would use Browser JavaScript through the smallest production-safe configuration that meets the stated outcome. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **The main thread renders and handles input; do not block it.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n- **Operation:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n- **Proof:** I would verify it by testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics.\n\n**Implementation:** I begin with the smallest configuration that can meet the stated outcome, keep access and resource scope narrow, and make invalid input or unsupported states fail clearly. I then add the operational controls in the same change: ownership, useful telemetry, bounded resource use, and a recovery path. Each extra component must address a named requirement rather than anticipated future complexity.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke. This names a concrete boundary and observable outcome rather than calling Browser JavaScript “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness. My final rule is **The main thread renders and handles input; do not block it.**",
        "modelAnswer": "**Recommended approach:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n\n**Implementation:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n\n**Concrete example:** A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke.\n\n**How to test it:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**Trade-off and limit:** For Browser JavaScript at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.",
        "explanation": "**Why this answer is correct:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n\n**In plain language:** The main thread renders and handles input; do not block it.\n\n**How it applies in production:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n\n**Evidence to ask for:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**What a complete answer includes:** Implement for high scale with bounded resources and measurable correctness.",
        "keyPoints": [
          "**Remember:** The main thread renders and handles input; do not block it.",
          "Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread.",
          "Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks."
        ],
        "realWorldExample": "🏭 **In production:** A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke.\n\n✅ **Why this is a strong answer:** It connects Browser JavaScript to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Browser JavaScript without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-44",
      "level": "advanced",
      "kind": "practical",
      "prompt": "How would you use Web Fundamentals to achieve the Web security outcome below for a commerce page used across mobile devices and assistive technology? Target outcome: A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts. Your answer should implement for high scale with bounded resources and measurable correctness.",
      "skillsTested": [
        "Web security",
        "Implementation and testing",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would use Web security through the smallest production-safe configuration that meets the stated outcome. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Never trust the client; escape output and lock down the browser.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n- **Operation:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n- **Proof:** I would verify it by testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics.\n\n**Implementation:** I begin with the smallest configuration that can meet the stated outcome, keep access and resource scope narrow, and make invalid input or unsupported states fail clearly. I then add the operational controls in the same change: ownership, useful telemetry, bounded resource use, and a recovery path. Each extra component must address a named requirement rather than anticipated future complexity.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts. This names a concrete boundary and observable outcome rather than calling Web security “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls. My final rule is **Never trust the client; escape output and lock down the browser.**",
        "modelAnswer": "**Recommended approach:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n\n**Implementation:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n\n**Concrete example:** A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts.\n\n**How to test it:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**Trade-off and limit:** For Web security at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.",
        "explanation": "**Why this answer is correct:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n\n**In plain language:** Never trust the client; escape output and lock down the browser.\n\n**How it applies in production:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n\n**Evidence to ask for:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**What a complete answer includes:** Implement for high scale with bounded resources and measurable correctness.",
        "keyPoints": [
          "**Remember:** Never trust the client; escape output and lock down the browser.",
          "Web security depends on not trusting the client and controlling what the browser is allowed to do.",
          "Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins."
        ],
        "realWorldExample": "🏭 **In production:** A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts.\n\n✅ **Why this is a strong answer:** It connects Web security to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Web security without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-50",
      "level": "advanced",
      "kind": "practical",
      "prompt": "How would you use Web Fundamentals to achieve the Caching and CDNs outcome below for a commerce page used across mobile devices and assistive technology? Target outcome: A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect. Your answer should implement for high scale with bounded resources and measurable correctness.",
      "skillsTested": [
        "Caching and CDNs",
        "Implementation and testing",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would use Caching and CDNs through the smallest production-safe configuration that meets the stated outcome. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Cache immutable assets hard; revalidate the documents that change.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n- **Operation:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n- **Proof:** I would verify it by testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics.\n\n**Implementation:** I begin with the smallest configuration that can meet the stated outcome, keep access and resource scope narrow, and make invalid input or unsupported states fail clearly. I then add the operational controls in the same change: ownership, useful telemetry, bounded resource use, and a recovery path. Each extra component must address a named requirement rather than anticipated future complexity.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect. This names a concrete boundary and observable outcome rather than calling Caching and CDNs “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely. My final rule is **Cache immutable assets hard; revalidate the documents that change.**",
        "modelAnswer": "**Recommended approach:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n\n**Implementation:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n\n**Concrete example:** A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect.\n\n**How to test it:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**Trade-off and limit:** For Caching and CDNs at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.",
        "explanation": "**Why this answer is correct:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n\n**In plain language:** Cache immutable assets hard; revalidate the documents that change.\n\n**How it applies in production:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n\n**Evidence to ask for:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**What a complete answer includes:** Implement for high scale with bounded resources and measurable correctness.",
        "keyPoints": [
          "**Remember:** Cache immutable assets hard; revalidate the documents that change.",
          "Browser and CDN caching avoid re-fetching unchanged resources.",
          "Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change."
        ],
        "realWorldExample": "🏭 **In production:** A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect.\n\n✅ **Why this is a strong answer:** It connects Caching and CDNs to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Caching and CDNs without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-56",
      "level": "advanced",
      "kind": "practical",
      "prompt": "How would you use Web Fundamentals to achieve the Progressive enhancement outcome below for a commerce page used across mobile devices and assistive technology? Target outcome: A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully. Your answer should implement for high scale with bounded resources and measurable correctness.",
      "skillsTested": [
        "Progressive enhancement",
        "Implementation and testing",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would use Progressive enhancement through the smallest production-safe configuration that meets the stated outcome. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Working baseline first; enhancement is a bonus, not a requirement.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n- **Operation:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n- **Proof:** I would verify it by testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics.\n\n**Implementation:** I begin with the smallest configuration that can meet the stated outcome, keep access and resource scope narrow, and make invalid input or unsupported states fail clearly. I then add the operational controls in the same change: ownership, useful telemetry, bounded resource use, and a recovery path. Each extra component must address a named requirement rather than anticipated future complexity.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully. This names a concrete boundary and observable outcome rather than calling Progressive enhancement “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** The page still functions when a script fails, on slow networks, or for assistive technology. My final rule is **Working baseline first; enhancement is a bonus, not a requirement.**",
        "modelAnswer": "**Recommended approach:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n\n**Implementation:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n\n**Concrete example:** A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully.\n\n**How to test it:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**Trade-off and limit:** For Progressive enhancement at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.",
        "explanation": "**Why this answer is correct:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n\n**In plain language:** Working baseline first; enhancement is a bonus, not a requirement.\n\n**How it applies in production:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n\n**Evidence to ask for:** Testing the intended result, rejected invalid or unauthorized input, one dependency failure, recovery, and the relevant logs or metrics\n\n**What a complete answer includes:** Implement for high scale with bounded resources and measurable correctness.",
        "keyPoints": [
          "**Remember:** Working baseline first; enhancement is a bonus, not a requirement.",
          "Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences.",
          "Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled."
        ],
        "realWorldExample": "🏭 **In production:** A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully.\n\n✅ **Why this is a strong answer:** It connects Progressive enhancement to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Progressive enhancement without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-03",
      "level": "advanced",
      "kind": "scenario",
      "prompt": "Review this Web Fundamentals proposal for a commerce page used across mobile devices and assistive technology: A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code. Focusing on Semantic HTML, would you approve it, change it, or reject it? Your answer should choose between credible alternatives under latency, scale, security, and cost constraints.",
      "skillsTested": [
        "Semantic HTML",
        "Scenario trade-offs",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would make the Semantic HTML decision conditionally, after its assumptions, failure behavior, and rollback are explicit. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Use the element whose built-in contract matches the job.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n- **Operation:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n- **Proof:** I would verify it by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Decision:** I would approve the direction only after making its assumptions explicit. I would confirm the workload shape, required guarantee, trust boundary, failure tolerance, recovery target, and cost ceiling. Then I would compare the proposal with the simplest credible alternative. The winning option is the one that meets those requirements with fewer failure modes, not the option with the longest feature list.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code. This names a concrete boundary and observable outcome rather than calling Semantic HTML “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Choose elements for purpose, then style them. My final rule is **Use the element whose built-in contract matches the job.**",
        "modelAnswer": "**Recommendation:** I would approve this direction, subject to the assumptions below. Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n\n**Production approach:** I would use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n\n**Concrete example:** A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code.\n\n**Alternative and proof:** I would reject a broader or more expensive option unless a measured requirement justifies it. I would prove this choice by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Assumptions and rollback:** For Semantic HTML at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions.",
        "explanation": "**Why this answer is correct:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n\n**In plain language:** Use the element whose built-in contract matches the job.\n\n**How it applies in production:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n\n**Evidence to ask for:** Checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement\n\n**What a complete answer includes:** Choose between credible alternatives under latency, scale, security, and cost constraints.",
        "keyPoints": [
          "**Remember:** Use the element whose built-in contract matches the job.",
          "Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs.",
          "Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions."
        ],
        "realWorldExample": "🏭 **In production:** A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code.\n\n✅ **Why this is a strong answer:** It connects Semantic HTML to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Semantic HTML without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Semantic HTML: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs."
            },
            {
              "label": "Protect",
              "detail": "Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-advanced-09",
      "level": "advanced",
      "kind": "scenario",
      "prompt": "Review this Web Fundamentals proposal for a commerce page used across mobile devices and assistive technology: A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks. Focusing on CSS layout, would you approve it, change it, or reject it? Your answer should choose between credible alternatives under latency, scale, security, and cost constraints.",
      "skillsTested": [
        "CSS layout",
        "Scenario trade-offs",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would make the CSS layout decision conditionally, after its assumptions, failure behavior, and rollback are explicit. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Let content size first; constrain only where necessary.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n- **Operation:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n- **Proof:** I would verify it by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Decision:** I would approve the direction only after making its assumptions explicit. I would confirm the workload shape, required guarantee, trust boundary, failure tolerance, recovery target, and cost ceiling. Then I would compare the proposal with the simplest credible alternative. The winning option is the one that meets those requirements with fewer failure modes, not the option with the longest feature list.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks. This names a concrete boundary and observable outcome rather than calling CSS layout “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Responsive design should follow content breakpoints rather than device names. My final rule is **Let content size first; constrain only where necessary.**",
        "modelAnswer": "**Recommendation:** I would approve this direction, subject to the assumptions below. CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n\n**Production approach:** I would prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n\n**Concrete example:** A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks.\n\n**Alternative and proof:** I would reject a broader or more expensive option unless a measured requirement justifies it. I would prove this choice by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Assumptions and rollback:** For CSS layout at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.",
        "explanation": "**Why this answer is correct:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n\n**In plain language:** Let content size first; constrain only where necessary.\n\n**How it applies in production:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n\n**Evidence to ask for:** Checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement\n\n**What a complete answer includes:** Choose between credible alternatives under latency, scale, security, and cost constraints.",
        "keyPoints": [
          "**Remember:** Let content size first; constrain only where necessary.",
          "CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space.",
          "Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions."
        ],
        "realWorldExample": "🏭 **In production:** A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks.\n\n✅ **Why this is a strong answer:** It connects CSS layout to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming CSS layout without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "CSS layout: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space."
            },
            {
              "label": "Protect",
              "detail": "Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-advanced-15",
      "level": "advanced",
      "kind": "scenario",
      "prompt": "Review this Web Fundamentals proposal for a commerce page used across mobile devices and assistive technology: Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard. Focusing on Accessibility, would you approve it, change it, or reject it? Your answer should choose between credible alternatives under latency, scale, security, and cost constraints.",
      "skillsTested": [
        "Accessibility",
        "Scenario trade-offs",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would make the Accessibility decision conditionally, after its assumptions, failure behavior, and rollback are explicit. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Name it, focus it, announce it, and keep it operable.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n- **Operation:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n- **Proof:** I would verify it by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Decision:** I would approve the direction only after making its assumptions explicit. I would confirm the workload shape, required guarantee, trust boundary, failure tolerance, recovery target, and cost ceiling. Then I would compare the proposal with the simplest credible alternative. The winning option is the one that meets those requirements with fewer failure modes, not the option with the longest feature list.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard. This names a concrete boundary and observable outcome rather than calling Accessibility “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** WCAG is a testable baseline, not a substitute for user testing. My final rule is **Name it, focus it, announce it, and keep it operable.**",
        "modelAnswer": "**Recommendation:** I would approve this direction, subject to the assumptions below. Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n\n**Production approach:** I would preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n\n**Concrete example:** Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard.\n\n**Alternative and proof:** I would reject a broader or more expensive option unless a measured requirement justifies it. I would prove this choice by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Assumptions and rollback:** For Accessibility at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size.",
        "explanation": "**Why this answer is correct:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n\n**In plain language:** Name it, focus it, announce it, and keep it operable.\n\n**How it applies in production:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n\n**Evidence to ask for:** Checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement\n\n**What a complete answer includes:** Choose between credible alternatives under latency, scale, security, and cost constraints.",
        "keyPoints": [
          "**Remember:** Name it, focus it, announce it, and keep it operable.",
          "Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods.",
          "Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size."
        ],
        "realWorldExample": "🏭 **In production:** Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard.\n\n✅ **Why this is a strong answer:** It connects Accessibility to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Accessibility without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Accessibility: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods."
            },
            {
              "label": "Protect",
              "detail": "Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-advanced-21",
      "level": "advanced",
      "kind": "scenario",
      "prompt": "Review this Web Fundamentals proposal for a commerce page used across mobile devices and assistive technology: A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck. Focusing on HTTP and browsers, would you approve it, change it, or reject it? Your answer should choose between credible alternatives under latency, scale, security, and cost constraints.",
      "skillsTested": [
        "HTTP and browsers",
        "Scenario trade-offs",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would make the HTTP and browsers decision conditionally, after its assumptions, failure behavior, and rollback are explicit. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Follow the request from name lookup to pixels.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n- **Operation:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n- **Proof:** I would verify it by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Decision:** I would approve the direction only after making its assumptions explicit. I would confirm the workload shape, required guarantee, trust boundary, failure tolerance, recovery target, and cost ceiling. Then I would compare the proposal with the simplest credible alternative. The winning option is the one that meets those requirements with fewer failure modes, not the option with the longest feature list.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck. This names a concrete boundary and observable outcome rather than calling HTTP and browsers “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Caching and connection reuse can matter more than JavaScript micro-optimizations. My final rule is **Follow the request from name lookup to pixels.**",
        "modelAnswer": "**Recommendation:** I would approve this direction, subject to the assumptions below. A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n\n**Production approach:** I would use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n\n**Concrete example:** A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck.\n\n**Alternative and proof:** I would reject a broader or more expensive option unless a measured requirement justifies it. I would prove this choice by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Assumptions and rollback:** For HTTP and browsers at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays.",
        "explanation": "**Why this answer is correct:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n\n**In plain language:** Follow the request from name lookup to pixels.\n\n**How it applies in production:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n\n**Evidence to ask for:** Checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement\n\n**What a complete answer includes:** Choose between credible alternatives under latency, scale, security, and cost constraints.",
        "keyPoints": [
          "**Remember:** Follow the request from name lookup to pixels.",
          "A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites.",
          "Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays."
        ],
        "realWorldExample": "🏭 **In production:** A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck.\n\n✅ **Why this is a strong answer:** It connects HTTP and browsers to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming HTTP and browsers without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "HTTP and browsers: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites."
            },
            {
              "label": "Protect",
              "detail": "Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-advanced-27",
      "level": "advanced",
      "kind": "scenario",
      "prompt": "Review this Web Fundamentals proposal for a commerce page used across mobile devices and assistive technology: A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP. Focusing on Core Web Vitals, would you approve it, change it, or reject it? Your answer should choose between credible alternatives under latency, scale, security, and cost constraints.",
      "skillsTested": [
        "Core Web Vitals",
        "Scenario trade-offs",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would make the Core Web Vitals decision conditionally, after its assumptions, failure behavior, and rollback are explicit. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Load the main thing, respond quickly, move nothing unexpectedly.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n- **Operation:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n- **Proof:** I would verify it by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Decision:** I would approve the direction only after making its assumptions explicit. I would confirm the workload shape, required guarantee, trust boundary, failure tolerance, recovery target, and cost ceiling. Then I would compare the proposal with the simplest credible alternative. The winning option is the one that meets those requirements with fewer failure modes, not the option with the longest feature list.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP. This names a concrete boundary and observable outcome rather than calling Core Web Vitals “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Field data matters because lab hardware and networks are only controlled samples. My final rule is **Load the main thing, respond quickly, move nothing unexpectedly.**",
        "modelAnswer": "**Recommendation:** I would approve this direction, subject to the assumptions below. Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n\n**Production approach:** I would attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n\n**Concrete example:** A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP.\n\n**Alternative and proof:** I would reject a broader or more expensive option unless a measured requirement justifies it. I would prove this choice by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Assumptions and rollback:** For Core Web Vitals at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content.",
        "explanation": "**Why this answer is correct:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n\n**In plain language:** Load the main thing, respond quickly, move nothing unexpectedly.\n\n**How it applies in production:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n\n**Evidence to ask for:** Checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement\n\n**What a complete answer includes:** Choose between credible alternatives under latency, scale, security, and cost constraints.",
        "keyPoints": [
          "**Remember:** Load the main thing, respond quickly, move nothing unexpectedly.",
          "Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability.",
          "Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content."
        ],
        "realWorldExample": "🏭 **In production:** A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP.\n\n✅ **Why this is a strong answer:** It connects Core Web Vitals to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Core Web Vitals without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Core Web Vitals: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability."
            },
            {
              "label": "Protect",
              "detail": "Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-advanced-33",
      "level": "advanced",
      "kind": "scenario",
      "prompt": "Review this Web Fundamentals proposal for a commerce page used across mobile devices and assistive technology: A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order. Focusing on Forms and validation, would you approve it, change it, or reject it? Your answer should choose between credible alternatives under latency, scale, security, and cost constraints.",
      "skillsTested": [
        "Forms and validation",
        "Scenario trade-offs",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would make the Forms and validation decision conditionally, after its assumptions, failure behavior, and rollback are explicit. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Validate near the user for feedback, at the server for trust.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n- **Operation:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n- **Proof:** I would verify it by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Decision:** I would approve the direction only after making its assumptions explicit. I would confirm the workload shape, required guarantee, trust boundary, failure tolerance, recovery target, and cost ceiling. Then I would compare the proposal with the simplest credible alternative. The winning option is the one that meets those requirements with fewer failure modes, not the option with the longest feature list.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order. This names a concrete boundary and observable outcome rather than calling Forms and validation “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted. My final rule is **Validate near the user for feedback, at the server for trust.**",
        "modelAnswer": "**Recommendation:** I would approve this direction, subject to the assumptions below. HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n\n**Production approach:** I would use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n\n**Concrete example:** A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order.\n\n**Alternative and proof:** I would reject a broader or more expensive option unless a measured requirement justifies it. I would prove this choice by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Assumptions and rollback:** For Forms and validation at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.",
        "explanation": "**Why this answer is correct:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n\n**In plain language:** Validate near the user for feedback, at the server for trust.\n\n**How it applies in production:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n\n**Evidence to ask for:** Checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement\n\n**What a complete answer includes:** Choose between credible alternatives under latency, scale, security, and cost constraints.",
        "keyPoints": [
          "**Remember:** Validate near the user for feedback, at the server for trust.",
          "HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility.",
          "Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server."
        ],
        "realWorldExample": "🏭 **In production:** A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order.\n\n✅ **Why this is a strong answer:** It connects Forms and validation to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Forms and validation without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Forms and validation: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility."
            },
            {
              "label": "Protect",
              "detail": "Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-advanced-39",
      "level": "advanced",
      "kind": "scenario",
      "prompt": "Review this Web Fundamentals proposal for a commerce page used across mobile devices and assistive technology: A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke. Focusing on Browser JavaScript, would you approve it, change it, or reject it? Your answer should choose between credible alternatives under latency, scale, security, and cost constraints.",
      "skillsTested": [
        "Browser JavaScript",
        "Scenario trade-offs",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would make the Browser JavaScript decision conditionally, after its assumptions, failure behavior, and rollback are explicit. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **The main thread renders and handles input; do not block it.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n- **Operation:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n- **Proof:** I would verify it by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Decision:** I would approve the direction only after making its assumptions explicit. I would confirm the workload shape, required guarantee, trust boundary, failure tolerance, recovery target, and cost ceiling. Then I would compare the proposal with the simplest credible alternative. The winning option is the one that meets those requirements with fewer failure modes, not the option with the longest feature list.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke. This names a concrete boundary and observable outcome rather than calling Browser JavaScript “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness. My final rule is **The main thread renders and handles input; do not block it.**",
        "modelAnswer": "**Recommendation:** I would approve this direction, subject to the assumptions below. Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n\n**Production approach:** I would delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n\n**Concrete example:** A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke.\n\n**Alternative and proof:** I would reject a broader or more expensive option unless a measured requirement justifies it. I would prove this choice by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Assumptions and rollback:** For Browser JavaScript at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.",
        "explanation": "**Why this answer is correct:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n\n**In plain language:** The main thread renders and handles input; do not block it.\n\n**How it applies in production:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n\n**Evidence to ask for:** Checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement\n\n**What a complete answer includes:** Choose between credible alternatives under latency, scale, security, and cost constraints.",
        "keyPoints": [
          "**Remember:** The main thread renders and handles input; do not block it.",
          "Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread.",
          "Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks."
        ],
        "realWorldExample": "🏭 **In production:** A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke.\n\n✅ **Why this is a strong answer:** It connects Browser JavaScript to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Browser JavaScript without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Browser JavaScript: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread."
            },
            {
              "label": "Protect",
              "detail": "Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-advanced-45",
      "level": "advanced",
      "kind": "scenario",
      "prompt": "Review this Web Fundamentals proposal for a commerce page used across mobile devices and assistive technology: A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts. Focusing on Web security, would you approve it, change it, or reject it? Your answer should choose between credible alternatives under latency, scale, security, and cost constraints.",
      "skillsTested": [
        "Web security",
        "Scenario trade-offs",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would make the Web security decision conditionally, after its assumptions, failure behavior, and rollback are explicit. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Never trust the client; escape output and lock down the browser.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n- **Operation:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n- **Proof:** I would verify it by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Decision:** I would approve the direction only after making its assumptions explicit. I would confirm the workload shape, required guarantee, trust boundary, failure tolerance, recovery target, and cost ceiling. Then I would compare the proposal with the simplest credible alternative. The winning option is the one that meets those requirements with fewer failure modes, not the option with the longest feature list.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts. This names a concrete boundary and observable outcome rather than calling Web security “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls. My final rule is **Never trust the client; escape output and lock down the browser.**",
        "modelAnswer": "**Recommendation:** I would approve this direction, subject to the assumptions below. Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n\n**Production approach:** I would escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n\n**Concrete example:** A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts.\n\n**Alternative and proof:** I would reject a broader or more expensive option unless a measured requirement justifies it. I would prove this choice by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Assumptions and rollback:** For Web security at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.",
        "explanation": "**Why this answer is correct:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n\n**In plain language:** Never trust the client; escape output and lock down the browser.\n\n**How it applies in production:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n\n**Evidence to ask for:** Checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement\n\n**What a complete answer includes:** Choose between credible alternatives under latency, scale, security, and cost constraints.",
        "keyPoints": [
          "**Remember:** Never trust the client; escape output and lock down the browser.",
          "Web security depends on not trusting the client and controlling what the browser is allowed to do.",
          "Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins."
        ],
        "realWorldExample": "🏭 **In production:** A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts.\n\n✅ **Why this is a strong answer:** It connects Web security to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Web security without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Web security: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Web security depends on not trusting the client and controlling what the browser is allowed to do."
            },
            {
              "label": "Protect",
              "detail": "Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-advanced-51",
      "level": "advanced",
      "kind": "scenario",
      "prompt": "Review this Web Fundamentals proposal for a commerce page used across mobile devices and assistive technology: A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect. Focusing on Caching and CDNs, would you approve it, change it, or reject it? Your answer should choose between credible alternatives under latency, scale, security, and cost constraints.",
      "skillsTested": [
        "Caching and CDNs",
        "Scenario trade-offs",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would make the Caching and CDNs decision conditionally, after its assumptions, failure behavior, and rollback are explicit. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Cache immutable assets hard; revalidate the documents that change.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n- **Operation:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n- **Proof:** I would verify it by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Decision:** I would approve the direction only after making its assumptions explicit. I would confirm the workload shape, required guarantee, trust boundary, failure tolerance, recovery target, and cost ceiling. Then I would compare the proposal with the simplest credible alternative. The winning option is the one that meets those requirements with fewer failure modes, not the option with the longest feature list.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect. This names a concrete boundary and observable outcome rather than calling Caching and CDNs “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely. My final rule is **Cache immutable assets hard; revalidate the documents that change.**",
        "modelAnswer": "**Recommendation:** I would approve this direction, subject to the assumptions below. Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n\n**Production approach:** I would fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n\n**Concrete example:** A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect.\n\n**Alternative and proof:** I would reject a broader or more expensive option unless a measured requirement justifies it. I would prove this choice by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Assumptions and rollback:** For Caching and CDNs at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.",
        "explanation": "**Why this answer is correct:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n\n**In plain language:** Cache immutable assets hard; revalidate the documents that change.\n\n**How it applies in production:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n\n**Evidence to ask for:** Checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement\n\n**What a complete answer includes:** Choose between credible alternatives under latency, scale, security, and cost constraints.",
        "keyPoints": [
          "**Remember:** Cache immutable assets hard; revalidate the documents that change.",
          "Browser and CDN caching avoid re-fetching unchanged resources.",
          "Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change."
        ],
        "realWorldExample": "🏭 **In production:** A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect.\n\n✅ **Why this is a strong answer:** It connects Caching and CDNs to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Caching and CDNs without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Caching and CDNs: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Browser and CDN caching avoid re-fetching unchanged resources."
            },
            {
              "label": "Protect",
              "detail": "Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-advanced-57",
      "level": "advanced",
      "kind": "scenario",
      "prompt": "Review this Web Fundamentals proposal for a commerce page used across mobile devices and assistive technology: A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully. Focusing on Progressive enhancement, would you approve it, change it, or reject it? Your answer should choose between credible alternatives under latency, scale, security, and cost constraints.",
      "skillsTested": [
        "Progressive enhancement",
        "Scenario trade-offs",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would make the Progressive enhancement decision conditionally, after its assumptions, failure behavior, and rollback are explicit. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Working baseline first; enhancement is a bonus, not a requirement.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n- **Operation:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n- **Proof:** I would verify it by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Decision:** I would approve the direction only after making its assumptions explicit. I would confirm the workload shape, required guarantee, trust boundary, failure tolerance, recovery target, and cost ceiling. Then I would compare the proposal with the simplest credible alternative. The winning option is the one that meets those requirements with fewer failure modes, not the option with the longest feature list.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully. This names a concrete boundary and observable outcome rather than calling Progressive enhancement “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** The page still functions when a script fails, on slow networks, or for assistive technology. My final rule is **Working baseline first; enhancement is a bonus, not a requirement.**",
        "modelAnswer": "**Recommendation:** I would approve this direction, subject to the assumptions below. Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n\n**Production approach:** I would make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n\n**Concrete example:** A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully.\n\n**Alternative and proof:** I would reject a broader or more expensive option unless a measured requirement justifies it. I would prove this choice by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Assumptions and rollback:** For Progressive enhancement at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.",
        "explanation": "**Why this answer is correct:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n\n**In plain language:** Working baseline first; enhancement is a bonus, not a requirement.\n\n**How it applies in production:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n\n**Evidence to ask for:** Checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement\n\n**What a complete answer includes:** Choose between credible alternatives under latency, scale, security, and cost constraints.",
        "keyPoints": [
          "**Remember:** Working baseline first; enhancement is a bonus, not a requirement.",
          "Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences.",
          "Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled."
        ],
        "realWorldExample": "🏭 **In production:** A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully.\n\n✅ **Why this is a strong answer:** It connects Progressive enhancement to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Progressive enhancement without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Progressive enhancement: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences."
            },
            {
              "label": "Protect",
              "detail": "Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-advanced-04",
      "level": "advanced",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code. Focusing on Semantic HTML, how would you investigate without guessing? Your answer should diagnose partial failure, contention, or saturation without guessing.",
      "skillsTested": [
        "Semantic HTML",
        "Evidence-based diagnosis",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the Semantic HTML failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Use the element whose built-in contract matches the job.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n- **Operation:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code. This names a concrete boundary and observable outcome rather than calling Semantic HTML “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Choose elements for purpose, then style them. My final rule is **Use the element whose built-in contract matches the job.**",
        "modelAnswer": "**Expected behavior:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** For Semantic HTML at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions.",
        "explanation": "**Why this answer is correct:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n\n**In plain language:** Use the element whose built-in contract matches the job.\n\n**How it applies in production:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Diagnose partial failure, contention, or saturation without guessing.",
        "keyPoints": [
          "**Remember:** Use the element whose built-in contract matches the job.",
          "Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs.",
          "Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions."
        ],
        "realWorldExample": "🏭 **In production:** A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code.\n\n✅ **Why this is a strong answer:** It connects Semantic HTML to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Semantic HTML without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-10",
      "level": "advanced",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks. Focusing on CSS layout, how would you investigate without guessing? Your answer should diagnose partial failure, contention, or saturation without guessing.",
      "skillsTested": [
        "CSS layout",
        "Evidence-based diagnosis",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the CSS layout failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Let content size first; constrain only where necessary.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n- **Operation:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks. This names a concrete boundary and observable outcome rather than calling CSS layout “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Responsive design should follow content breakpoints rather than device names. My final rule is **Let content size first; constrain only where necessary.**",
        "modelAnswer": "**Expected behavior:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** For CSS layout at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.",
        "explanation": "**Why this answer is correct:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n\n**In plain language:** Let content size first; constrain only where necessary.\n\n**How it applies in production:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Diagnose partial failure, contention, or saturation without guessing.",
        "keyPoints": [
          "**Remember:** Let content size first; constrain only where necessary.",
          "CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space.",
          "Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions."
        ],
        "realWorldExample": "🏭 **In production:** A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks.\n\n✅ **Why this is a strong answer:** It connects CSS layout to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming CSS layout without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-16",
      "level": "advanced",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard. Focusing on Accessibility, how would you investigate without guessing? Your answer should diagnose partial failure, contention, or saturation without guessing.",
      "skillsTested": [
        "Accessibility",
        "Evidence-based diagnosis",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the Accessibility failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Name it, focus it, announce it, and keep it operable.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n- **Operation:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard. This names a concrete boundary and observable outcome rather than calling Accessibility “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** WCAG is a testable baseline, not a substitute for user testing. My final rule is **Name it, focus it, announce it, and keep it operable.**",
        "modelAnswer": "**Expected behavior:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** For Accessibility at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size.",
        "explanation": "**Why this answer is correct:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n\n**In plain language:** Name it, focus it, announce it, and keep it operable.\n\n**How it applies in production:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Diagnose partial failure, contention, or saturation without guessing.",
        "keyPoints": [
          "**Remember:** Name it, focus it, announce it, and keep it operable.",
          "Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods.",
          "Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size."
        ],
        "realWorldExample": "🏭 **In production:** Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard.\n\n✅ **Why this is a strong answer:** It connects Accessibility to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Accessibility without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-22",
      "level": "advanced",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck. Focusing on HTTP and browsers, how would you investigate without guessing? Your answer should diagnose partial failure, contention, or saturation without guessing.",
      "skillsTested": [
        "HTTP and browsers",
        "Evidence-based diagnosis",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the HTTP and browsers failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Follow the request from name lookup to pixels.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n- **Operation:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck. This names a concrete boundary and observable outcome rather than calling HTTP and browsers “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Caching and connection reuse can matter more than JavaScript micro-optimizations. My final rule is **Follow the request from name lookup to pixels.**",
        "modelAnswer": "**Expected behavior:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** For HTTP and browsers at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays.",
        "explanation": "**Why this answer is correct:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n\n**In plain language:** Follow the request from name lookup to pixels.\n\n**How it applies in production:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Diagnose partial failure, contention, or saturation without guessing.",
        "keyPoints": [
          "**Remember:** Follow the request from name lookup to pixels.",
          "A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites.",
          "Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays."
        ],
        "realWorldExample": "🏭 **In production:** A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck.\n\n✅ **Why this is a strong answer:** It connects HTTP and browsers to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming HTTP and browsers without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-28",
      "level": "advanced",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP. Focusing on Core Web Vitals, how would you investigate without guessing? Your answer should diagnose partial failure, contention, or saturation without guessing.",
      "skillsTested": [
        "Core Web Vitals",
        "Evidence-based diagnosis",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the Core Web Vitals failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Load the main thing, respond quickly, move nothing unexpectedly.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n- **Operation:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP. This names a concrete boundary and observable outcome rather than calling Core Web Vitals “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Field data matters because lab hardware and networks are only controlled samples. My final rule is **Load the main thing, respond quickly, move nothing unexpectedly.**",
        "modelAnswer": "**Expected behavior:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** For Core Web Vitals at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content.",
        "explanation": "**Why this answer is correct:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n\n**In plain language:** Load the main thing, respond quickly, move nothing unexpectedly.\n\n**How it applies in production:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Diagnose partial failure, contention, or saturation without guessing.",
        "keyPoints": [
          "**Remember:** Load the main thing, respond quickly, move nothing unexpectedly.",
          "Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability.",
          "Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content."
        ],
        "realWorldExample": "🏭 **In production:** A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP.\n\n✅ **Why this is a strong answer:** It connects Core Web Vitals to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Core Web Vitals without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-34",
      "level": "advanced",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order. Focusing on Forms and validation, how would you investigate without guessing? Your answer should diagnose partial failure, contention, or saturation without guessing.",
      "skillsTested": [
        "Forms and validation",
        "Evidence-based diagnosis",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the Forms and validation failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Validate near the user for feedback, at the server for trust.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n- **Operation:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order. This names a concrete boundary and observable outcome rather than calling Forms and validation “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted. My final rule is **Validate near the user for feedback, at the server for trust.**",
        "modelAnswer": "**Expected behavior:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** For Forms and validation at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.",
        "explanation": "**Why this answer is correct:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n\n**In plain language:** Validate near the user for feedback, at the server for trust.\n\n**How it applies in production:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Diagnose partial failure, contention, or saturation without guessing.",
        "keyPoints": [
          "**Remember:** Validate near the user for feedback, at the server for trust.",
          "HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility.",
          "Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server."
        ],
        "realWorldExample": "🏭 **In production:** A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order.\n\n✅ **Why this is a strong answer:** It connects Forms and validation to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Forms and validation without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-40",
      "level": "advanced",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke. Focusing on Browser JavaScript, how would you investigate without guessing? Your answer should diagnose partial failure, contention, or saturation without guessing.",
      "skillsTested": [
        "Browser JavaScript",
        "Evidence-based diagnosis",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the Browser JavaScript failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **The main thread renders and handles input; do not block it.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n- **Operation:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke. This names a concrete boundary and observable outcome rather than calling Browser JavaScript “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness. My final rule is **The main thread renders and handles input; do not block it.**",
        "modelAnswer": "**Expected behavior:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** For Browser JavaScript at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.",
        "explanation": "**Why this answer is correct:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n\n**In plain language:** The main thread renders and handles input; do not block it.\n\n**How it applies in production:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Diagnose partial failure, contention, or saturation without guessing.",
        "keyPoints": [
          "**Remember:** The main thread renders and handles input; do not block it.",
          "Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread.",
          "Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks."
        ],
        "realWorldExample": "🏭 **In production:** A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke.\n\n✅ **Why this is a strong answer:** It connects Browser JavaScript to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Browser JavaScript without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-46",
      "level": "advanced",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts. Focusing on Web security, how would you investigate without guessing? Your answer should diagnose partial failure, contention, or saturation without guessing.",
      "skillsTested": [
        "Web security",
        "Evidence-based diagnosis",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the Web security failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Never trust the client; escape output and lock down the browser.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n- **Operation:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts. This names a concrete boundary and observable outcome rather than calling Web security “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls. My final rule is **Never trust the client; escape output and lock down the browser.**",
        "modelAnswer": "**Expected behavior:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** For Web security at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.",
        "explanation": "**Why this answer is correct:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n\n**In plain language:** Never trust the client; escape output and lock down the browser.\n\n**How it applies in production:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Diagnose partial failure, contention, or saturation without guessing.",
        "keyPoints": [
          "**Remember:** Never trust the client; escape output and lock down the browser.",
          "Web security depends on not trusting the client and controlling what the browser is allowed to do.",
          "Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins."
        ],
        "realWorldExample": "🏭 **In production:** A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts.\n\n✅ **Why this is a strong answer:** It connects Web security to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Web security without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-52",
      "level": "advanced",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect. Focusing on Caching and CDNs, how would you investigate without guessing? Your answer should diagnose partial failure, contention, or saturation without guessing.",
      "skillsTested": [
        "Caching and CDNs",
        "Evidence-based diagnosis",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the Caching and CDNs failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Cache immutable assets hard; revalidate the documents that change.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n- **Operation:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect. This names a concrete boundary and observable outcome rather than calling Caching and CDNs “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely. My final rule is **Cache immutable assets hard; revalidate the documents that change.**",
        "modelAnswer": "**Expected behavior:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** For Caching and CDNs at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.",
        "explanation": "**Why this answer is correct:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n\n**In plain language:** Cache immutable assets hard; revalidate the documents that change.\n\n**How it applies in production:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Diagnose partial failure, contention, or saturation without guessing.",
        "keyPoints": [
          "**Remember:** Cache immutable assets hard; revalidate the documents that change.",
          "Browser and CDN caching avoid re-fetching unchanged resources.",
          "Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change."
        ],
        "realWorldExample": "🏭 **In production:** A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect.\n\n✅ **Why this is a strong answer:** It connects Caching and CDNs to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Caching and CDNs without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-58",
      "level": "advanced",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully. Focusing on Progressive enhancement, how would you investigate without guessing? Your answer should diagnose partial failure, contention, or saturation without guessing.",
      "skillsTested": [
        "Progressive enhancement",
        "Evidence-based diagnosis",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the Progressive enhancement failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Working baseline first; enhancement is a bonus, not a requirement.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n- **Operation:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully. This names a concrete boundary and observable outcome rather than calling Progressive enhancement “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** The page still functions when a script fails, on slow networks, or for assistive technology. My final rule is **Working baseline first; enhancement is a bonus, not a requirement.**",
        "modelAnswer": "**Expected behavior:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** For Progressive enhancement at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.",
        "explanation": "**Why this answer is correct:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n\n**In plain language:** Working baseline first; enhancement is a bonus, not a requirement.\n\n**How it applies in production:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Diagnose partial failure, contention, or saturation without guessing.",
        "keyPoints": [
          "**Remember:** Working baseline first; enhancement is a bonus, not a requirement.",
          "Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences.",
          "Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled."
        ],
        "realWorldExample": "🏭 **In production:** A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully.\n\n✅ **Why this is a strong answer:** It connects Progressive enhancement to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Progressive enhancement without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-05",
      "level": "advanced",
      "kind": "design",
      "prompt": "For a commerce page used across mobile devices and assistive technology, how would you design the part of the system concerned with Semantic HTML using Web Fundamentals? The target outcome is: A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code. Your answer should design for failure isolation, recovery, evolution, and an explicit SLO.",
      "skillsTested": [
        "Semantic HTML",
        "System design",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would design Semantic HTML around a clear boundary, predictable failure behavior, and evidence that proves the result. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Use the element whose built-in contract matches the job.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n- **Operation:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n- **Proof:** I would verify it by walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO.\n\n**Design:** I would give Semantic HTML a clear boundary: defined inputs and outputs, one owner, explicit authorization, bounded resource use, observable failure, and a rehearsed recovery path. I would walk a successful request through the design, then repeat the walk for invalid input, overload, dependency failure, and rollback. That exposes hidden coupling before production traffic does.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code. This names a concrete boundary and observable outcome rather than calling Semantic HTML “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Choose elements for purpose, then style them. My final rule is **Use the element whose built-in contract matches the job.**",
        "modelAnswer": "**Boundary:** I would give Semantic HTML a clear owner, inputs, outputs, failure behavior, and recovery path. Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n\n**Production controls:** I would use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n\n**Concrete example:** A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code.\n\n**How to prove it:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**Trade-off and redesign point:** For Semantic HTML at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions.",
        "explanation": "**Why this answer is correct:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n\n**In plain language:** Use the element whose built-in contract matches the job.\n\n**How it applies in production:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n\n**Evidence to ask for:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**What a complete answer includes:** Design for failure isolation, recovery, evolution, and an explicit SLO.",
        "keyPoints": [
          "**Remember:** Use the element whose built-in contract matches the job.",
          "Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs.",
          "Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions."
        ],
        "realWorldExample": "🏭 **In production:** A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code.\n\n✅ **Why this is a strong answer:** It connects Semantic HTML to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Semantic HTML without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Semantic HTML: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs."
            },
            {
              "label": "Protect",
              "detail": "Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-advanced-11",
      "level": "advanced",
      "kind": "design",
      "prompt": "For a commerce page used across mobile devices and assistive technology, how would you design the part of the system concerned with CSS layout using Web Fundamentals? The target outcome is: A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks. Your answer should design for failure isolation, recovery, evolution, and an explicit SLO.",
      "skillsTested": [
        "CSS layout",
        "System design",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would design CSS layout around a clear boundary, predictable failure behavior, and evidence that proves the result. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Let content size first; constrain only where necessary.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n- **Operation:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n- **Proof:** I would verify it by walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO.\n\n**Design:** I would give CSS layout a clear boundary: defined inputs and outputs, one owner, explicit authorization, bounded resource use, observable failure, and a rehearsed recovery path. I would walk a successful request through the design, then repeat the walk for invalid input, overload, dependency failure, and rollback. That exposes hidden coupling before production traffic does.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks. This names a concrete boundary and observable outcome rather than calling CSS layout “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Responsive design should follow content breakpoints rather than device names. My final rule is **Let content size first; constrain only where necessary.**",
        "modelAnswer": "**Boundary:** I would give CSS layout a clear owner, inputs, outputs, failure behavior, and recovery path. CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n\n**Production controls:** I would prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n\n**Concrete example:** A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks.\n\n**How to prove it:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**Trade-off and redesign point:** For CSS layout at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.",
        "explanation": "**Why this answer is correct:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n\n**In plain language:** Let content size first; constrain only where necessary.\n\n**How it applies in production:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n\n**Evidence to ask for:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**What a complete answer includes:** Design for failure isolation, recovery, evolution, and an explicit SLO.",
        "keyPoints": [
          "**Remember:** Let content size first; constrain only where necessary.",
          "CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space.",
          "Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions."
        ],
        "realWorldExample": "🏭 **In production:** A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks.\n\n✅ **Why this is a strong answer:** It connects CSS layout to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming CSS layout without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "CSS layout: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space."
            },
            {
              "label": "Protect",
              "detail": "Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-advanced-17",
      "level": "advanced",
      "kind": "design",
      "prompt": "For a commerce page used across mobile devices and assistive technology, how would you design the part of the system concerned with Accessibility using Web Fundamentals? The target outcome is: Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard. Your answer should design for failure isolation, recovery, evolution, and an explicit SLO.",
      "skillsTested": [
        "Accessibility",
        "System design",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would design Accessibility around a clear boundary, predictable failure behavior, and evidence that proves the result. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Name it, focus it, announce it, and keep it operable.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n- **Operation:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n- **Proof:** I would verify it by walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO.\n\n**Design:** I would give Accessibility a clear boundary: defined inputs and outputs, one owner, explicit authorization, bounded resource use, observable failure, and a rehearsed recovery path. I would walk a successful request through the design, then repeat the walk for invalid input, overload, dependency failure, and rollback. That exposes hidden coupling before production traffic does.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard. This names a concrete boundary and observable outcome rather than calling Accessibility “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** WCAG is a testable baseline, not a substitute for user testing. My final rule is **Name it, focus it, announce it, and keep it operable.**",
        "modelAnswer": "**Boundary:** I would give Accessibility a clear owner, inputs, outputs, failure behavior, and recovery path. Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n\n**Production controls:** I would preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n\n**Concrete example:** Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard.\n\n**How to prove it:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**Trade-off and redesign point:** For Accessibility at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size.",
        "explanation": "**Why this answer is correct:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n\n**In plain language:** Name it, focus it, announce it, and keep it operable.\n\n**How it applies in production:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n\n**Evidence to ask for:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**What a complete answer includes:** Design for failure isolation, recovery, evolution, and an explicit SLO.",
        "keyPoints": [
          "**Remember:** Name it, focus it, announce it, and keep it operable.",
          "Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods.",
          "Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size."
        ],
        "realWorldExample": "🏭 **In production:** Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard.\n\n✅ **Why this is a strong answer:** It connects Accessibility to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Accessibility without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Accessibility: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods."
            },
            {
              "label": "Protect",
              "detail": "Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-advanced-23",
      "level": "advanced",
      "kind": "design",
      "prompt": "For a commerce page used across mobile devices and assistive technology, how would you design the part of the system concerned with HTTP and browsers using Web Fundamentals? The target outcome is: A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck. Your answer should design for failure isolation, recovery, evolution, and an explicit SLO.",
      "skillsTested": [
        "HTTP and browsers",
        "System design",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would design HTTP and browsers around a clear boundary, predictable failure behavior, and evidence that proves the result. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Follow the request from name lookup to pixels.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n- **Operation:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n- **Proof:** I would verify it by walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO.\n\n**Design:** I would give HTTP and browsers a clear boundary: defined inputs and outputs, one owner, explicit authorization, bounded resource use, observable failure, and a rehearsed recovery path. I would walk a successful request through the design, then repeat the walk for invalid input, overload, dependency failure, and rollback. That exposes hidden coupling before production traffic does.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck. This names a concrete boundary and observable outcome rather than calling HTTP and browsers “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Caching and connection reuse can matter more than JavaScript micro-optimizations. My final rule is **Follow the request from name lookup to pixels.**",
        "modelAnswer": "**Boundary:** I would give HTTP and browsers a clear owner, inputs, outputs, failure behavior, and recovery path. A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n\n**Production controls:** I would use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n\n**Concrete example:** A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck.\n\n**How to prove it:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**Trade-off and redesign point:** For HTTP and browsers at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays.",
        "explanation": "**Why this answer is correct:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n\n**In plain language:** Follow the request from name lookup to pixels.\n\n**How it applies in production:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n\n**Evidence to ask for:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**What a complete answer includes:** Design for failure isolation, recovery, evolution, and an explicit SLO.",
        "keyPoints": [
          "**Remember:** Follow the request from name lookup to pixels.",
          "A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites.",
          "Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays."
        ],
        "realWorldExample": "🏭 **In production:** A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck.\n\n✅ **Why this is a strong answer:** It connects HTTP and browsers to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming HTTP and browsers without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "HTTP and browsers: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites."
            },
            {
              "label": "Protect",
              "detail": "Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-advanced-29",
      "level": "advanced",
      "kind": "design",
      "prompt": "For a commerce page used across mobile devices and assistive technology, how would you design the part of the system concerned with Core Web Vitals using Web Fundamentals? The target outcome is: A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP. Your answer should design for failure isolation, recovery, evolution, and an explicit SLO.",
      "skillsTested": [
        "Core Web Vitals",
        "System design",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would design Core Web Vitals around a clear boundary, predictable failure behavior, and evidence that proves the result. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Load the main thing, respond quickly, move nothing unexpectedly.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n- **Operation:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n- **Proof:** I would verify it by walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO.\n\n**Design:** I would give Core Web Vitals a clear boundary: defined inputs and outputs, one owner, explicit authorization, bounded resource use, observable failure, and a rehearsed recovery path. I would walk a successful request through the design, then repeat the walk for invalid input, overload, dependency failure, and rollback. That exposes hidden coupling before production traffic does.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP. This names a concrete boundary and observable outcome rather than calling Core Web Vitals “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Field data matters because lab hardware and networks are only controlled samples. My final rule is **Load the main thing, respond quickly, move nothing unexpectedly.**",
        "modelAnswer": "**Boundary:** I would give Core Web Vitals a clear owner, inputs, outputs, failure behavior, and recovery path. Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n\n**Production controls:** I would attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n\n**Concrete example:** A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP.\n\n**How to prove it:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**Trade-off and redesign point:** For Core Web Vitals at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content.",
        "explanation": "**Why this answer is correct:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n\n**In plain language:** Load the main thing, respond quickly, move nothing unexpectedly.\n\n**How it applies in production:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n\n**Evidence to ask for:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**What a complete answer includes:** Design for failure isolation, recovery, evolution, and an explicit SLO.",
        "keyPoints": [
          "**Remember:** Load the main thing, respond quickly, move nothing unexpectedly.",
          "Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability.",
          "Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content."
        ],
        "realWorldExample": "🏭 **In production:** A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP.\n\n✅ **Why this is a strong answer:** It connects Core Web Vitals to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Core Web Vitals without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Core Web Vitals: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability."
            },
            {
              "label": "Protect",
              "detail": "Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-advanced-35",
      "level": "advanced",
      "kind": "design",
      "prompt": "For a commerce page used across mobile devices and assistive technology, how would you design the part of the system concerned with Forms and validation using Web Fundamentals? The target outcome is: A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order. Your answer should design for failure isolation, recovery, evolution, and an explicit SLO.",
      "skillsTested": [
        "Forms and validation",
        "System design",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would design Forms and validation around a clear boundary, predictable failure behavior, and evidence that proves the result. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Validate near the user for feedback, at the server for trust.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n- **Operation:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n- **Proof:** I would verify it by walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO.\n\n**Design:** I would give Forms and validation a clear boundary: defined inputs and outputs, one owner, explicit authorization, bounded resource use, observable failure, and a rehearsed recovery path. I would walk a successful request through the design, then repeat the walk for invalid input, overload, dependency failure, and rollback. That exposes hidden coupling before production traffic does.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order. This names a concrete boundary and observable outcome rather than calling Forms and validation “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted. My final rule is **Validate near the user for feedback, at the server for trust.**",
        "modelAnswer": "**Boundary:** I would give Forms and validation a clear owner, inputs, outputs, failure behavior, and recovery path. HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n\n**Production controls:** I would use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n\n**Concrete example:** A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order.\n\n**How to prove it:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**Trade-off and redesign point:** For Forms and validation at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.",
        "explanation": "**Why this answer is correct:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n\n**In plain language:** Validate near the user for feedback, at the server for trust.\n\n**How it applies in production:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n\n**Evidence to ask for:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**What a complete answer includes:** Design for failure isolation, recovery, evolution, and an explicit SLO.",
        "keyPoints": [
          "**Remember:** Validate near the user for feedback, at the server for trust.",
          "HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility.",
          "Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server."
        ],
        "realWorldExample": "🏭 **In production:** A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order.\n\n✅ **Why this is a strong answer:** It connects Forms and validation to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Forms and validation without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Forms and validation: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility."
            },
            {
              "label": "Protect",
              "detail": "Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-advanced-41",
      "level": "advanced",
      "kind": "design",
      "prompt": "For a commerce page used across mobile devices and assistive technology, how would you design the part of the system concerned with Browser JavaScript using Web Fundamentals? The target outcome is: A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke. Your answer should design for failure isolation, recovery, evolution, and an explicit SLO.",
      "skillsTested": [
        "Browser JavaScript",
        "System design",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would design Browser JavaScript around a clear boundary, predictable failure behavior, and evidence that proves the result. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **The main thread renders and handles input; do not block it.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n- **Operation:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n- **Proof:** I would verify it by walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO.\n\n**Design:** I would give Browser JavaScript a clear boundary: defined inputs and outputs, one owner, explicit authorization, bounded resource use, observable failure, and a rehearsed recovery path. I would walk a successful request through the design, then repeat the walk for invalid input, overload, dependency failure, and rollback. That exposes hidden coupling before production traffic does.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke. This names a concrete boundary and observable outcome rather than calling Browser JavaScript “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness. My final rule is **The main thread renders and handles input; do not block it.**",
        "modelAnswer": "**Boundary:** I would give Browser JavaScript a clear owner, inputs, outputs, failure behavior, and recovery path. Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n\n**Production controls:** I would delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n\n**Concrete example:** A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke.\n\n**How to prove it:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**Trade-off and redesign point:** For Browser JavaScript at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.",
        "explanation": "**Why this answer is correct:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n\n**In plain language:** The main thread renders and handles input; do not block it.\n\n**How it applies in production:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n\n**Evidence to ask for:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**What a complete answer includes:** Design for failure isolation, recovery, evolution, and an explicit SLO.",
        "keyPoints": [
          "**Remember:** The main thread renders and handles input; do not block it.",
          "Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread.",
          "Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks."
        ],
        "realWorldExample": "🏭 **In production:** A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke.\n\n✅ **Why this is a strong answer:** It connects Browser JavaScript to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Browser JavaScript without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Browser JavaScript: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread."
            },
            {
              "label": "Protect",
              "detail": "Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-advanced-47",
      "level": "advanced",
      "kind": "design",
      "prompt": "For a commerce page used across mobile devices and assistive technology, how would you design the part of the system concerned with Web security using Web Fundamentals? The target outcome is: A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts. Your answer should design for failure isolation, recovery, evolution, and an explicit SLO.",
      "skillsTested": [
        "Web security",
        "System design",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would design Web security around a clear boundary, predictable failure behavior, and evidence that proves the result. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Never trust the client; escape output and lock down the browser.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n- **Operation:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n- **Proof:** I would verify it by walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO.\n\n**Design:** I would give Web security a clear boundary: defined inputs and outputs, one owner, explicit authorization, bounded resource use, observable failure, and a rehearsed recovery path. I would walk a successful request through the design, then repeat the walk for invalid input, overload, dependency failure, and rollback. That exposes hidden coupling before production traffic does.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts. This names a concrete boundary and observable outcome rather than calling Web security “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls. My final rule is **Never trust the client; escape output and lock down the browser.**",
        "modelAnswer": "**Boundary:** I would give Web security a clear owner, inputs, outputs, failure behavior, and recovery path. Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n\n**Production controls:** I would escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n\n**Concrete example:** A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts.\n\n**How to prove it:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**Trade-off and redesign point:** For Web security at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.",
        "explanation": "**Why this answer is correct:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n\n**In plain language:** Never trust the client; escape output and lock down the browser.\n\n**How it applies in production:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n\n**Evidence to ask for:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**What a complete answer includes:** Design for failure isolation, recovery, evolution, and an explicit SLO.",
        "keyPoints": [
          "**Remember:** Never trust the client; escape output and lock down the browser.",
          "Web security depends on not trusting the client and controlling what the browser is allowed to do.",
          "Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins."
        ],
        "realWorldExample": "🏭 **In production:** A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts.\n\n✅ **Why this is a strong answer:** It connects Web security to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Web security without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Web security: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Web security depends on not trusting the client and controlling what the browser is allowed to do."
            },
            {
              "label": "Protect",
              "detail": "Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-advanced-53",
      "level": "advanced",
      "kind": "design",
      "prompt": "For a commerce page used across mobile devices and assistive technology, how would you design the part of the system concerned with Caching and CDNs using Web Fundamentals? The target outcome is: A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect. Your answer should design for failure isolation, recovery, evolution, and an explicit SLO.",
      "skillsTested": [
        "Caching and CDNs",
        "System design",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would design Caching and CDNs around a clear boundary, predictable failure behavior, and evidence that proves the result. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Cache immutable assets hard; revalidate the documents that change.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n- **Operation:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n- **Proof:** I would verify it by walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO.\n\n**Design:** I would give Caching and CDNs a clear boundary: defined inputs and outputs, one owner, explicit authorization, bounded resource use, observable failure, and a rehearsed recovery path. I would walk a successful request through the design, then repeat the walk for invalid input, overload, dependency failure, and rollback. That exposes hidden coupling before production traffic does.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect. This names a concrete boundary and observable outcome rather than calling Caching and CDNs “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely. My final rule is **Cache immutable assets hard; revalidate the documents that change.**",
        "modelAnswer": "**Boundary:** I would give Caching and CDNs a clear owner, inputs, outputs, failure behavior, and recovery path. Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n\n**Production controls:** I would fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n\n**Concrete example:** A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect.\n\n**How to prove it:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**Trade-off and redesign point:** For Caching and CDNs at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.",
        "explanation": "**Why this answer is correct:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n\n**In plain language:** Cache immutable assets hard; revalidate the documents that change.\n\n**How it applies in production:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n\n**Evidence to ask for:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**What a complete answer includes:** Design for failure isolation, recovery, evolution, and an explicit SLO.",
        "keyPoints": [
          "**Remember:** Cache immutable assets hard; revalidate the documents that change.",
          "Browser and CDN caching avoid re-fetching unchanged resources.",
          "Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change."
        ],
        "realWorldExample": "🏭 **In production:** A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect.\n\n✅ **Why this is a strong answer:** It connects Caching and CDNs to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Caching and CDNs without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Caching and CDNs: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Browser and CDN caching avoid re-fetching unchanged resources."
            },
            {
              "label": "Protect",
              "detail": "Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-advanced-59",
      "level": "advanced",
      "kind": "design",
      "prompt": "For a commerce page used across mobile devices and assistive technology, how would you design the part of the system concerned with Progressive enhancement using Web Fundamentals? The target outcome is: A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully. Your answer should design for failure isolation, recovery, evolution, and an explicit SLO.",
      "skillsTested": [
        "Progressive enhancement",
        "System design",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would design Progressive enhancement around a clear boundary, predictable failure behavior, and evidence that proves the result. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Working baseline first; enhancement is a bonus, not a requirement.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n- **Operation:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n- **Proof:** I would verify it by walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO.\n\n**Design:** I would give Progressive enhancement a clear boundary: defined inputs and outputs, one owner, explicit authorization, bounded resource use, observable failure, and a rehearsed recovery path. I would walk a successful request through the design, then repeat the walk for invalid input, overload, dependency failure, and rollback. That exposes hidden coupling before production traffic does.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully. This names a concrete boundary and observable outcome rather than calling Progressive enhancement “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** The page still functions when a script fails, on slow networks, or for assistive technology. My final rule is **Working baseline first; enhancement is a bonus, not a requirement.**",
        "modelAnswer": "**Boundary:** I would give Progressive enhancement a clear owner, inputs, outputs, failure behavior, and recovery path. Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n\n**Production controls:** I would make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n\n**Concrete example:** A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully.\n\n**How to prove it:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**Trade-off and redesign point:** For Progressive enhancement at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.",
        "explanation": "**Why this answer is correct:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n\n**In plain language:** Working baseline first; enhancement is a bonus, not a requirement.\n\n**How it applies in production:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n\n**Evidence to ask for:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**What a complete answer includes:** Design for failure isolation, recovery, evolution, and an explicit SLO.",
        "keyPoints": [
          "**Remember:** Working baseline first; enhancement is a bonus, not a requirement.",
          "Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences.",
          "Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled."
        ],
        "realWorldExample": "🏭 **In production:** A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully.\n\n✅ **Why this is a strong answer:** It connects Progressive enhancement to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Progressive enhancement without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Progressive enhancement: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences."
            },
            {
              "label": "Protect",
              "detail": "Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-advanced-06",
      "level": "advanced",
      "kind": "conceptual",
      "prompt": "What is Semantic HTML in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code. Your answer should teach the trade-off through a counterexample and define when the decision changes.",
      "skillsTested": [
        "Semantic HTML",
        "Concept explanation",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** Semantic HTML is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Use the element whose built-in contract matches the job.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n- **Operation:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n- **Proof:** I would verify it by showing one expected Semantic HTML behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee Semantic HTML provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code. This names a concrete boundary and observable outcome rather than calling Semantic HTML “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Counterexample:** Naming Semantic HTML without its guarantee, failure boundary, or proof is not understanding it. I would prefer a simpler alternative whenever it meets the same measured requirement.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Choose elements for purpose, then style them. My final rule is **Use the element whose built-in contract matches the job.**",
        "modelAnswer": "**Direct answer:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n\n**Counterexample:** A team does not understand Semantic HTML if it cannot explain or verify those guarantees.\n\n**Production use:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n\n**Concrete example:** A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code.\n\n**Decision limit:** For Semantic HTML at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions.",
        "explanation": "**Why this answer is correct:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n\n**In plain language:** Use the element whose built-in contract matches the job.\n\n**How it applies in production:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n\n**Evidence to ask for:** Showing one expected Semantic HTML behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Teach the trade-off through a counterexample and define when the decision changes.",
        "keyPoints": [
          "**Remember:** Use the element whose built-in contract matches the job.",
          "Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs.",
          "Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions."
        ],
        "realWorldExample": "🏭 **In production:** A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code.\n\n✅ **Why this is a strong answer:** It connects Semantic HTML to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Semantic HTML without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-12",
      "level": "advanced",
      "kind": "conceptual",
      "prompt": "What is CSS layout in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks. Your answer should teach the trade-off through a counterexample and define when the decision changes.",
      "skillsTested": [
        "CSS layout",
        "Concept explanation",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** CSS layout is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Let content size first; constrain only where necessary.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n- **Operation:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n- **Proof:** I would verify it by showing one expected CSS layout behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee CSS layout provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks. This names a concrete boundary and observable outcome rather than calling CSS layout “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Counterexample:** Naming CSS layout without its guarantee, failure boundary, or proof is not understanding it. I would prefer a simpler alternative whenever it meets the same measured requirement.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Responsive design should follow content breakpoints rather than device names. My final rule is **Let content size first; constrain only where necessary.**",
        "modelAnswer": "**Direct answer:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n\n**Counterexample:** A team does not understand CSS layout if it cannot explain or verify those guarantees.\n\n**Production use:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n\n**Concrete example:** A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks.\n\n**Decision limit:** For CSS layout at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.",
        "explanation": "**Why this answer is correct:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n\n**In plain language:** Let content size first; constrain only where necessary.\n\n**How it applies in production:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n\n**Evidence to ask for:** Showing one expected CSS layout behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Teach the trade-off through a counterexample and define when the decision changes.",
        "keyPoints": [
          "**Remember:** Let content size first; constrain only where necessary.",
          "CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space.",
          "Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions."
        ],
        "realWorldExample": "🏭 **In production:** A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks.\n\n✅ **Why this is a strong answer:** It connects CSS layout to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming CSS layout without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-18",
      "level": "advanced",
      "kind": "conceptual",
      "prompt": "What is Accessibility in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard. Your answer should teach the trade-off through a counterexample and define when the decision changes.",
      "skillsTested": [
        "Accessibility",
        "Concept explanation",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** Accessibility is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Name it, focus it, announce it, and keep it operable.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n- **Operation:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n- **Proof:** I would verify it by showing one expected Accessibility behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee Accessibility provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard. This names a concrete boundary and observable outcome rather than calling Accessibility “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Counterexample:** Naming Accessibility without its guarantee, failure boundary, or proof is not understanding it. I would prefer a simpler alternative whenever it meets the same measured requirement.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** WCAG is a testable baseline, not a substitute for user testing. My final rule is **Name it, focus it, announce it, and keep it operable.**",
        "modelAnswer": "**Direct answer:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n\n**Counterexample:** A team does not understand Accessibility if it cannot explain or verify those guarantees.\n\n**Production use:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n\n**Concrete example:** Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard.\n\n**Decision limit:** For Accessibility at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size.",
        "explanation": "**Why this answer is correct:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n\n**In plain language:** Name it, focus it, announce it, and keep it operable.\n\n**How it applies in production:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n\n**Evidence to ask for:** Showing one expected Accessibility behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Teach the trade-off through a counterexample and define when the decision changes.",
        "keyPoints": [
          "**Remember:** Name it, focus it, announce it, and keep it operable.",
          "Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods.",
          "Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size."
        ],
        "realWorldExample": "🏭 **In production:** Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard.\n\n✅ **Why this is a strong answer:** It connects Accessibility to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Accessibility without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-24",
      "level": "advanced",
      "kind": "conceptual",
      "prompt": "What is HTTP and browsers in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck. Your answer should teach the trade-off through a counterexample and define when the decision changes.",
      "skillsTested": [
        "HTTP and browsers",
        "Concept explanation",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** HTTP and browsers is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Follow the request from name lookup to pixels.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n- **Operation:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n- **Proof:** I would verify it by showing one expected HTTP and browsers behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee HTTP and browsers provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck. This names a concrete boundary and observable outcome rather than calling HTTP and browsers “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Counterexample:** Naming HTTP and browsers without its guarantee, failure boundary, or proof is not understanding it. I would prefer a simpler alternative whenever it meets the same measured requirement.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Caching and connection reuse can matter more than JavaScript micro-optimizations. My final rule is **Follow the request from name lookup to pixels.**",
        "modelAnswer": "**Direct answer:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n\n**Counterexample:** A team does not understand HTTP and browsers if it cannot explain or verify those guarantees.\n\n**Production use:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n\n**Concrete example:** A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck.\n\n**Decision limit:** For HTTP and browsers at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays.",
        "explanation": "**Why this answer is correct:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n\n**In plain language:** Follow the request from name lookup to pixels.\n\n**How it applies in production:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n\n**Evidence to ask for:** Showing one expected HTTP and browsers behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Teach the trade-off through a counterexample and define when the decision changes.",
        "keyPoints": [
          "**Remember:** Follow the request from name lookup to pixels.",
          "A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites.",
          "Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays."
        ],
        "realWorldExample": "🏭 **In production:** A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck.\n\n✅ **Why this is a strong answer:** It connects HTTP and browsers to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming HTTP and browsers without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-30",
      "level": "advanced",
      "kind": "conceptual",
      "prompt": "What is Core Web Vitals in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP. Your answer should teach the trade-off through a counterexample and define when the decision changes.",
      "skillsTested": [
        "Core Web Vitals",
        "Concept explanation",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** Core Web Vitals is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Load the main thing, respond quickly, move nothing unexpectedly.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n- **Operation:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n- **Proof:** I would verify it by showing one expected Core Web Vitals behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee Core Web Vitals provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP. This names a concrete boundary and observable outcome rather than calling Core Web Vitals “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Counterexample:** Naming Core Web Vitals without its guarantee, failure boundary, or proof is not understanding it. I would prefer a simpler alternative whenever it meets the same measured requirement.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Field data matters because lab hardware and networks are only controlled samples. My final rule is **Load the main thing, respond quickly, move nothing unexpectedly.**",
        "modelAnswer": "**Direct answer:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n\n**Counterexample:** A team does not understand Core Web Vitals if it cannot explain or verify those guarantees.\n\n**Production use:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n\n**Concrete example:** A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP.\n\n**Decision limit:** For Core Web Vitals at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content.",
        "explanation": "**Why this answer is correct:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n\n**In plain language:** Load the main thing, respond quickly, move nothing unexpectedly.\n\n**How it applies in production:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n\n**Evidence to ask for:** Showing one expected Core Web Vitals behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Teach the trade-off through a counterexample and define when the decision changes.",
        "keyPoints": [
          "**Remember:** Load the main thing, respond quickly, move nothing unexpectedly.",
          "Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability.",
          "Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content."
        ],
        "realWorldExample": "🏭 **In production:** A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP.\n\n✅ **Why this is a strong answer:** It connects Core Web Vitals to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Core Web Vitals without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-36",
      "level": "advanced",
      "kind": "conceptual",
      "prompt": "What is Forms and validation in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order. Your answer should teach the trade-off through a counterexample and define when the decision changes.",
      "skillsTested": [
        "Forms and validation",
        "Concept explanation",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** Forms and validation is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Validate near the user for feedback, at the server for trust.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n- **Operation:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n- **Proof:** I would verify it by showing one expected Forms and validation behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee Forms and validation provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order. This names a concrete boundary and observable outcome rather than calling Forms and validation “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Counterexample:** Naming Forms and validation without its guarantee, failure boundary, or proof is not understanding it. I would prefer a simpler alternative whenever it meets the same measured requirement.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted. My final rule is **Validate near the user for feedback, at the server for trust.**",
        "modelAnswer": "**Direct answer:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n\n**Counterexample:** A team does not understand Forms and validation if it cannot explain or verify those guarantees.\n\n**Production use:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n\n**Concrete example:** A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order.\n\n**Decision limit:** For Forms and validation at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.",
        "explanation": "**Why this answer is correct:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n\n**In plain language:** Validate near the user for feedback, at the server for trust.\n\n**How it applies in production:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n\n**Evidence to ask for:** Showing one expected Forms and validation behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Teach the trade-off through a counterexample and define when the decision changes.",
        "keyPoints": [
          "**Remember:** Validate near the user for feedback, at the server for trust.",
          "HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility.",
          "Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server."
        ],
        "realWorldExample": "🏭 **In production:** A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order.\n\n✅ **Why this is a strong answer:** It connects Forms and validation to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Forms and validation without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-42",
      "level": "advanced",
      "kind": "conceptual",
      "prompt": "What is Browser JavaScript in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke. Your answer should teach the trade-off through a counterexample and define when the decision changes.",
      "skillsTested": [
        "Browser JavaScript",
        "Concept explanation",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** Browser JavaScript is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **The main thread renders and handles input; do not block it.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n- **Operation:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n- **Proof:** I would verify it by showing one expected Browser JavaScript behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee Browser JavaScript provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke. This names a concrete boundary and observable outcome rather than calling Browser JavaScript “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Counterexample:** Naming Browser JavaScript without its guarantee, failure boundary, or proof is not understanding it. I would prefer a simpler alternative whenever it meets the same measured requirement.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness. My final rule is **The main thread renders and handles input; do not block it.**",
        "modelAnswer": "**Direct answer:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n\n**Counterexample:** A team does not understand Browser JavaScript if it cannot explain or verify those guarantees.\n\n**Production use:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n\n**Concrete example:** A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke.\n\n**Decision limit:** For Browser JavaScript at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.",
        "explanation": "**Why this answer is correct:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n\n**In plain language:** The main thread renders and handles input; do not block it.\n\n**How it applies in production:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n\n**Evidence to ask for:** Showing one expected Browser JavaScript behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Teach the trade-off through a counterexample and define when the decision changes.",
        "keyPoints": [
          "**Remember:** The main thread renders and handles input; do not block it.",
          "Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread.",
          "Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks."
        ],
        "realWorldExample": "🏭 **In production:** A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke.\n\n✅ **Why this is a strong answer:** It connects Browser JavaScript to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Browser JavaScript without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-48",
      "level": "advanced",
      "kind": "conceptual",
      "prompt": "What is Web security in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts. Your answer should teach the trade-off through a counterexample and define when the decision changes.",
      "skillsTested": [
        "Web security",
        "Concept explanation",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** Web security is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Never trust the client; escape output and lock down the browser.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n- **Operation:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n- **Proof:** I would verify it by showing one expected Web security behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee Web security provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts. This names a concrete boundary and observable outcome rather than calling Web security “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Counterexample:** Naming Web security without its guarantee, failure boundary, or proof is not understanding it. I would prefer a simpler alternative whenever it meets the same measured requirement.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls. My final rule is **Never trust the client; escape output and lock down the browser.**",
        "modelAnswer": "**Direct answer:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n\n**Counterexample:** A team does not understand Web security if it cannot explain or verify those guarantees.\n\n**Production use:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n\n**Concrete example:** A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts.\n\n**Decision limit:** For Web security at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.",
        "explanation": "**Why this answer is correct:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n\n**In plain language:** Never trust the client; escape output and lock down the browser.\n\n**How it applies in production:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n\n**Evidence to ask for:** Showing one expected Web security behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Teach the trade-off through a counterexample and define when the decision changes.",
        "keyPoints": [
          "**Remember:** Never trust the client; escape output and lock down the browser.",
          "Web security depends on not trusting the client and controlling what the browser is allowed to do.",
          "Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins."
        ],
        "realWorldExample": "🏭 **In production:** A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts.\n\n✅ **Why this is a strong answer:** It connects Web security to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Web security without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-54",
      "level": "advanced",
      "kind": "conceptual",
      "prompt": "What is Caching and CDNs in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect. Your answer should teach the trade-off through a counterexample and define when the decision changes.",
      "skillsTested": [
        "Caching and CDNs",
        "Concept explanation",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** Caching and CDNs is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Cache immutable assets hard; revalidate the documents that change.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n- **Operation:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n- **Proof:** I would verify it by showing one expected Caching and CDNs behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee Caching and CDNs provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect. This names a concrete boundary and observable outcome rather than calling Caching and CDNs “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Counterexample:** Naming Caching and CDNs without its guarantee, failure boundary, or proof is not understanding it. I would prefer a simpler alternative whenever it meets the same measured requirement.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely. My final rule is **Cache immutable assets hard; revalidate the documents that change.**",
        "modelAnswer": "**Direct answer:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n\n**Counterexample:** A team does not understand Caching and CDNs if it cannot explain or verify those guarantees.\n\n**Production use:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n\n**Concrete example:** A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect.\n\n**Decision limit:** For Caching and CDNs at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.",
        "explanation": "**Why this answer is correct:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n\n**In plain language:** Cache immutable assets hard; revalidate the documents that change.\n\n**How it applies in production:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n\n**Evidence to ask for:** Showing one expected Caching and CDNs behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Teach the trade-off through a counterexample and define when the decision changes.",
        "keyPoints": [
          "**Remember:** Cache immutable assets hard; revalidate the documents that change.",
          "Browser and CDN caching avoid re-fetching unchanged resources.",
          "Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change."
        ],
        "realWorldExample": "🏭 **In production:** A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect.\n\n✅ **Why this is a strong answer:** It connects Caching and CDNs to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Caching and CDNs without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-advanced-60",
      "level": "advanced",
      "kind": "conceptual",
      "prompt": "What is Progressive enhancement in Web Fundamentals, how does it work, and why does it matter? Use this production case in your explanation: A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully. Your answer should teach the trade-off through a counterexample and define when the decision changes.",
      "skillsTested": [
        "Progressive enhancement",
        "Concept explanation",
        "Advanced reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** Progressive enhancement is best understood through the guarantee it provides and the boundary where that guarantee stops. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Working baseline first; enhancement is a bonus, not a requirement.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n- **Operation:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n- **Proof:** I would verify it by showing one expected Progressive enhancement behavior and one boundary case, then connecting both to observable evidence.\n\n**How it works:** I separate the label from the mechanism. First I state the guarantee Progressive enhancement provides. Then I trace the inputs, decisions, and resulting behavior, including the boundary where that guarantee stops. Finally I connect it to an observable production signal. That makes the explanation testable instead of a list of product names.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully. This names a concrete boundary and observable outcome rather than calling Progressive enhancement “best practice.”\n\n**Depth and trade-off:** For a senior-level decision, I would quantify latency, throughput, correctness, availability, security, recovery, and cost. I would identify what changes first at scale and the measured threshold for reconsidering the design.\n\n**Counterexample:** Naming Progressive enhancement without its guarantee, failure boundary, or proof is not understanding it. I would prefer a simpler alternative whenever it meets the same measured requirement.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** The page still functions when a script fails, on slow networks, or for assistive technology. My final rule is **Working baseline first; enhancement is a bonus, not a requirement.**",
        "modelAnswer": "**Direct answer:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n\n**Counterexample:** A team does not understand Progressive enhancement if it cannot explain or verify those guarantees.\n\n**Production use:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n\n**Concrete example:** A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully.\n\n**Decision limit:** For Progressive enhancement at scale I would quantify latency, correctness, security, and cost limits, compare the simplest credible alternative, and name the measured threshold that changes this design. My check: make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.",
        "explanation": "**Why this answer is correct:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n\n**In plain language:** Working baseline first; enhancement is a bonus, not a requirement.\n\n**How it applies in production:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n\n**Evidence to ask for:** Showing one expected Progressive enhancement behavior and one boundary case, then connecting both to observable evidence\n\n**What a complete answer includes:** Teach the trade-off through a counterexample and define when the decision changes.",
        "keyPoints": [
          "**Remember:** Working baseline first; enhancement is a bonus, not a requirement.",
          "Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences.",
          "Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled."
        ],
        "realWorldExample": "🏭 **In production:** A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully.\n\n✅ **Why this is a strong answer:** It connects Progressive enhancement to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Progressive enhancement without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-scenario-01",
      "level": "scenario",
      "kind": "scenario",
      "prompt": "Review this Web Fundamentals proposal for a commerce page used across mobile devices and assistive technology: A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code. Focusing on Semantic HTML, would you approve it, change it, or reject it? Your answer should make and defend a real production decision with incomplete information.",
      "skillsTested": [
        "Semantic HTML",
        "Scenario trade-offs",
        "Scenario reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would make the Semantic HTML decision conditionally, after its assumptions, failure behavior, and rollback are explicit. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Use the element whose built-in contract matches the job.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n- **Operation:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n- **Proof:** I would verify it by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Decision:** I would approve the direction only after making its assumptions explicit. I would confirm the workload shape, required guarantee, trust boundary, failure tolerance, recovery target, and cost ceiling. Then I would compare the proposal with the simplest credible alternative. The winning option is the one that meets those requirements with fewer failure modes, not the option with the longest feature list.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code. This names a concrete boundary and observable outcome rather than calling Semantic HTML “best practice.”\n\n**Depth and trade-off:** Because details are incomplete, I would state assumptions, choose a reversible starting point, and define evidence that invalidates it. If scale, failure, or security evidence changes, I would revise the design and rerun the checks.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Choose elements for purpose, then style them. My final rule is **Use the element whose built-in contract matches the job.**",
        "modelAnswer": "**Recommendation:** I would approve this direction, subject to the assumptions below. Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n\n**Production approach:** I would use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n\n**Concrete example:** A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code.\n\n**Alternative and proof:** I would reject a broader or more expensive option unless a measured requirement justifies it. I would prove this choice by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Assumptions and rollback:** My assumptions: the workload fits current Semantic HTML limits, the required guarantees match the mechanism, and rollback is available. If a measurement disproves one, I take the simplest credible alternative and retest — verifying by use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions.",
        "explanation": "**Why this answer is correct:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n\n**In plain language:** Use the element whose built-in contract matches the job.\n\n**How it applies in production:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n\n**Evidence to ask for:** Checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement\n\n**What a complete answer includes:** Make and defend a real production decision with incomplete information.",
        "keyPoints": [
          "**Remember:** Use the element whose built-in contract matches the job.",
          "Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs.",
          "Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions."
        ],
        "realWorldExample": "🏭 **In production:** A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code.\n\n✅ **Why this is a strong answer:** It connects Semantic HTML to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Semantic HTML without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Semantic HTML: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs."
            },
            {
              "label": "Protect",
              "detail": "Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-scenario-04",
      "level": "scenario",
      "kind": "scenario",
      "prompt": "Review this Web Fundamentals proposal for a commerce page used across mobile devices and assistive technology: A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks. Focusing on CSS layout, would you approve it, change it, or reject it? Your answer should make and defend a real production decision with incomplete information.",
      "skillsTested": [
        "CSS layout",
        "Scenario trade-offs",
        "Scenario reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would make the CSS layout decision conditionally, after its assumptions, failure behavior, and rollback are explicit. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Let content size first; constrain only where necessary.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n- **Operation:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n- **Proof:** I would verify it by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Decision:** I would approve the direction only after making its assumptions explicit. I would confirm the workload shape, required guarantee, trust boundary, failure tolerance, recovery target, and cost ceiling. Then I would compare the proposal with the simplest credible alternative. The winning option is the one that meets those requirements with fewer failure modes, not the option with the longest feature list.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks. This names a concrete boundary and observable outcome rather than calling CSS layout “best practice.”\n\n**Depth and trade-off:** Because details are incomplete, I would state assumptions, choose a reversible starting point, and define evidence that invalidates it. If scale, failure, or security evidence changes, I would revise the design and rerun the checks.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Responsive design should follow content breakpoints rather than device names. My final rule is **Let content size first; constrain only where necessary.**",
        "modelAnswer": "**Recommendation:** I would approve this direction, subject to the assumptions below. CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n\n**Production approach:** I would prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n\n**Concrete example:** A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks.\n\n**Alternative and proof:** I would reject a broader or more expensive option unless a measured requirement justifies it. I would prove this choice by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Assumptions and rollback:** My assumptions: the workload fits current CSS layout limits, the required guarantees match the mechanism, and rollback is available. If a measurement disproves one, I take the simplest credible alternative and retest — verifying by prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.",
        "explanation": "**Why this answer is correct:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n\n**In plain language:** Let content size first; constrain only where necessary.\n\n**How it applies in production:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n\n**Evidence to ask for:** Checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement\n\n**What a complete answer includes:** Make and defend a real production decision with incomplete information.",
        "keyPoints": [
          "**Remember:** Let content size first; constrain only where necessary.",
          "CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space.",
          "Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions."
        ],
        "realWorldExample": "🏭 **In production:** A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks.\n\n✅ **Why this is a strong answer:** It connects CSS layout to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming CSS layout without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "CSS layout: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space."
            },
            {
              "label": "Protect",
              "detail": "Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-scenario-07",
      "level": "scenario",
      "kind": "scenario",
      "prompt": "Review this Web Fundamentals proposal for a commerce page used across mobile devices and assistive technology: Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard. Focusing on Accessibility, would you approve it, change it, or reject it? Your answer should make and defend a real production decision with incomplete information.",
      "skillsTested": [
        "Accessibility",
        "Scenario trade-offs",
        "Scenario reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would make the Accessibility decision conditionally, after its assumptions, failure behavior, and rollback are explicit. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Name it, focus it, announce it, and keep it operable.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n- **Operation:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n- **Proof:** I would verify it by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Decision:** I would approve the direction only after making its assumptions explicit. I would confirm the workload shape, required guarantee, trust boundary, failure tolerance, recovery target, and cost ceiling. Then I would compare the proposal with the simplest credible alternative. The winning option is the one that meets those requirements with fewer failure modes, not the option with the longest feature list.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard. This names a concrete boundary and observable outcome rather than calling Accessibility “best practice.”\n\n**Depth and trade-off:** Because details are incomplete, I would state assumptions, choose a reversible starting point, and define evidence that invalidates it. If scale, failure, or security evidence changes, I would revise the design and rerun the checks.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** WCAG is a testable baseline, not a substitute for user testing. My final rule is **Name it, focus it, announce it, and keep it operable.**",
        "modelAnswer": "**Recommendation:** I would approve this direction, subject to the assumptions below. Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n\n**Production approach:** I would preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n\n**Concrete example:** Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard.\n\n**Alternative and proof:** I would reject a broader or more expensive option unless a measured requirement justifies it. I would prove this choice by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Assumptions and rollback:** My assumptions: the workload fits current Accessibility limits, the required guarantees match the mechanism, and rollback is available. If a measurement disproves one, I take the simplest credible alternative and retest — verifying by preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size.",
        "explanation": "**Why this answer is correct:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n\n**In plain language:** Name it, focus it, announce it, and keep it operable.\n\n**How it applies in production:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n\n**Evidence to ask for:** Checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement\n\n**What a complete answer includes:** Make and defend a real production decision with incomplete information.",
        "keyPoints": [
          "**Remember:** Name it, focus it, announce it, and keep it operable.",
          "Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods.",
          "Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size."
        ],
        "realWorldExample": "🏭 **In production:** Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard.\n\n✅ **Why this is a strong answer:** It connects Accessibility to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Accessibility without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Accessibility: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods."
            },
            {
              "label": "Protect",
              "detail": "Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-scenario-10",
      "level": "scenario",
      "kind": "scenario",
      "prompt": "Review this Web Fundamentals proposal for a commerce page used across mobile devices and assistive technology: A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck. Focusing on HTTP and browsers, would you approve it, change it, or reject it? Your answer should make and defend a real production decision with incomplete information.",
      "skillsTested": [
        "HTTP and browsers",
        "Scenario trade-offs",
        "Scenario reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would make the HTTP and browsers decision conditionally, after its assumptions, failure behavior, and rollback are explicit. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Follow the request from name lookup to pixels.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n- **Operation:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n- **Proof:** I would verify it by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Decision:** I would approve the direction only after making its assumptions explicit. I would confirm the workload shape, required guarantee, trust boundary, failure tolerance, recovery target, and cost ceiling. Then I would compare the proposal with the simplest credible alternative. The winning option is the one that meets those requirements with fewer failure modes, not the option with the longest feature list.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck. This names a concrete boundary and observable outcome rather than calling HTTP and browsers “best practice.”\n\n**Depth and trade-off:** Because details are incomplete, I would state assumptions, choose a reversible starting point, and define evidence that invalidates it. If scale, failure, or security evidence changes, I would revise the design and rerun the checks.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Caching and connection reuse can matter more than JavaScript micro-optimizations. My final rule is **Follow the request from name lookup to pixels.**",
        "modelAnswer": "**Recommendation:** I would approve this direction, subject to the assumptions below. A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n\n**Production approach:** I would use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n\n**Concrete example:** A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck.\n\n**Alternative and proof:** I would reject a broader or more expensive option unless a measured requirement justifies it. I would prove this choice by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Assumptions and rollback:** My assumptions: the workload fits current HTTP and browsers limits, the required guarantees match the mechanism, and rollback is available. If a measurement disproves one, I take the simplest credible alternative and retest — verifying by use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays.",
        "explanation": "**Why this answer is correct:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n\n**In plain language:** Follow the request from name lookup to pixels.\n\n**How it applies in production:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n\n**Evidence to ask for:** Checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement\n\n**What a complete answer includes:** Make and defend a real production decision with incomplete information.",
        "keyPoints": [
          "**Remember:** Follow the request from name lookup to pixels.",
          "A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites.",
          "Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays."
        ],
        "realWorldExample": "🏭 **In production:** A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck.\n\n✅ **Why this is a strong answer:** It connects HTTP and browsers to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming HTTP and browsers without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "HTTP and browsers: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites."
            },
            {
              "label": "Protect",
              "detail": "Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-scenario-13",
      "level": "scenario",
      "kind": "scenario",
      "prompt": "Review this Web Fundamentals proposal for a commerce page used across mobile devices and assistive technology: A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP. Focusing on Core Web Vitals, would you approve it, change it, or reject it? Your answer should make and defend a real production decision with incomplete information.",
      "skillsTested": [
        "Core Web Vitals",
        "Scenario trade-offs",
        "Scenario reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would make the Core Web Vitals decision conditionally, after its assumptions, failure behavior, and rollback are explicit. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Load the main thing, respond quickly, move nothing unexpectedly.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n- **Operation:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n- **Proof:** I would verify it by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Decision:** I would approve the direction only after making its assumptions explicit. I would confirm the workload shape, required guarantee, trust boundary, failure tolerance, recovery target, and cost ceiling. Then I would compare the proposal with the simplest credible alternative. The winning option is the one that meets those requirements with fewer failure modes, not the option with the longest feature list.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP. This names a concrete boundary and observable outcome rather than calling Core Web Vitals “best practice.”\n\n**Depth and trade-off:** Because details are incomplete, I would state assumptions, choose a reversible starting point, and define evidence that invalidates it. If scale, failure, or security evidence changes, I would revise the design and rerun the checks.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Field data matters because lab hardware and networks are only controlled samples. My final rule is **Load the main thing, respond quickly, move nothing unexpectedly.**",
        "modelAnswer": "**Recommendation:** I would approve this direction, subject to the assumptions below. Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n\n**Production approach:** I would attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n\n**Concrete example:** A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP.\n\n**Alternative and proof:** I would reject a broader or more expensive option unless a measured requirement justifies it. I would prove this choice by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Assumptions and rollback:** My assumptions: the workload fits current Core Web Vitals limits, the required guarantees match the mechanism, and rollback is available. If a measurement disproves one, I take the simplest credible alternative and retest — verifying by attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content.",
        "explanation": "**Why this answer is correct:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n\n**In plain language:** Load the main thing, respond quickly, move nothing unexpectedly.\n\n**How it applies in production:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n\n**Evidence to ask for:** Checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement\n\n**What a complete answer includes:** Make and defend a real production decision with incomplete information.",
        "keyPoints": [
          "**Remember:** Load the main thing, respond quickly, move nothing unexpectedly.",
          "Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability.",
          "Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content."
        ],
        "realWorldExample": "🏭 **In production:** A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP.\n\n✅ **Why this is a strong answer:** It connects Core Web Vitals to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Core Web Vitals without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Core Web Vitals: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability."
            },
            {
              "label": "Protect",
              "detail": "Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-scenario-16",
      "level": "scenario",
      "kind": "scenario",
      "prompt": "Review this Web Fundamentals proposal for a commerce page used across mobile devices and assistive technology: A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order. Focusing on Forms and validation, would you approve it, change it, or reject it? Your answer should make and defend a real production decision with incomplete information.",
      "skillsTested": [
        "Forms and validation",
        "Scenario trade-offs",
        "Scenario reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would make the Forms and validation decision conditionally, after its assumptions, failure behavior, and rollback are explicit. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Validate near the user for feedback, at the server for trust.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n- **Operation:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n- **Proof:** I would verify it by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Decision:** I would approve the direction only after making its assumptions explicit. I would confirm the workload shape, required guarantee, trust boundary, failure tolerance, recovery target, and cost ceiling. Then I would compare the proposal with the simplest credible alternative. The winning option is the one that meets those requirements with fewer failure modes, not the option with the longest feature list.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order. This names a concrete boundary and observable outcome rather than calling Forms and validation “best practice.”\n\n**Depth and trade-off:** Because details are incomplete, I would state assumptions, choose a reversible starting point, and define evidence that invalidates it. If scale, failure, or security evidence changes, I would revise the design and rerun the checks.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted. My final rule is **Validate near the user for feedback, at the server for trust.**",
        "modelAnswer": "**Recommendation:** I would approve this direction, subject to the assumptions below. HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n\n**Production approach:** I would use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n\n**Concrete example:** A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order.\n\n**Alternative and proof:** I would reject a broader or more expensive option unless a measured requirement justifies it. I would prove this choice by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Assumptions and rollback:** My assumptions: the workload fits current Forms and validation limits, the required guarantees match the mechanism, and rollback is available. If a measurement disproves one, I take the simplest credible alternative and retest — verifying by use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.",
        "explanation": "**Why this answer is correct:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n\n**In plain language:** Validate near the user for feedback, at the server for trust.\n\n**How it applies in production:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n\n**Evidence to ask for:** Checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement\n\n**What a complete answer includes:** Make and defend a real production decision with incomplete information.",
        "keyPoints": [
          "**Remember:** Validate near the user for feedback, at the server for trust.",
          "HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility.",
          "Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server."
        ],
        "realWorldExample": "🏭 **In production:** A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order.\n\n✅ **Why this is a strong answer:** It connects Forms and validation to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Forms and validation without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Forms and validation: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility."
            },
            {
              "label": "Protect",
              "detail": "Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-scenario-19",
      "level": "scenario",
      "kind": "scenario",
      "prompt": "Review this Web Fundamentals proposal for a commerce page used across mobile devices and assistive technology: A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke. Focusing on Browser JavaScript, would you approve it, change it, or reject it? Your answer should make and defend a real production decision with incomplete information.",
      "skillsTested": [
        "Browser JavaScript",
        "Scenario trade-offs",
        "Scenario reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would make the Browser JavaScript decision conditionally, after its assumptions, failure behavior, and rollback are explicit. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **The main thread renders and handles input; do not block it.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n- **Operation:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n- **Proof:** I would verify it by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Decision:** I would approve the direction only after making its assumptions explicit. I would confirm the workload shape, required guarantee, trust boundary, failure tolerance, recovery target, and cost ceiling. Then I would compare the proposal with the simplest credible alternative. The winning option is the one that meets those requirements with fewer failure modes, not the option with the longest feature list.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke. This names a concrete boundary and observable outcome rather than calling Browser JavaScript “best practice.”\n\n**Depth and trade-off:** Because details are incomplete, I would state assumptions, choose a reversible starting point, and define evidence that invalidates it. If scale, failure, or security evidence changes, I would revise the design and rerun the checks.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness. My final rule is **The main thread renders and handles input; do not block it.**",
        "modelAnswer": "**Recommendation:** I would approve this direction, subject to the assumptions below. Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n\n**Production approach:** I would delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n\n**Concrete example:** A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke.\n\n**Alternative and proof:** I would reject a broader or more expensive option unless a measured requirement justifies it. I would prove this choice by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Assumptions and rollback:** My assumptions: the workload fits current Browser JavaScript limits, the required guarantees match the mechanism, and rollback is available. If a measurement disproves one, I take the simplest credible alternative and retest — verifying by delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.",
        "explanation": "**Why this answer is correct:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n\n**In plain language:** The main thread renders and handles input; do not block it.\n\n**How it applies in production:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n\n**Evidence to ask for:** Checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement\n\n**What a complete answer includes:** Make and defend a real production decision with incomplete information.",
        "keyPoints": [
          "**Remember:** The main thread renders and handles input; do not block it.",
          "Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread.",
          "Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks."
        ],
        "realWorldExample": "🏭 **In production:** A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke.\n\n✅ **Why this is a strong answer:** It connects Browser JavaScript to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Browser JavaScript without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Browser JavaScript: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread."
            },
            {
              "label": "Protect",
              "detail": "Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-scenario-22",
      "level": "scenario",
      "kind": "scenario",
      "prompt": "Review this Web Fundamentals proposal for a commerce page used across mobile devices and assistive technology: A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts. Focusing on Web security, would you approve it, change it, or reject it? Your answer should make and defend a real production decision with incomplete information.",
      "skillsTested": [
        "Web security",
        "Scenario trade-offs",
        "Scenario reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would make the Web security decision conditionally, after its assumptions, failure behavior, and rollback are explicit. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Never trust the client; escape output and lock down the browser.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n- **Operation:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n- **Proof:** I would verify it by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Decision:** I would approve the direction only after making its assumptions explicit. I would confirm the workload shape, required guarantee, trust boundary, failure tolerance, recovery target, and cost ceiling. Then I would compare the proposal with the simplest credible alternative. The winning option is the one that meets those requirements with fewer failure modes, not the option with the longest feature list.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts. This names a concrete boundary and observable outcome rather than calling Web security “best practice.”\n\n**Depth and trade-off:** Because details are incomplete, I would state assumptions, choose a reversible starting point, and define evidence that invalidates it. If scale, failure, or security evidence changes, I would revise the design and rerun the checks.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls. My final rule is **Never trust the client; escape output and lock down the browser.**",
        "modelAnswer": "**Recommendation:** I would approve this direction, subject to the assumptions below. Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n\n**Production approach:** I would escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n\n**Concrete example:** A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts.\n\n**Alternative and proof:** I would reject a broader or more expensive option unless a measured requirement justifies it. I would prove this choice by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Assumptions and rollback:** My assumptions: the workload fits current Web security limits, the required guarantees match the mechanism, and rollback is available. If a measurement disproves one, I take the simplest credible alternative and retest — verifying by escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.",
        "explanation": "**Why this answer is correct:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n\n**In plain language:** Never trust the client; escape output and lock down the browser.\n\n**How it applies in production:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n\n**Evidence to ask for:** Checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement\n\n**What a complete answer includes:** Make and defend a real production decision with incomplete information.",
        "keyPoints": [
          "**Remember:** Never trust the client; escape output and lock down the browser.",
          "Web security depends on not trusting the client and controlling what the browser is allowed to do.",
          "Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins."
        ],
        "realWorldExample": "🏭 **In production:** A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts.\n\n✅ **Why this is a strong answer:** It connects Web security to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Web security without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Web security: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Web security depends on not trusting the client and controlling what the browser is allowed to do."
            },
            {
              "label": "Protect",
              "detail": "Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-scenario-25",
      "level": "scenario",
      "kind": "scenario",
      "prompt": "Review this Web Fundamentals proposal for a commerce page used across mobile devices and assistive technology: A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect. Focusing on Caching and CDNs, would you approve it, change it, or reject it? Your answer should make and defend a real production decision with incomplete information.",
      "skillsTested": [
        "Caching and CDNs",
        "Scenario trade-offs",
        "Scenario reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would make the Caching and CDNs decision conditionally, after its assumptions, failure behavior, and rollback are explicit. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Cache immutable assets hard; revalidate the documents that change.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n- **Operation:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n- **Proof:** I would verify it by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Decision:** I would approve the direction only after making its assumptions explicit. I would confirm the workload shape, required guarantee, trust boundary, failure tolerance, recovery target, and cost ceiling. Then I would compare the proposal with the simplest credible alternative. The winning option is the one that meets those requirements with fewer failure modes, not the option with the longest feature list.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect. This names a concrete boundary and observable outcome rather than calling Caching and CDNs “best practice.”\n\n**Depth and trade-off:** Because details are incomplete, I would state assumptions, choose a reversible starting point, and define evidence that invalidates it. If scale, failure, or security evidence changes, I would revise the design and rerun the checks.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely. My final rule is **Cache immutable assets hard; revalidate the documents that change.**",
        "modelAnswer": "**Recommendation:** I would approve this direction, subject to the assumptions below. Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n\n**Production approach:** I would fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n\n**Concrete example:** A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect.\n\n**Alternative and proof:** I would reject a broader or more expensive option unless a measured requirement justifies it. I would prove this choice by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Assumptions and rollback:** My assumptions: the workload fits current Caching and CDNs limits, the required guarantees match the mechanism, and rollback is available. If a measurement disproves one, I take the simplest credible alternative and retest — verifying by fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.",
        "explanation": "**Why this answer is correct:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n\n**In plain language:** Cache immutable assets hard; revalidate the documents that change.\n\n**How it applies in production:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n\n**Evidence to ask for:** Checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement\n\n**What a complete answer includes:** Make and defend a real production decision with incomplete information.",
        "keyPoints": [
          "**Remember:** Cache immutable assets hard; revalidate the documents that change.",
          "Browser and CDN caching avoid re-fetching unchanged resources.",
          "Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change."
        ],
        "realWorldExample": "🏭 **In production:** A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect.\n\n✅ **Why this is a strong answer:** It connects Caching and CDNs to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Caching and CDNs without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Caching and CDNs: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Browser and CDN caching avoid re-fetching unchanged resources."
            },
            {
              "label": "Protect",
              "detail": "Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-scenario-28",
      "level": "scenario",
      "kind": "scenario",
      "prompt": "Review this Web Fundamentals proposal for a commerce page used across mobile devices and assistive technology: A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully. Focusing on Progressive enhancement, would you approve it, change it, or reject it? Your answer should make and defend a real production decision with incomplete information.",
      "skillsTested": [
        "Progressive enhancement",
        "Scenario trade-offs",
        "Scenario reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would make the Progressive enhancement decision conditionally, after its assumptions, failure behavior, and rollback are explicit. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Working baseline first; enhancement is a bonus, not a requirement.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n- **Operation:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n- **Proof:** I would verify it by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Decision:** I would approve the direction only after making its assumptions explicit. I would confirm the workload shape, required guarantee, trust boundary, failure tolerance, recovery target, and cost ceiling. Then I would compare the proposal with the simplest credible alternative. The winning option is the one that meets those requirements with fewer failure modes, not the option with the longest feature list.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully. This names a concrete boundary and observable outcome rather than calling Progressive enhancement “best practice.”\n\n**Depth and trade-off:** Because details are incomplete, I would state assumptions, choose a reversible starting point, and define evidence that invalidates it. If scale, failure, or security evidence changes, I would revise the design and rerun the checks.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** The page still functions when a script fails, on slow networks, or for assistive technology. My final rule is **Working baseline first; enhancement is a bonus, not a requirement.**",
        "modelAnswer": "**Recommendation:** I would approve this direction, subject to the assumptions below. Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n\n**Production approach:** I would make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n\n**Concrete example:** A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully.\n\n**Alternative and proof:** I would reject a broader or more expensive option unless a measured requirement justifies it. I would prove this choice by checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement.\n\n**Assumptions and rollback:** My assumptions: the workload fits current Progressive enhancement limits, the required guarantees match the mechanism, and rollback is available. If a measurement disproves one, I take the simplest credible alternative and retest — verifying by make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.",
        "explanation": "**Why this answer is correct:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n\n**In plain language:** Working baseline first; enhancement is a bonus, not a requirement.\n\n**How it applies in production:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n\n**Evidence to ask for:** Checking the stated assumptions with representative load, failure injection, security review, recovery rehearsal, and cost measurement\n\n**What a complete answer includes:** Make and defend a real production decision with incomplete information.",
        "keyPoints": [
          "**Remember:** Working baseline first; enhancement is a bonus, not a requirement.",
          "Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences.",
          "Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled."
        ],
        "realWorldExample": "🏭 **In production:** A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully.\n\n✅ **Why this is a strong answer:** It connects Progressive enhancement to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Progressive enhancement without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Progressive enhancement: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences."
            },
            {
              "label": "Protect",
              "detail": "Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-scenario-02",
      "level": "scenario",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code. Focusing on Semantic HTML, how would you investigate without guessing? Your answer should lead an incident response from symptoms to evidence, mitigation, and prevention.",
      "skillsTested": [
        "Semantic HTML",
        "Evidence-based diagnosis",
        "Scenario reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the Semantic HTML failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Use the element whose built-in contract matches the job.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n- **Operation:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code. This names a concrete boundary and observable outcome rather than calling Semantic HTML “best practice.”\n\n**Depth and trade-off:** Because details are incomplete, I would state assumptions, choose a reversible starting point, and define evidence that invalidates it. If scale, failure, or security evidence changes, I would revise the design and rerun the checks.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Choose elements for purpose, then style them. My final rule is **Use the element whose built-in contract matches the job.**",
        "modelAnswer": "**Expected behavior:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** My assumptions: the workload fits current Semantic HTML limits, the required guarantees match the mechanism, and rollback is available. If a measurement disproves one, I take the simplest credible alternative and retest — verifying by use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions.",
        "explanation": "**Why this answer is correct:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n\n**In plain language:** Use the element whose built-in contract matches the job.\n\n**How it applies in production:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Lead an incident response from symptoms to evidence, mitigation, and prevention.",
        "keyPoints": [
          "**Remember:** Use the element whose built-in contract matches the job.",
          "Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs.",
          "Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions."
        ],
        "realWorldExample": "🏭 **In production:** A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code.\n\n✅ **Why this is a strong answer:** It connects Semantic HTML to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Semantic HTML without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-scenario-05",
      "level": "scenario",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks. Focusing on CSS layout, how would you investigate without guessing? Your answer should lead an incident response from symptoms to evidence, mitigation, and prevention.",
      "skillsTested": [
        "CSS layout",
        "Evidence-based diagnosis",
        "Scenario reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the CSS layout failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Let content size first; constrain only where necessary.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n- **Operation:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks. This names a concrete boundary and observable outcome rather than calling CSS layout “best practice.”\n\n**Depth and trade-off:** Because details are incomplete, I would state assumptions, choose a reversible starting point, and define evidence that invalidates it. If scale, failure, or security evidence changes, I would revise the design and rerun the checks.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Responsive design should follow content breakpoints rather than device names. My final rule is **Let content size first; constrain only where necessary.**",
        "modelAnswer": "**Expected behavior:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** My assumptions: the workload fits current CSS layout limits, the required guarantees match the mechanism, and rollback is available. If a measurement disproves one, I take the simplest credible alternative and retest — verifying by prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.",
        "explanation": "**Why this answer is correct:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n\n**In plain language:** Let content size first; constrain only where necessary.\n\n**How it applies in production:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Lead an incident response from symptoms to evidence, mitigation, and prevention.",
        "keyPoints": [
          "**Remember:** Let content size first; constrain only where necessary.",
          "CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space.",
          "Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions."
        ],
        "realWorldExample": "🏭 **In production:** A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks.\n\n✅ **Why this is a strong answer:** It connects CSS layout to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming CSS layout without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-scenario-08",
      "level": "scenario",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard. Focusing on Accessibility, how would you investigate without guessing? Your answer should lead an incident response from symptoms to evidence, mitigation, and prevention.",
      "skillsTested": [
        "Accessibility",
        "Evidence-based diagnosis",
        "Scenario reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the Accessibility failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Name it, focus it, announce it, and keep it operable.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n- **Operation:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard. This names a concrete boundary and observable outcome rather than calling Accessibility “best practice.”\n\n**Depth and trade-off:** Because details are incomplete, I would state assumptions, choose a reversible starting point, and define evidence that invalidates it. If scale, failure, or security evidence changes, I would revise the design and rerun the checks.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** WCAG is a testable baseline, not a substitute for user testing. My final rule is **Name it, focus it, announce it, and keep it operable.**",
        "modelAnswer": "**Expected behavior:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** My assumptions: the workload fits current Accessibility limits, the required guarantees match the mechanism, and rollback is available. If a measurement disproves one, I take the simplest credible alternative and retest — verifying by preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size.",
        "explanation": "**Why this answer is correct:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n\n**In plain language:** Name it, focus it, announce it, and keep it operable.\n\n**How it applies in production:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Lead an incident response from symptoms to evidence, mitigation, and prevention.",
        "keyPoints": [
          "**Remember:** Name it, focus it, announce it, and keep it operable.",
          "Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods.",
          "Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size."
        ],
        "realWorldExample": "🏭 **In production:** Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard.\n\n✅ **Why this is a strong answer:** It connects Accessibility to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Accessibility without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-scenario-11",
      "level": "scenario",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck. Focusing on HTTP and browsers, how would you investigate without guessing? Your answer should lead an incident response from symptoms to evidence, mitigation, and prevention.",
      "skillsTested": [
        "HTTP and browsers",
        "Evidence-based diagnosis",
        "Scenario reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the HTTP and browsers failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Follow the request from name lookup to pixels.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n- **Operation:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck. This names a concrete boundary and observable outcome rather than calling HTTP and browsers “best practice.”\n\n**Depth and trade-off:** Because details are incomplete, I would state assumptions, choose a reversible starting point, and define evidence that invalidates it. If scale, failure, or security evidence changes, I would revise the design and rerun the checks.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Caching and connection reuse can matter more than JavaScript micro-optimizations. My final rule is **Follow the request from name lookup to pixels.**",
        "modelAnswer": "**Expected behavior:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** My assumptions: the workload fits current HTTP and browsers limits, the required guarantees match the mechanism, and rollback is available. If a measurement disproves one, I take the simplest credible alternative and retest — verifying by use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays.",
        "explanation": "**Why this answer is correct:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n\n**In plain language:** Follow the request from name lookup to pixels.\n\n**How it applies in production:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Lead an incident response from symptoms to evidence, mitigation, and prevention.",
        "keyPoints": [
          "**Remember:** Follow the request from name lookup to pixels.",
          "A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites.",
          "Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays."
        ],
        "realWorldExample": "🏭 **In production:** A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck.\n\n✅ **Why this is a strong answer:** It connects HTTP and browsers to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming HTTP and browsers without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-scenario-14",
      "level": "scenario",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP. Focusing on Core Web Vitals, how would you investigate without guessing? Your answer should lead an incident response from symptoms to evidence, mitigation, and prevention.",
      "skillsTested": [
        "Core Web Vitals",
        "Evidence-based diagnosis",
        "Scenario reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the Core Web Vitals failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Load the main thing, respond quickly, move nothing unexpectedly.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n- **Operation:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP. This names a concrete boundary and observable outcome rather than calling Core Web Vitals “best practice.”\n\n**Depth and trade-off:** Because details are incomplete, I would state assumptions, choose a reversible starting point, and define evidence that invalidates it. If scale, failure, or security evidence changes, I would revise the design and rerun the checks.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Field data matters because lab hardware and networks are only controlled samples. My final rule is **Load the main thing, respond quickly, move nothing unexpectedly.**",
        "modelAnswer": "**Expected behavior:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** My assumptions: the workload fits current Core Web Vitals limits, the required guarantees match the mechanism, and rollback is available. If a measurement disproves one, I take the simplest credible alternative and retest — verifying by attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content.",
        "explanation": "**Why this answer is correct:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n\n**In plain language:** Load the main thing, respond quickly, move nothing unexpectedly.\n\n**How it applies in production:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Lead an incident response from symptoms to evidence, mitigation, and prevention.",
        "keyPoints": [
          "**Remember:** Load the main thing, respond quickly, move nothing unexpectedly.",
          "Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability.",
          "Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content."
        ],
        "realWorldExample": "🏭 **In production:** A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP.\n\n✅ **Why this is a strong answer:** It connects Core Web Vitals to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Core Web Vitals without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-scenario-17",
      "level": "scenario",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order. Focusing on Forms and validation, how would you investigate without guessing? Your answer should lead an incident response from symptoms to evidence, mitigation, and prevention.",
      "skillsTested": [
        "Forms and validation",
        "Evidence-based diagnosis",
        "Scenario reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the Forms and validation failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Validate near the user for feedback, at the server for trust.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n- **Operation:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order. This names a concrete boundary and observable outcome rather than calling Forms and validation “best practice.”\n\n**Depth and trade-off:** Because details are incomplete, I would state assumptions, choose a reversible starting point, and define evidence that invalidates it. If scale, failure, or security evidence changes, I would revise the design and rerun the checks.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted. My final rule is **Validate near the user for feedback, at the server for trust.**",
        "modelAnswer": "**Expected behavior:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** My assumptions: the workload fits current Forms and validation limits, the required guarantees match the mechanism, and rollback is available. If a measurement disproves one, I take the simplest credible alternative and retest — verifying by use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.",
        "explanation": "**Why this answer is correct:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n\n**In plain language:** Validate near the user for feedback, at the server for trust.\n\n**How it applies in production:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Lead an incident response from symptoms to evidence, mitigation, and prevention.",
        "keyPoints": [
          "**Remember:** Validate near the user for feedback, at the server for trust.",
          "HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility.",
          "Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server."
        ],
        "realWorldExample": "🏭 **In production:** A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order.\n\n✅ **Why this is a strong answer:** It connects Forms and validation to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Forms and validation without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-scenario-20",
      "level": "scenario",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke. Focusing on Browser JavaScript, how would you investigate without guessing? Your answer should lead an incident response from symptoms to evidence, mitigation, and prevention.",
      "skillsTested": [
        "Browser JavaScript",
        "Evidence-based diagnosis",
        "Scenario reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the Browser JavaScript failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **The main thread renders and handles input; do not block it.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n- **Operation:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke. This names a concrete boundary and observable outcome rather than calling Browser JavaScript “best practice.”\n\n**Depth and trade-off:** Because details are incomplete, I would state assumptions, choose a reversible starting point, and define evidence that invalidates it. If scale, failure, or security evidence changes, I would revise the design and rerun the checks.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness. My final rule is **The main thread renders and handles input; do not block it.**",
        "modelAnswer": "**Expected behavior:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** My assumptions: the workload fits current Browser JavaScript limits, the required guarantees match the mechanism, and rollback is available. If a measurement disproves one, I take the simplest credible alternative and retest — verifying by delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.",
        "explanation": "**Why this answer is correct:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n\n**In plain language:** The main thread renders and handles input; do not block it.\n\n**How it applies in production:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Lead an incident response from symptoms to evidence, mitigation, and prevention.",
        "keyPoints": [
          "**Remember:** The main thread renders and handles input; do not block it.",
          "Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread.",
          "Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks."
        ],
        "realWorldExample": "🏭 **In production:** A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke.\n\n✅ **Why this is a strong answer:** It connects Browser JavaScript to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Browser JavaScript without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-scenario-23",
      "level": "scenario",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts. Focusing on Web security, how would you investigate without guessing? Your answer should lead an incident response from symptoms to evidence, mitigation, and prevention.",
      "skillsTested": [
        "Web security",
        "Evidence-based diagnosis",
        "Scenario reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the Web security failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Never trust the client; escape output and lock down the browser.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n- **Operation:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts. This names a concrete boundary and observable outcome rather than calling Web security “best practice.”\n\n**Depth and trade-off:** Because details are incomplete, I would state assumptions, choose a reversible starting point, and define evidence that invalidates it. If scale, failure, or security evidence changes, I would revise the design and rerun the checks.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls. My final rule is **Never trust the client; escape output and lock down the browser.**",
        "modelAnswer": "**Expected behavior:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** My assumptions: the workload fits current Web security limits, the required guarantees match the mechanism, and rollback is available. If a measurement disproves one, I take the simplest credible alternative and retest — verifying by escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.",
        "explanation": "**Why this answer is correct:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n\n**In plain language:** Never trust the client; escape output and lock down the browser.\n\n**How it applies in production:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Lead an incident response from symptoms to evidence, mitigation, and prevention.",
        "keyPoints": [
          "**Remember:** Never trust the client; escape output and lock down the browser.",
          "Web security depends on not trusting the client and controlling what the browser is allowed to do.",
          "Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins."
        ],
        "realWorldExample": "🏭 **In production:** A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts.\n\n✅ **Why this is a strong answer:** It connects Web security to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Web security without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-scenario-26",
      "level": "scenario",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect. Focusing on Caching and CDNs, how would you investigate without guessing? Your answer should lead an incident response from symptoms to evidence, mitigation, and prevention.",
      "skillsTested": [
        "Caching and CDNs",
        "Evidence-based diagnosis",
        "Scenario reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the Caching and CDNs failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Cache immutable assets hard; revalidate the documents that change.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n- **Operation:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect. This names a concrete boundary and observable outcome rather than calling Caching and CDNs “best practice.”\n\n**Depth and trade-off:** Because details are incomplete, I would state assumptions, choose a reversible starting point, and define evidence that invalidates it. If scale, failure, or security evidence changes, I would revise the design and rerun the checks.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely. My final rule is **Cache immutable assets hard; revalidate the documents that change.**",
        "modelAnswer": "**Expected behavior:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** My assumptions: the workload fits current Caching and CDNs limits, the required guarantees match the mechanism, and rollback is available. If a measurement disproves one, I take the simplest credible alternative and retest — verifying by fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.",
        "explanation": "**Why this answer is correct:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n\n**In plain language:** Cache immutable assets hard; revalidate the documents that change.\n\n**How it applies in production:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Lead an incident response from symptoms to evidence, mitigation, and prevention.",
        "keyPoints": [
          "**Remember:** Cache immutable assets hard; revalidate the documents that change.",
          "Browser and CDN caching avoid re-fetching unchanged resources.",
          "Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change."
        ],
        "realWorldExample": "🏭 **In production:** A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect.\n\n✅ **Why this is a strong answer:** It connects Caching and CDNs to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Caching and CDNs without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-scenario-29",
      "level": "scenario",
      "kind": "troubleshooting",
      "prompt": "In a commerce page used across mobile devices and assistive technology, this expected outcome fails intermittently in production: A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully. Focusing on Progressive enhancement, how would you investigate without guessing? Your answer should lead an incident response from symptoms to evidence, mitigation, and prevention.",
      "skillsTested": [
        "Progressive enhancement",
        "Evidence-based diagnosis",
        "Scenario reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would diagnose the Progressive enhancement failure from evidence along the request or data path, not by changing settings blindly. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Working baseline first; enhancement is a bonus, not a requirement.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n- **Operation:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n- **Proof:** I would verify it by reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal.\n\n**Diagnosis:** I start with the user-visible symptom and its timeline, then compare one failing path with one healthy path. I follow the request or data flow in order and inspect evidence at each boundary before changing configuration. Once one observation disproves a layer, I move to the next. I change one variable, reproduce the original symptom, and keep the evidence that confirms the cause.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully. This names a concrete boundary and observable outcome rather than calling Progressive enhancement “best practice.”\n\n**Depth and trade-off:** Because details are incomplete, I would state assumptions, choose a reversible starting point, and define evidence that invalidates it. If scale, failure, or security evidence changes, I would revise the design and rerun the checks.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** The page still functions when a script fails, on slow networks, or for assistive technology. My final rule is **Working baseline first; enhancement is a bonus, not a requirement.**",
        "modelAnswer": "**Expected behavior:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n\n**Evidence first:** Confirm user impact and timeline, then compare one failing request or instance with a healthy one.\n\n**Narrow the cause:** I would make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n\n**Confirm and prevent:** Change one variable at a time. Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal. Add a regression check and alert on the signal that exposed the failure.\n\n**Trade-off and limit:** My assumptions: the workload fits current Progressive enhancement limits, the required guarantees match the mechanism, and rollback is available. If a measurement disproves one, I take the simplest credible alternative and retest — verifying by make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.",
        "explanation": "**Why this answer is correct:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n\n**In plain language:** Working baseline first; enhancement is a bonus, not a requirement.\n\n**How it applies in production:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n\n**Evidence to ask for:** Reproducing or isolating one symptom, narrowing it with ordered evidence, and confirming the fix with the original failing signal\n\n**What a complete answer includes:** Lead an incident response from symptoms to evidence, mitigation, and prevention.",
        "keyPoints": [
          "**Remember:** Working baseline first; enhancement is a bonus, not a requirement.",
          "Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences.",
          "Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled."
        ],
        "realWorldExample": "🏭 **In production:** A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully.\n\n✅ **Why this is a strong answer:** It connects Progressive enhancement to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Progressive enhancement without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ]
      }
    },
    {
      "id": "web-fundamentals-scenario-03",
      "level": "scenario",
      "kind": "design",
      "prompt": "For a commerce page used across mobile devices and assistive technology, how would you design the part of the system concerned with Semantic HTML using Web Fundamentals? The target outcome is: A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code. Your answer should whiteboard the end-to-end design, failure paths, security controls, cost, and rollback.",
      "skillsTested": [
        "Semantic HTML",
        "System design",
        "Scenario reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would design Semantic HTML around a clear boundary, predictable failure behavior, and evidence that proves the result. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Use the element whose built-in contract matches the job.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n- **Operation:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n- **Proof:** I would verify it by walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO.\n\n**Design:** I would give Semantic HTML a clear boundary: defined inputs and outputs, one owner, explicit authorization, bounded resource use, observable failure, and a rehearsed recovery path. I would walk a successful request through the design, then repeat the walk for invalid input, overload, dependency failure, and rollback. That exposes hidden coupling before production traffic does.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code. This names a concrete boundary and observable outcome rather than calling Semantic HTML “best practice.”\n\n**Depth and trade-off:** Because details are incomplete, I would state assumptions, choose a reversible starting point, and define evidence that invalidates it. If scale, failure, or security evidence changes, I would revise the design and rerun the checks.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Choose elements for purpose, then style them. My final rule is **Use the element whose built-in contract matches the job.**",
        "modelAnswer": "**Boundary:** I would give Semantic HTML a clear owner, inputs, outputs, failure behavior, and recovery path. Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n\n**Production controls:** I would use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n\n**Concrete example:** A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code.\n\n**How to prove it:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**Trade-off and redesign point:** My assumptions: the workload fits current Semantic HTML limits, the required guarantees match the mechanism, and rollback is available. If a measurement disproves one, I take the simplest credible alternative and retest — verifying by use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions.",
        "explanation": "**Why this answer is correct:** Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs. Choose elements for purpose, then style them.\n\n**In plain language:** Use the element whose built-in contract matches the job.\n\n**How it applies in production:** Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions. Verify with keyboard-only use and the browser accessibility tree.\n\n**Evidence to ask for:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**What a complete answer includes:** Whiteboard the end-to-end design, failure paths, security controls, cost, and rollback.",
        "keyPoints": [
          "**Remember:** Use the element whose built-in contract matches the job.",
          "Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs.",
          "Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions."
        ],
        "realWorldExample": "🏭 **In production:** A commerce product selector uses fieldset, legend, labels, and buttons, so it works with screen readers and without custom keyboard code.\n\n✅ **Why this is a strong answer:** It connects Semantic HTML to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Semantic HTML without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Semantic HTML: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Semantic HTML gives content native meaning, keyboard behavior, form semantics, and an accessibility tree before JavaScript runs."
            },
            {
              "label": "Protect",
              "detail": "Use headings in order, labels for controls, buttons for actions, links for navigation, and landmarks for page regions."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-scenario-06",
      "level": "scenario",
      "kind": "design",
      "prompt": "For a commerce page used across mobile devices and assistive technology, how would you design the part of the system concerned with CSS layout using Web Fundamentals? The target outcome is: A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks. Your answer should whiteboard the end-to-end design, failure paths, security controls, cost, and rollback.",
      "skillsTested": [
        "CSS layout",
        "System design",
        "Scenario reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would design CSS layout around a clear boundary, predictable failure behavior, and evidence that proves the result. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Let content size first; constrain only where necessary.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n- **Operation:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n- **Proof:** I would verify it by walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO.\n\n**Design:** I would give CSS layout a clear boundary: defined inputs and outputs, one owner, explicit authorization, bounded resource use, observable failure, and a rehearsed recovery path. I would walk a successful request through the design, then repeat the walk for invalid input, overload, dependency failure, and rollback. That exposes hidden coupling before production traffic does.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks. This names a concrete boundary and observable outcome rather than calling CSS layout “best practice.”\n\n**Depth and trade-off:** Because details are incomplete, I would state assumptions, choose a reversible starting point, and define evidence that invalidates it. If scale, failure, or security evidence changes, I would revise the design and rerun the checks.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Responsive design should follow content breakpoints rather than device names. My final rule is **Let content size first; constrain only where necessary.**",
        "modelAnswer": "**Boundary:** I would give CSS layout a clear owner, inputs, outputs, failure behavior, and recovery path. CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n\n**Production controls:** I would prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n\n**Concrete example:** A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks.\n\n**How to prove it:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**Trade-off and redesign point:** My assumptions: the workload fits current CSS layout limits, the required guarantees match the mechanism, and rollback is available. If a measurement disproves one, I take the simplest credible alternative and retest — verifying by prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.",
        "explanation": "**Why this answer is correct:** CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space. Responsive design should follow content breakpoints rather than device names.\n\n**In plain language:** Let content size first; constrain only where necessary.\n\n**How it applies in production:** Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions.\n\n**Evidence to ask for:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**What a complete answer includes:** Whiteboard the end-to-end design, failure paths, security controls, cost, and rollback.",
        "keyPoints": [
          "**Remember:** Let content size first; constrain only where necessary.",
          "CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space.",
          "Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions."
        ],
        "realWorldExample": "🏭 **In production:** A product grid uses auto-fit and minmax so cards reflow naturally from small phones to ultrawide screens without JavaScript width checks.\n\n✅ **Why this is a strong answer:** It connects CSS layout to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming CSS layout without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "CSS layout: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "CSS layout uses normal flow, flexbox for one-dimensional alignment, grid for two-dimensional placement, and intrinsic sizing to negotiate available space."
            },
            {
              "label": "Protect",
              "detail": "Prevent overflow with min-width: 0 on flexible children, use logical properties, test zoom and long content, and avoid fixed heights for text regions."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-scenario-09",
      "level": "scenario",
      "kind": "design",
      "prompt": "For a commerce page used across mobile devices and assistive technology, how would you design the part of the system concerned with Accessibility using Web Fundamentals? The target outcome is: Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard. Your answer should whiteboard the end-to-end design, failure paths, security controls, cost, and rollback.",
      "skillsTested": [
        "Accessibility",
        "System design",
        "Scenario reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would design Accessibility around a clear boundary, predictable failure behavior, and evidence that proves the result. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Name it, focus it, announce it, and keep it operable.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n- **Operation:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n- **Proof:** I would verify it by walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO.\n\n**Design:** I would give Accessibility a clear boundary: defined inputs and outputs, one owner, explicit authorization, bounded resource use, observable failure, and a rehearsed recovery path. I would walk a successful request through the design, then repeat the walk for invalid input, overload, dependency failure, and rollback. That exposes hidden coupling before production traffic does.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard. This names a concrete boundary and observable outcome rather than calling Accessibility “best practice.”\n\n**Depth and trade-off:** Because details are incomplete, I would state assumptions, choose a reversible starting point, and define evidence that invalidates it. If scale, failure, or security evidence changes, I would revise the design and rerun the checks.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** WCAG is a testable baseline, not a substitute for user testing. My final rule is **Name it, focus it, announce it, and keep it operable.**",
        "modelAnswer": "**Boundary:** I would give Accessibility a clear owner, inputs, outputs, failure behavior, and recovery path. Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n\n**Production controls:** I would preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n\n**Concrete example:** Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard.\n\n**How to prove it:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**Trade-off and redesign point:** My assumptions: the workload fits current Accessibility limits, the required guarantees match the mechanism, and rollback is available. If a measurement disproves one, I take the simplest credible alternative and retest — verifying by preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size.",
        "explanation": "**Why this answer is correct:** Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods. WCAG is a testable baseline, not a substitute for user testing.\n\n**In plain language:** Name it, focus it, announce it, and keep it operable.\n\n**How it applies in production:** Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size. Test keyboard, screen reader smoke flows, zoom, and automated rules.\n\n**Evidence to ask for:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**What a complete answer includes:** Whiteboard the end-to-end design, failure paths, security controls, cost, and rollback.",
        "keyPoints": [
          "**Remember:** Name it, focus it, announce it, and keep it operable.",
          "Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods.",
          "Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size."
        ],
        "realWorldExample": "🏭 **In production:** Checkout announces validation errors, moves focus only when necessary, and remains usable at 200% zoom with a keyboard.\n\n✅ **Why this is a strong answer:** It connects Accessibility to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Accessibility without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Accessibility: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Accessibility means people can perceive, operate, understand, and reliably use the interface with diverse input and output methods."
            },
            {
              "label": "Protect",
              "detail": "Preserve focus order, visible focus, contrast, names, error associations, reduced motion, and touch target size."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-scenario-12",
      "level": "scenario",
      "kind": "design",
      "prompt": "For a commerce page used across mobile devices and assistive technology, how would you design the part of the system concerned with HTTP and browsers using Web Fundamentals? The target outcome is: A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck. Your answer should whiteboard the end-to-end design, failure paths, security controls, cost, and rollback.",
      "skillsTested": [
        "HTTP and browsers",
        "System design",
        "Scenario reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would design HTTP and browsers around a clear boundary, predictable failure behavior, and evidence that proves the result. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Follow the request from name lookup to pixels.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n- **Operation:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n- **Proof:** I would verify it by walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO.\n\n**Design:** I would give HTTP and browsers a clear boundary: defined inputs and outputs, one owner, explicit authorization, bounded resource use, observable failure, and a rehearsed recovery path. I would walk a successful request through the design, then repeat the walk for invalid input, overload, dependency failure, and rollback. That exposes hidden coupling before production traffic does.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck. This names a concrete boundary and observable outcome rather than calling HTTP and browsers “best practice.”\n\n**Depth and trade-off:** Because details are incomplete, I would state assumptions, choose a reversible starting point, and define evidence that invalidates it. If scale, failure, or security evidence changes, I would revise the design and rerun the checks.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Caching and connection reuse can matter more than JavaScript micro-optimizations. My final rule is **Follow the request from name lookup to pixels.**",
        "modelAnswer": "**Boundary:** I would give HTTP and browsers a clear owner, inputs, outputs, failure behavior, and recovery path. A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n\n**Production controls:** I would use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n\n**Concrete example:** A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck.\n\n**How to prove it:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**Trade-off and redesign point:** My assumptions: the workload fits current HTTP and browsers limits, the required guarantees match the mechanism, and rollback is available. If a measurement disproves one, I take the simplest credible alternative and retest — verifying by use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays.",
        "explanation": "**Why this answer is correct:** A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites. Caching and connection reuse can matter more than JavaScript micro-optimizations.\n\n**In plain language:** Follow the request from name lookup to pixels.\n\n**How it applies in production:** Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays. Validate cache headers, compression, priorities, and third-party cost.\n\n**Evidence to ask for:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**What a complete answer includes:** Whiteboard the end-to-end design, failure paths, security controls, cost, and rollback.",
        "keyPoints": [
          "**Remember:** Follow the request from name lookup to pixels.",
          "A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites.",
          "Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays."
        ],
        "realWorldExample": "🏭 **In production:** A slow storefront is fixed by caching immutable assets and reducing origin TTFB after the network waterfall shows JavaScript execution was not the bottleneck.\n\n✅ **Why this is a strong answer:** It connects HTTP and browsers to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming HTTP and browsers without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "HTTP and browsers: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "A browser resolves DNS, establishes transport and TLS, sends HTTP, parses streamed bytes, builds DOM and CSSOM, lays out, paints, and composites."
            },
            {
              "label": "Protect",
              "detail": "Use DevTools waterfalls and server timing to locate DNS, connection, TTFB, transfer, or main-thread delays."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-scenario-15",
      "level": "scenario",
      "kind": "design",
      "prompt": "For a commerce page used across mobile devices and assistive technology, how would you design the part of the system concerned with Core Web Vitals using Web Fundamentals? The target outcome is: A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP. Your answer should whiteboard the end-to-end design, failure paths, security controls, cost, and rollback.",
      "skillsTested": [
        "Core Web Vitals",
        "System design",
        "Scenario reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would design Core Web Vitals around a clear boundary, predictable failure behavior, and evidence that proves the result. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Load the main thing, respond quickly, move nothing unexpectedly.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n- **Operation:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n- **Proof:** I would verify it by walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO.\n\n**Design:** I would give Core Web Vitals a clear boundary: defined inputs and outputs, one owner, explicit authorization, bounded resource use, observable failure, and a rehearsed recovery path. I would walk a successful request through the design, then repeat the walk for invalid input, overload, dependency failure, and rollback. That exposes hidden coupling before production traffic does.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP. This names a concrete boundary and observable outcome rather than calling Core Web Vitals “best practice.”\n\n**Depth and trade-off:** Because details are incomplete, I would state assumptions, choose a reversible starting point, and define evidence that invalidates it. If scale, failure, or security evidence changes, I would revise the design and rerun the checks.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Field data matters because lab hardware and networks are only controlled samples. My final rule is **Load the main thing, respond quickly, move nothing unexpectedly.**",
        "modelAnswer": "**Boundary:** I would give Core Web Vitals a clear owner, inputs, outputs, failure behavior, and recovery path. Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n\n**Production controls:** I would attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n\n**Concrete example:** A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP.\n\n**How to prove it:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**Trade-off and redesign point:** My assumptions: the workload fits current Core Web Vitals limits, the required guarantees match the mechanism, and rollback is available. If a measurement disproves one, I take the simplest credible alternative and retest — verifying by attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content.",
        "explanation": "**Why this answer is correct:** Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability. Field data matters because lab hardware and networks are only controlled samples.\n\n**In plain language:** Load the main thing, respond quickly, move nothing unexpectedly.\n\n**How it applies in production:** Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content. Segment real-user data by page and device, then set regression budgets.\n\n**Evidence to ask for:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**What a complete answer includes:** Whiteboard the end-to-end design, failure paths, security controls, cost, and rollback.",
        "keyPoints": [
          "**Remember:** Load the main thing, respond quickly, move nothing unexpectedly.",
          "Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability.",
          "Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content."
        ],
        "realWorldExample": "🏭 **In production:** A commerce page reserves image and promotion space, preloads only its true hero, and breaks a long click handler into smaller work, improving CLS, LCP, and INP.\n\n✅ **Why this is a strong answer:** It connects Core Web Vitals to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Core Web Vitals without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Core Web Vitals: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Core Web Vitals measure user experience: LCP for loading, INP for interaction responsiveness, and CLS for visual stability."
            },
            {
              "label": "Protect",
              "detail": "Attribute LCP to its resource and server path, INP to event and rendering work, and CLS to unsized or late content."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-scenario-18",
      "level": "scenario",
      "kind": "design",
      "prompt": "For a commerce page used across mobile devices and assistive technology, how would you design the part of the system concerned with Forms and validation using Web Fundamentals? The target outcome is: A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order. Your answer should whiteboard the end-to-end design, failure paths, security controls, cost, and rollback.",
      "skillsTested": [
        "Forms and validation",
        "System design",
        "Scenario reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would design Forms and validation around a clear boundary, predictable failure behavior, and evidence that proves the result. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Validate near the user for feedback, at the server for trust.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n- **Operation:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n- **Proof:** I would verify it by walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO.\n\n**Design:** I would give Forms and validation a clear boundary: defined inputs and outputs, one owner, explicit authorization, bounded resource use, observable failure, and a rehearsed recovery path. I would walk a successful request through the design, then repeat the walk for invalid input, overload, dependency failure, and rollback. That exposes hidden coupling before production traffic does.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order. This names a concrete boundary and observable outcome rather than calling Forms and validation “best practice.”\n\n**Depth and trade-off:** Because details are incomplete, I would state assumptions, choose a reversible starting point, and define evidence that invalidates it. If scale, failure, or security evidence changes, I would revise the design and rerun the checks.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted. My final rule is **Validate near the user for feedback, at the server for trust.**",
        "modelAnswer": "**Boundary:** I would give Forms and validation a clear owner, inputs, outputs, failure behavior, and recovery path. HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n\n**Production controls:** I would use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n\n**Concrete example:** A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order.\n\n**How to prove it:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**Trade-off and redesign point:** My assumptions: the workload fits current Forms and validation limits, the required guarantees match the mechanism, and rollback is available. If a measurement disproves one, I take the simplest credible alternative and retest — verifying by use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.",
        "explanation": "**Why this answer is correct:** HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility. Client validation improves feedback but never replaces server validation, because anything from the browser is untrusted.\n\n**In plain language:** Validate near the user for feedback, at the server for trust.\n\n**How it applies in production:** Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server.\n\n**Evidence to ask for:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**What a complete answer includes:** Whiteboard the end-to-end design, failure paths, security controls, cost, and rollback.",
        "keyPoints": [
          "**Remember:** Validate near the user for feedback, at the server for trust.",
          "HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility.",
          "Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server."
        ],
        "realWorldExample": "🏭 **In production:** A commerce checkout uses type=email and required for instant feedback but the server rejects a malformed address again before creating the order.\n\n✅ **Why this is a strong answer:** It connects Forms and validation to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Forms and validation without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Forms and validation: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "HTML forms submit data natively with built-in validation attributes, keyboard support, and accessibility."
            },
            {
              "label": "Protect",
              "detail": "Use appropriate input types and required/pattern attributes, associate labels and error messages, keep the native submit working, and re-validate every field on the server."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-scenario-21",
      "level": "scenario",
      "kind": "design",
      "prompt": "For a commerce page used across mobile devices and assistive technology, how would you design the part of the system concerned with Browser JavaScript using Web Fundamentals? The target outcome is: A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke. Your answer should whiteboard the end-to-end design, failure paths, security controls, cost, and rollback.",
      "skillsTested": [
        "Browser JavaScript",
        "System design",
        "Scenario reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would design Browser JavaScript around a clear boundary, predictable failure behavior, and evidence that proves the result. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **The main thread renders and handles input; do not block it.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n- **Operation:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n- **Proof:** I would verify it by walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO.\n\n**Design:** I would give Browser JavaScript a clear boundary: defined inputs and outputs, one owner, explicit authorization, bounded resource use, observable failure, and a rehearsed recovery path. I would walk a successful request through the design, then repeat the walk for invalid input, overload, dependency failure, and rollback. That exposes hidden coupling before production traffic does.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke. This names a concrete boundary and observable outcome rather than calling Browser JavaScript “best practice.”\n\n**Depth and trade-off:** Because details are incomplete, I would state assumptions, choose a reversible starting point, and define evidence that invalidates it. If scale, failure, or security evidence changes, I would revise the design and rerun the checks.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness. My final rule is **The main thread renders and handles input; do not block it.**",
        "modelAnswer": "**Boundary:** I would give Browser JavaScript a clear owner, inputs, outputs, failure behavior, and recovery path. Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n\n**Production controls:** I would delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n\n**Concrete example:** A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke.\n\n**How to prove it:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**Trade-off and redesign point:** My assumptions: the workload fits current Browser JavaScript limits, the required guarantees match the mechanism, and rollback is available. If a measurement disproves one, I take the simplest credible alternative and retest — verifying by delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.",
        "explanation": "**Why this answer is correct:** Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread. Heavy synchronous work blocks rendering and input, so long tasks show up as jank and poor responsiveness.\n\n**In plain language:** The main thread renders and handles input; do not block it.\n\n**How it applies in production:** Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks.\n\n**Evidence to ask for:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**What a complete answer includes:** Whiteboard the end-to-end design, failure paths, security controls, cost, and rollback.",
        "keyPoints": [
          "**Remember:** The main thread renders and handles input; do not block it.",
          "Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread.",
          "Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks."
        ],
        "realWorldExample": "🏭 **In production:** A commerce page keeps filtering responsive by debouncing the search input and updating the DOM in one batched write instead of on every keystroke.\n\n✅ **Why this is a strong answer:** It connects Browser JavaScript to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Browser JavaScript without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Browser JavaScript: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Browser JavaScript manipulates the DOM, handles events, and calls network APIs, all on the main thread."
            },
            {
              "label": "Protect",
              "detail": "Delegate events, batch DOM reads and writes to avoid layout thrash, debounce expensive handlers, and move heavy computation to a Web Worker or break it into smaller tasks."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-scenario-24",
      "level": "scenario",
      "kind": "design",
      "prompt": "For a commerce page used across mobile devices and assistive technology, how would you design the part of the system concerned with Web security using Web Fundamentals? The target outcome is: A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts. Your answer should whiteboard the end-to-end design, failure paths, security controls, cost, and rollback.",
      "skillsTested": [
        "Web security",
        "System design",
        "Scenario reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would design Web security around a clear boundary, predictable failure behavior, and evidence that proves the result. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Never trust the client; escape output and lock down the browser.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n- **Operation:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n- **Proof:** I would verify it by walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO.\n\n**Design:** I would give Web security a clear boundary: defined inputs and outputs, one owner, explicit authorization, bounded resource use, observable failure, and a rehearsed recovery path. I would walk a successful request through the design, then repeat the walk for invalid input, overload, dependency failure, and rollback. That exposes hidden coupling before production traffic does.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts. This names a concrete boundary and observable outcome rather than calling Web security “best practice.”\n\n**Depth and trade-off:** Because details are incomplete, I would state assumptions, choose a reversible starting point, and define evidence that invalidates it. If scale, failure, or security evidence changes, I would revise the design and rerun the checks.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls. My final rule is **Never trust the client; escape output and lock down the browser.**",
        "modelAnswer": "**Boundary:** I would give Web security a clear owner, inputs, outputs, failure behavior, and recovery path. Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n\n**Production controls:** I would escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n\n**Concrete example:** A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts.\n\n**How to prove it:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**Trade-off and redesign point:** My assumptions: the workload fits current Web security limits, the required guarantees match the mechanism, and rollback is available. If a measurement disproves one, I take the simplest credible alternative and retest — verifying by escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.",
        "explanation": "**Why this answer is correct:** Web security depends on not trusting the client and controlling what the browser is allowed to do. XSS, CSRF, and mixed content are the common failures; Content Security Policy, same-origin policy, and CORS are the browser controls.\n\n**In plain language:** Never trust the client; escape output and lock down the browser.\n\n**How it applies in production:** Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins.\n\n**Evidence to ask for:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**What a complete answer includes:** Whiteboard the end-to-end design, failure paths, security controls, cost, and rollback.",
        "keyPoints": [
          "**Remember:** Never trust the client; escape output and lock down the browser.",
          "Web security depends on not trusting the client and controlling what the browser is allowed to do.",
          "Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins."
        ],
        "realWorldExample": "🏭 **In production:** A commerce site blocks a reflected XSS attempt because it escapes user content and its CSP disallows inline scripts.\n\n✅ **Why this is a strong answer:** It connects Web security to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Web security without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Web security: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Web security depends on not trusting the client and controlling what the browser is allowed to do."
            },
            {
              "label": "Protect",
              "detail": "Escape output by context to prevent XSS, use anti-CSRF tokens or SameSite cookies, set a strict CSP, serve everything over HTTPS, and configure CORS to allow only trusted origins."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-scenario-27",
      "level": "scenario",
      "kind": "design",
      "prompt": "For a commerce page used across mobile devices and assistive technology, how would you design the part of the system concerned with Caching and CDNs using Web Fundamentals? The target outcome is: A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect. Your answer should whiteboard the end-to-end design, failure paths, security controls, cost, and rollback.",
      "skillsTested": [
        "Caching and CDNs",
        "System design",
        "Scenario reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would design Caching and CDNs around a clear boundary, predictable failure behavior, and evidence that proves the result. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Cache immutable assets hard; revalidate the documents that change.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n- **Operation:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n- **Proof:** I would verify it by walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO.\n\n**Design:** I would give Caching and CDNs a clear boundary: defined inputs and outputs, one owner, explicit authorization, bounded resource use, observable failure, and a rehearsed recovery path. I would walk a successful request through the design, then repeat the walk for invalid input, overload, dependency failure, and rollback. That exposes hidden coupling before production traffic does.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect. This names a concrete boundary and observable outcome rather than calling Caching and CDNs “best practice.”\n\n**Depth and trade-off:** Because details are incomplete, I would state assumptions, choose a reversible starting point, and define evidence that invalidates it. If scale, failure, or security evidence changes, I would revise the design and rerun the checks.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely. My final rule is **Cache immutable assets hard; revalidate the documents that change.**",
        "modelAnswer": "**Boundary:** I would give Caching and CDNs a clear owner, inputs, outputs, failure behavior, and recovery path. Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n\n**Production controls:** I would fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n\n**Concrete example:** A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect.\n\n**How to prove it:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**Trade-off and redesign point:** My assumptions: the workload fits current Caching and CDNs limits, the required guarantees match the mechanism, and rollback is available. If a measurement disproves one, I take the simplest credible alternative and retest — verifying by fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.",
        "explanation": "**Why this answer is correct:** Browser and CDN caching avoid re-fetching unchanged resources. Cache-Control, ETag, and immutable filenames control freshness; a wrong policy either serves stale data or defeats caching entirely.\n\n**In plain language:** Cache immutable assets hard; revalidate the documents that change.\n\n**How it applies in production:** Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change.\n\n**Evidence to ask for:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**What a complete answer includes:** Whiteboard the end-to-end design, failure paths, security controls, cost, and rollback.",
        "keyPoints": [
          "**Remember:** Cache immutable assets hard; revalidate the documents that change.",
          "Browser and CDN caching avoid re-fetching unchanged resources.",
          "Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change."
        ],
        "realWorldExample": "🏭 **In production:** A commerce site serves hashed CSS and JS with a one-year immutable cache while HTML revalidates, so repeat visits load instantly but deploys still take effect.\n\n✅ **Why this is a strong answer:** It connects Caching and CDNs to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Caching and CDNs without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Caching and CDNs: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Browser and CDN caching avoid re-fetching unchanged resources."
            },
            {
              "label": "Protect",
              "detail": "Fingerprint static assets and cache them immutably for a year, keep HTML short-lived or revalidated, use a CDN close to users, and validate with ETags where content may change."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    },
    {
      "id": "web-fundamentals-scenario-30",
      "level": "scenario",
      "kind": "design",
      "prompt": "For a commerce page used across mobile devices and assistive technology, how would you design the part of the system concerned with Progressive enhancement using Web Fundamentals? The target outcome is: A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully. Your answer should whiteboard the end-to-end design, failure paths, security controls, cost, and rollback.",
      "skillsTested": [
        "Progressive enhancement",
        "System design",
        "Scenario reasoning"
      ],
      "answer": {
        "spokenAnswer": "**Direct answer:** I would design Progressive enhancement around a clear boundary, predictable failure behavior, and evidence that proves the result. In Web Fundamentals, I would answer the requirement first, explain the mechanism, and state its operating limit clearly in production.\n\n**Mental model:** My shorthand is: **Working baseline first; enhancement is a bonus, not a requirement.** It keeps the boundary, guarantee, and operator visible instead of mistaking a service name for a complete design.\n\n**Key ideas:**\n\n- **Mechanism:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n- **Operation:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n- **Proof:** I would verify it by walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO.\n\n**Design:** I would give Progressive enhancement a clear boundary: defined inputs and outputs, one owner, explicit authorization, bounded resource use, observable failure, and a rehearsed recovery path. I would walk a successful request through the design, then repeat the walk for invalid input, overload, dependency failure, and rollback. That exposes hidden coupling before production traffic does.\n\n**Production example:** For a commerce page used across mobile devices and assistive technology, a commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully. This names a concrete boundary and observable outcome rather than calling Progressive enhancement “best practice.”\n\n**Depth and trade-off:** Because details are incomplete, I would state assumptions, choose a reversible starting point, and define evidence that invalidates it. If scale, failure, or security evidence changes, I would revise the design and rerun the checks.\n\n**Verification and caveat:** I would test the intended path, invalid or unauthorized input, one dependency failure, and recovery while watching the relevant telemetry. **Critical caveat:** The page still functions when a script fails, on slow networks, or for assistive technology. My final rule is **Working baseline first; enhancement is a bonus, not a requirement.**",
        "modelAnswer": "**Boundary:** I would give Progressive enhancement a clear owner, inputs, outputs, failure behavior, and recovery path. Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n\n**Production controls:** I would make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n\n**Concrete example:** A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully.\n\n**How to prove it:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**Trade-off and redesign point:** My assumptions: the workload fits current Progressive enhancement limits, the required guarantees match the mechanism, and rollback is available. If a measurement disproves one, I take the simplest credible alternative and retest — verifying by make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.",
        "explanation": "**Why this answer is correct:** Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences. The page still functions when a script fails, on slow networks, or for assistive technology.\n\n**In plain language:** Working baseline first; enhancement is a bonus, not a requirement.\n\n**How it applies in production:** Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled.\n\n**Evidence to ask for:** Walking through success, invalid input, saturation, dependency failure, recovery, authorization, observability, and the stated SLO\n\n**What a complete answer includes:** Whiteboard the end-to-end design, failure paths, security controls, cost, and rollback.",
        "keyPoints": [
          "**Remember:** Working baseline first; enhancement is a bonus, not a requirement.",
          "Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences.",
          "Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled."
        ],
        "realWorldExample": "🏭 **In production:** A commerce checkout works as a normal multi-page form submission, and client-side validation and single-page transitions enhance it only when JavaScript loads successfully.\n\n✅ **Why this is a strong answer:** It connects Progressive enhancement to a concrete outcome and explains what the team would observe to confirm the decision works safely.",
        "commonMistakes": [
          "Naming Progressive enhancement without explaining its mechanism, ownership, or limit.",
          "Describing only success and omitting failure behavior, security, observability, or recovery."
        ],
        "flow": {
          "title": "Progressive enhancement: requirement-to-proof flow",
          "steps": [
            {
              "label": "Clarify",
              "detail": "A commerce page used across mobile devices and assistive technology: state workload, correctness, security, scale, and recovery requirements."
            },
            {
              "label": "Choose",
              "detail": "Progressive enhancement builds a working baseline with HTML and server responses, then layers CSS and JavaScript for richer experiences."
            },
            {
              "label": "Protect",
              "detail": "Make core flows work with plain forms and links, enhance with JavaScript for those who have it, avoid making essential content depend on client rendering, and test with scripts disabled."
            },
            {
              "label": "Prove",
              "detail": "Test success, overload, dependency failure, recovery, and the user-visible latency or correctness target."
            }
          ]
        }
      }
    }
  ]
}
